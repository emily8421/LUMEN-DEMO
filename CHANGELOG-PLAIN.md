# CHANGELOG-PLAIN

本文是 LUMEN-DEMO 项目自身 `CHANGELOG.md` 的大白话同步版，按项目版本解释“这版实际改变了什么”。

- 项目版本入口：`VERSION`
- 正式项目 changelog：`CHANGELOG.md`
- 继承的母模板版本：`TEMPLATE-BASE.md`
- 母模板发布说明：`upstream/CHANGELOG.md` / `upstream/CHANGELOG-PLAIN.md`

历史上随模板同步带入的 `v1.x` 记录不再在本文重复展开；需要审计时看 `CHANGELOG.md` 的”历史模板同步记录”和 `sync-records/template-sync/`。

## v3.10.0（2026-08-17）

这版把「界面为什么显得乱」系统性治理了一遍，并落成了 LUMEN 第一份**前端设计规范文档**。

**治理了什么**：先给全部样式做了一次体检，把问题归成十类——字号有 22 种、圆角 9 种、字重 5 档（还有个非标的 650）、列表项和按钮全是白底方框、键盘焦点在 3 处根本看不见（无障碍缺陷）。然后照着 6 个公认优秀的网站（Linear、Notion、Sanity、Claude 等）的设计参数，把散乱的取值收敛成固定档位：字号 5 档、字重 3 档、圆角 4 档——现在打开界面，所有文字、按钮、卡片的规格都来自同一张「参数表」。

**观感上你会注意到**：标签、成员这类列表页不再是一行一个白框，而是像一张干净的表（行与行用细线分隔）；「取消」「返回」这类次要按钮从白底方块退成了文字级操作；输入框边框变淡但仍然清晰。整体从「满屏白盒子」变成主流工具软件那种「平铺 + 一层壳」的样子。顺带修了 3 处键盘用户看不到焦点位置的缺陷。

**防复乱三道闸**：① 规范文档（`frontend-design-system.md`，什么场景用什么规则一查便知）；② 参数单点（所有视觉参数只在一个文件里定义）；③ CI 自动拦（谁再往组件里写死字号、圆角、颜色，提交直接红灯）。以后 AI 生成界面代码也必须照这套规范来，不会再各写各的。

这份规范也是给母模板的「成功案例」候选——里面抽象出的通用规则（比如「一视图一容器」）可以在其他项目复用。

## v3.9.0（2026-08-16）

这次给 LUMEN 加上了「换肤」能力：右上角头像菜单里可以切换四套主题——**浅色**（默认，和原来观感基本一致但修好了两处对比度小毛病）、**深色**（夜间看文档用）、**暖米纸**（米白底 + 珊瑚橘点缀的纸感风格）、**原稿**（改造前的样子原样冻结，留作对照）。代码块在四套主题下统一固定深色，跟主流文档工具做法一致。

背后做的事比「换几个色」多：把散落在 28 个样式文件里 **202 处写死的颜色**全部收编进一个「色卡」文件（tokens.css），以后换主题只改色卡、不用挨个文件找。还配了一个 CI 自动检查——谁再往组件文件里写死颜色，提交时会直接红灯拦下。另外把这套规矩写进了项目规则（CSS 纪律 5 条），以后 AI 生成代码也有硬约束可依，不会再各写各的。

浅色主题还按用户实测反馈优化了层次感：背景加深了一小档、卡片加了极轻的投影，白色面板和背景的过渡更自然。全程按无障碍标准校验了对比度（正文可读性全部达标）。

这批同时是母模板「UI 知识层」的第一次实战试点：从参考案例分析、对比度计算、静态原型到双层评估，完整走了一遍新流程，评估结论（含 5 条可回流的通用经验）已落盘研究文档。下一个更大目标「前端设计系统规范」也在本批立项——目标是让整个界面的视觉规则都有据可查，AI 照规范生成代码。

## v3.8.27（2026-08-13）

最后一批、也是最复杂的一批：文档模块接入自动生成类型，整个「后端契约自动生成前端类型」工程至此**全部完成**。

文档模块特殊在哪：后端给「文档列表」时为了省流量不带正文，只有点开某一篇单独取详情时才带。以前前端类型把两种情况混成一个（正文可有可无），拿「有没有正文」当「详情加载好了没」的判断。这次老老实实分成「列表版 / 详情版」两种类型，判断改成明确问「是详情版吗」。

顺手修了一个历史小错：标签视图「标签下的文档」接口，后端返回的其实是个瘦身版，前端一直假装它是完整文档——这次按真实样子改了（界面上无任何变化，因为实际只用到标题和权限）。

最后上了保险栓：CI 里「自动生成类型是否和后端契约同步」的检查从「仅供参考」升级为「不同步就不许合并」。以后后端改接口、前端忘了重新生成类型，会直接红灯拦下。另外这次 CI 还真拦到一个小问题（一个文件多了一行超出瘦身基线），说明这套自动检查是好用的。

## v3.8.26（2026-08-13）

继续接入自动生成的前端类型（第四批）：这次接了账户和空间相关的 4 个模块——登录认证、空间列表与切换、空间成员管理、全局用户管理。原本计划 6 个，其中 LLM 配置那部分之前已随搜索模块做过，用户搜索本来就在成员模块里，所以这批实际 4 个。

几个值得说明的点：登录响应里的「当前空间 ID」后端类型标了可能为空，但实际登录用户必定有个人空间（注册时自动建），前端类型按实际情况收窄，保持和本地会话存储的校验一致；「角色」「状态」这类字段（管理员/成员、启用/禁用）仍手写保留精确限定。管理员用户空间抽屉里有层嵌套结构（已加入空间列表里的角色字段），做了嵌套收窄。

整体行为不变，纯类型层调整（打包产物逐字节没变）。只剩最后的文档模块（最复杂、要单独规划）。

## v3.8.25（2026-08-13）

继续接入自动生成的前端类型（第三批）：这次接了术语相关的 6 个模块——术语、术语领域、快速录入、AI 润色、导入、导出。原本计划 7 个，其中问答（rag）那部分上一批已经随搜索模块一起做完了，所以这批实际 6 个。

接法和前两批一样：数据主体用自动生成的类型，需要精确限定的（比如术语状态、导入结果状态、导出任务状态）手写保留。几个前后端命名对不上的地方做了映射（前端叫 ImportResponse 后端叫 ImportFileView 之类）；导入模块的批量结果里有个前端自己拼装失败条目的地方，补齐了对齐后端契约的三个空值字段。

整体行为不变，纯类型层调整。剩余账户空间 6 个模块 + 文档模块（最复杂）后续陆续接。

## v3.8.24（2026-08-13）

继续把后端 API 契约自动生成前端类型（Slice A 引入后的第二批）：这次接了 4 个模块——文件夹、文档链接、时间轴、搜索。接法和标签模块一样：数据主体用自动生成的类型，需要精确限定的（状态、事件类型、权限等）还是手写保留。

文件夹模块顺手清理了一个历史遗留的多余字段（后端本来不返回、前端手写多加了，没人用）。时间轴和搜索模块前后端命名对不上（前端叫 Result 后端叫 ResultView 之类），接的时候做了对应映射；时间轴还有「嵌套类型」（列表里的元素也要精确限定）需要一并处理。

整体行为不变，纯类型层调整。剩余 12 个模块 + 文档模块（最复杂）后续陆续接。

## v3.8.23（2026-08-13）

这版开始把后端 API 契约自动生成前端类型，取代之前前后端各自手写一份响应类型（容易不同步）：

- 引入 openapi-typescript：从后端 OpenAPI 描述自动生成前端 TypeScript 类型文件（generated.ts），后端改字段前端类型会自动跟上。
- 先拿「标签（tags）」模块试点：标签相关类型改成用自动生成的；标签状态（启用/归档）这类需要精确限定的仍手写保留编译期检查（因为 OpenAPI 描述里这些字段是普通字符串，全用生成的会丢掉精确性）。
- 加了一个 CI 检查：如果改了后端契约但忘了重新生成前端类型，CI 会报警（目前先不阻断，等全部模块接完再强制）。
- 文件大小检查放过了自动生成的文件（它本来就大、是机器写的，不该按手写文件标准限制）。

整体行为不变，纯类型层调整。剩余 16 个模块（含较复杂的文档模块）后续陆续接入。

## v3.8.22（2026-08-13）

这版继续拆分超长前端文件，把剩下 3 个超过 250 行的组件拆完（前端文件膨胀治理收尾）：

- 工作区主区（279 行）把「文档视图」那一段抽成单独文件。
- 文档编辑器（485 行）拆成「分隔条拖拽 / 撤销栈 / Markdown 工具栏 / 编辑表单 / 预览」几个小文件；编辑器的撤销、AI 选区、工具栏插入共用同一个输入框引用，集中放在编辑表单里。
- 本地挂载面板（563 行，最大）拆成「导入编排 / 头部 / 导入条 / 目录树 / 右键菜单 / 内联输入」几个小文件；目录树再把右键菜单和内联输入框抽出去，从 302 行降到 242。

至此超过 250 行的前端文件只剩 2 个核心状态 hook（有意保留，不再拆）。整体行为不变，纯结构调整。

## v3.8.21（2026-08-13）

这版拆分超长的前端组件文件，6 个超过 250 行的组件全部拆成小文件：

- 文档目录树（569 行）拆成「主树 / 文件夹节点 / 文档行」三份。
- 术语领域树（405 行）拆成「主树 / 领域节点」两份。
- 顶栏（274 行）拆成「顶栏 / 帮助弹层 / 用户菜单 / 栏开关」四份。
- 左栏面板（366 行）拆成「面板 / 文件头 / 术语区 / 上下文列表」四份。
- 批量导入弹窗（335 行）拆成「文件收集工具 / 拖拽区 / 结果列表」三份。
- 文档侧栏（266 行）拆成「主面板 / 版本 / 链接 / 标签」四份。
- 顺带删掉了文件夹树和术语树里「重复写了两遍」的改名输入框和右键菜单关闭逻辑（约 170 行重复代码）。
- 超限清单从 11 个减到 5 个；界面和功能完全不变。

## v3.8.20（2026-08-13）

这版拆分超长的样式文件，4 个超过 300 行的 CSS 全部拆成小文件：

- 主工作区样式（722 行）拆成「文件夹树 / 编辑面板 / 布局」三份。
- 本地挂载样式（589 行）拆成「面板 / 目录树 / 文档预览 / 右键菜单」四份。
- 整体布局样式（458 行）拆成「顶栏 / 工作区网格」两份。
- 新手引导样式（317 行）把「欢迎页清单」并入欢迎样式文件。
- 超限清单从 15 个减到 11 个（样式文件全部达标）；页面外观不变（样式打包后体积和之前完全一样）。

## v3.8.19（2026-08-13）

这版继续拆分超长的前端逻辑文件，把 4 个超过行数上限的「数据 hook / 工具」拆成多个小文件：

- 本地挂载的文件系统工具拆成 3 份（目录遍历 / 浏览器存储 / 授权读写）。
- 文件夹 hook 拆出「新建 / 重命名内联输入」小组件和纯计算函数。
- 文档 hook 拆出「版本 / 链接」侧数据和「下载 / 导出」动作。
- 本地挂载主 hook 拆出「编辑 / 增删改查」小组件。
- 超限清单从 18 个减到 15 个；文档 hook 因为 10 多个功能动作互相引用，硬拆会变成传参地狱，按前几版口径登记为例外。

## v3.8.18（2026-08-13）

这版继续给前端入口文件「瘦身」，并修正了文件大小检查的口径：

- App.tsx 从 359 行压到 90 行——把「组织各功能数据 + 跨功能动作」和「界面布局 + 弹窗开关」分别抽到两个独立文件，App 只负责拼装。
- 文件膨胀检查改为按文件类型区分上限：全局样式和主入口 300 行、页面 / 组件 250 行（和设计文档口径对齐）。
- 之前 19 个超限文件里有 2 个（App.tsx、AI 助手样式）降到上限内，从「超限清单」移除；App 编排层作为特例登记（原因：多个动作与数据互相引用，硬拆反而更绕）。

## v3.8.17（2026-08-13）

这版把前端最大的入口文件"瘦身"，并加了一道防"文件膨胀"的检查：

1. **App 瘦身**：之前前端总入口 `App.tsx` 546 行，又管状态又写界面，太臃肿。现在把界面拆成 3 个专门文件——登录/注册面板、主工作区、悬浮层（命令面板 + AI 助手）——`App.tsx` 降到 360 行，只管"调度"。
2. **防膨胀检查**：新增一个检查：以后任何前端文件超过 250 行（或者已经超限的文件继续变长），代码检查会直接报错。已经超限的 19 个文件登记为"存量"，只许降不许涨。

纯前端工程治理：不改页面功能、不加依赖、不影响后端。

## v3.8.16（2026-08-13）

这版把后端的环境变量读取集中到了一处，并给生产环境加了一道安全校验：

1. **配置集中**：之前 `DATABASE_URL`（数据库连接）、`LUMEN_ENV`（运行环境）、`LUMEN_DEMO_TOKEN_KEY`（登录签名钥匙）分散在几个文件里各读各的；现在统一收进 `backend/config.py`，一处定义、类型化读取，改配置只动一个文件。
2. **生产安全校验**：登录 token 的签名钥匙如果还是默认的弱值，生产环境（`LUMEN_ENV=production`）会直接拒绝启动——防止钥匙可预测、token 被伪造。本地开发 / 测试 / 演示不受影响（本就不是对外暴露面）。

纯后端工程治理：不改接口语义、不动数据库、不加业务功能。新引入一个依赖 `pydantic-settings`（读取配置的标准工具）。

## v3.8.15（2026-08-13）

这版统一了后端日志：之前还有个别地方用 `print` 打日志（没有时间戳 / 级别 / 来源，排障看不到），有些异常被"悄悄吞掉"（捕获后什么都不记）。现在：

1. **`print` 全换 `logger`**：最后一处 `print`（embedding 加载失败时的降级提示）也换成了正式日志，后端生产代码不再有 `print`。
2. **给"悄悄吞"的异常补日志**：文档索引失败、AI 问答降级、登录时请求无权限空间回退这几处，现在都会记一条 warning 日志说明原因——出问题时日志里有线索可查。
3. **加了"禁 print"的检查**：以后新代码再写 `print`，代码检查会直接报错，逼大家用正式日志（`scripts/` 里的运行 / smoke 脚本除外，它们打印输出是正常用途）。

纯后端工程治理：不改接口语义、不加依赖、不动数据库。

## v3.8.14（2026-08-13）

这版给后端启动加了"硬性自检"和"健康探针"：之前后端启动时如果连不上数据库，只是 `print` 一句话就继续跑，接口真正调用时才报错（带病运行）；容器编排也分不清后端"进程活着"和"真的能服务"。现在：

1. **启动失败就停**：生产 / PG 模式下数据库初始化失败，后端直接拒绝启动并记日志——不再带病运行；纯 demo（内存）模式仍容忍，方便本地无库启动。
2. **健康探针**：新增 `GET /api/health/live`（进程活着就 200）和 `GET /api/health/ready`（数据库连得上才 200，连不上 503 + 业务码 5031），供容器编排和运维探活。
3. **业务码不混用**：数据库未就绪用新码 5031，和"AI / OCR 服务不可用"的 5030 区分开，一个码一个意思。

纯后端工程治理：不改接口语义、不加依赖、不动数据库。顺带把启动日志里的 `print` 换成了正式 `logger`；生产 `docker-compose.prod.yml` 后端容器也接上了 `/api/health/ready` 健康检查。

## v3.8.13（2026-08-13）

这版给后端每个接口补上了“正式的数据长相”（`response_model`）：之前 64 个接口全都没有明确响应契约，路由靠手工拼字典返回，前端照着后端手抄一份类型，后端哪天加了字段 / 改了可空性，前端没人提醒，运行到那一步才暴露。现在：

1. **接口有“身份证”**：62 个 JSON 接口全部标注明确的响应模型，返回的数据必须符合模型，不符合直接报错——不再“悄悄不对”。
2. **说明书入库**：把整份接口说明书（OpenAPI）导出成 `openapi/openapi.json` 固定下来。
3. **CI 防漂移**：新增 `schema-diff` 检查，改接口必须同步更新说明书，否则 CI 报错——后端加字段、前端跟着改，从此有机器契约兜底。

纯后端工程治理：不改接口语义、不加依赖、不动数据库。二进制下载接口（md / zip / pdf）不走 JSON 信封，明确不纳入。前端“用说明书自动生成类型”（codegen，需要新装一个工具包）留作后续候选。

## v3.8.12（2026-08-13）

这版给“谁能看到哪些文档”做了一次安全收口：以前列表 / 搜索 / 问答等查询路径是先把库里所有文档读出来、再在内存里按权限过滤，每处调用都要记得过滤，漏一次就可能越权；现在把“只返回当前用户在当前空间有权限的文档”下沉到数据层做成默认查询，8 处用户查询路径统一走这一个入口，由数据层直接按空间 + 权限过滤。权限规则与之前完全一致、行为不变，只是少了一个“漏调就出安全问题”的口子。纯后端重构，不新增功能、不改接口、不加依赖。

## v3.8.11（2026-08-12）

这版给后端补了一个”类型检查器”（mypy）——之前后端只有 ruff（查代码规范）和 pytest（查行为），没人查”类型对不对”（比如函数声明接收 `int`，调用方却传了 `int | None`，运行时可能出 None bug）。这次补上，复刻之前给前端加 ESLint 的”先软后硬”三步走：

1. **装检查器 + 摸清家底**（Slice A）：引入 mypy，首跑发现 **190 个类型问题**。
2. **揪出真问题 + 清存量**（Slice B，分两刀）：
   - 最有价值的一刀：发现 **45 处 `current_space_id`（可能为 None）传给了声明”必填 int”的函数**——这是真实的类型契约缺口（”未选空间时调文档操作”的 None 路径没被后端处理），ruff 和编译器都抓不到，mypy 一跑就抓到。修法是把入口的 `TokenContext.current_space_id` 收紧成”必填 int”，并在鉴权入口拦截 None（要求重新登录选空间）。
   - 顺手修了 ~20 个真实类型 bug（`re.match` 结果可能 None 却直接取值、SQLAlchemy `rowcount` 用法、几处”更新后可能 None”没守卫等）。
   - 清掉 120 处”以防 fastapi 没装”的死代码（19 个文件里 `try: import fastapi except: 用假的顶上`——fastapi 是必装依赖，这段从来跑不到，代码自己都标了”覆盖不到”）。
3. **CI 接入**：加 `backend-typecheck` 检查任务到 CI，从”不阻断”起步，清完 190 个问题后升为”必须通过”。
4. **验证**：类型检查 **190→0 问题** + ruff 通过 + 307 个测试全过（新增 1 个”None 拦截”单测）+ CI 检查任务绿灯。

> 版本从 3.8.10 升到 3.8.11（PATCH——纯工程治理引入类型检查工具，不改任何功能）。详见 `CHANGELOG.md`。

## v3.8.10（2026-08-12）

这版给前端补了一个"代码检查器"（ESLint）——之前只有后端有（ruff），前端只靠编译器（tsc）查类型，查不了"写法不规范 / 没用到的变量 / React Hook 用错了"这类问题。这次补上：

1. **装检查器 + 规则**：引入 ESLint + TypeScript / React Hooks 规则插件，配了一套偏严但聪明的规则（避开和 tsc 重复检查）。
2. **揪出一个真 bug**：检查器首跑发现 `QuickEntryFeature` 组件有个**条件调用 Hook 的真 bug**（在 `if (!isOpen) return` 提前返回之后才调 `useRef`/`useEffect`，违反 React Hook 规则，会导致运行时崩溃）——编译器（tsc）完全没发现，检查器一跑就抓到了。这正是引入它的核心价值。
3. **存量清理**：首跑共 10 个问题（5 错 + 5 警告），全部修完——1 个真 bug + 3 个写法小瑕疵（机械修）+ 5 个 Hook 依赖警告（部分补依赖、部分是有意为之加注释说明、1 个性能改进把列表改成记忆化）。
4. **CI 接入**：加了 `frontend-lint` 检查任务到 CI，从"不阻断"（advisory）起步，清完存量后升为"必须通过"（required）。
5. **验证**：检查 0 问题 + 前端构建 301 模块零回归 + CI 检查任务绿灯。

> 版本从 3.8.9 升到 3.8.10（PATCH——纯工程治理引入检查工具，不改任何功能）。详见 `CHANGELOG.md`。

## v3.8.9（2026-08-12）

这版是给「代码卫生检查」来一次大扫除——把积压的 37 条"不够讲究"的写法一次性清完，没有任何功能变化：

1. **两类旧账**：一类是"导入了从没用过的东西、声明了从没读过的变量"（26 条）——用工具自动删掉多余部分，该执行的代码照旧执行；另一类是"import 语句没按规定放在文件开头"（11 条）——手动挪到文件开头，一字不改。
2. **中途发现一个坑**：有个叫"签名密钥"的常量，检查器认为它"没用"，删掉后测试立刻崩了——原来测试一直在借它用。把它放回去，并打个"这是对外借出的、别删"的标记。
3. **验证**：检查数从 37 降到 0；平时跑的 306 个测试全部照常通过。

> 版本从 3.8.8 升到 3.8.9（PATCH——纯代码卫生整理，不改任何功能）。详见 `CHANGELOG.md`。

## v3.8.8（2026-08-12）

这版给「自动检查」装上了最后一扇门：让那 48 个连真实数据库的测试在每次提交时都被自动跑一遍。还是代码质量治理，没有新功能：

1. **以前这些测试只在你自己电脑上跑**：CI 的检查机器不跑它们，所以上次修好的 7 个数据库 bug 万一又改回去，机器不会发现。这版给 CI 加了一个新检查任务（`backend-integration`）：每次提交自动开一个临时数据库，把这 48 个测试完整跑一遍，跑挂就拦下这次提交。
2. **特意做了三道保险，防止「假装通过」**：数据库没起来 → 直接报错；测试库没建 → 自动补建；连不上或插件缺失 → 直接失败。绝不出现「全部跳过、却显示绿灯」的假象。
3. **顺手把配套文档和版本号更新了**（v3.8.7 → v3.8.8）。以后 merge 前人工核对这个新检查是绿的（流程约定；GitHub 免费版私有仓库不提供「必过」的强制开关）。

> 版本从 3.8.7 升到 3.8.8（PATCH——纯 CI 配置，不改任何功能）。验证：本地 48 个集成测试全过 + 平时 306 个单元测试没被改坏 + 故意制造故障时检查会如实报错（不会假装通过）。详见 `CHANGELOG.md`。

## v3.8.7（2026-08-12）

这版把上次摸出来的 7 个数据库旧账全部修掉，还是代码质量治理，没有新功能：

1. **问题根子就一个**：代码在「把数据真正写进数据库」之前，就急着去读它——于是拿回来的编号是空的、时间戳是个空壳。上次改事务时发现了一批同类问题，这版把这一类全部排查补掉（一个文件里补了 17 处「先落库再读」）。
2. **顺手纠正一个记错的**：上次登记有个失败是「AI 服务环境没配好」，实际查下来不是环境问题，是同一个「编号没落库」的 bug——AI 服务在测试里本来就被模拟了，跟环境无关。
3. **验证**：这 7 个测试全过；平时跑的 306 个测试也没被改坏；代码规范检查没有新增告警。

## v3.8.6（2026-08-12）

这版做 P1 第三项（CQ-P1-003）：把「多步骤写数据库」的操作统一捆进事务，并证明事务真的会回滚。还是没有新功能，是代码质量治理：

1. **给「写库」套上事务（Slice A + B）**：以前创建 / 更新文档、导入文本，每写一小步就单独存一次账——中间一步出错，前面已存的改不回来，数据库会留下半截子数据。这版把「一个业务操作」的多步写捆成一个事务：要么全部成功一起存，要么全都不算数。导入任务记录和失败标记特意放在事务外面，保证「任务要么标完成、要么标失败」不会因为回滚而丢失；还顺手删掉了一处把分块写了两遍的重复代码。
2. **证明回滚真的有效（Slice C）**：写了一个「故意搞破坏」的测试——导入中途故意让一个环节报错，然后查数据库确认：文档没留下、任务没乱标。这个测试要连真实数据库跑，所以单独标记，平时默认不跑。
3. **顺手修好一个隐藏 bug**：建「导入任务」记录时，代码拿不到它的编号（拿回来是空的），导致真实数据库下导入功能会崩。原因是数据库新记录的编号要「先落库才分配」，代码没等落库就去读了。补了一行「先落库」，修好了——这也顺带暴露了一类潜在问题（建记录拿编号），已登记技术债，以后统一排查。
4. **摸清一批旧账**：因为这次连了真实数据库，把整套数据库测试第一次完整跑了一遍，发现 7 个以前从没跑过的存量失败（不是这次改出来的）——有建「术语」也拿不到编号的同类问题、日期格式的坑、AI 服务环境没配好。已登记为技术债，以后单独处理。

> 版本从 3.8.5 升到 3.8.6（PATCH——纯事务重构，不改对外接口）。验证：后端全量 306 个单元测试全过（零回归）+ ruff 旧债维持 37 不增 + 新回滚测试连真实数据库通过 + PR CI 全绿。**CQ-P1-003（事务边界）至此全部完成**。详见 `CHANGELOG.md`。

## v3.8.5（2026-08-12）

这版把 repository（持久化层）的接口契约补全到 service 层（P1 第二项 CQ-P1-002 的 Slice C）。还是没有新功能，是给代码补类型标注，让编辑器 / IDE 能识别 repository 是什么：

1. **给 service 层所有用 repository 的函数贴上类型标签**：以前 service 层每个函数的 `repository` 参数是裸的（没写类型），编辑器不知道它是什么，敲 `repository.` 不会提示有哪些方法。这版给 17 个 service 文件里约 80 个这样的参数补上 `: RepositoryProtocol` 类型注解，持久化层单例入口也标了类型——编辑器现在能补全、跳转、划红线报错。代码怎么跑完全没变（注解是给工具看的，不是给程序看的）。
2. **顺手清掉两处「假装某方法可能不存在」的死兜底**：timeline 和 search 里原来写着「万一 repository 没有这个方法怎么办」的防御代码，实际上这个方法一定存在（有测试守护），属于多余。这版改成直接调用，代码更诚实。
3. **原本计划的「按业务域拆分大接口」评估后搁置**：诊断时发现，绝大多数 service 一用就跨好几个业务域（最极端的 timeline 跨 7 个），硬拆反而会把「万能接口」从 repository 转嫁到 service 函数签名上；而且测试早就有一个内存版假对象（DemoRepository）当替身，「测试要造假很麻烦」这个痛点本来就不疼。所以拆分搁置，留作以后候选。

> 一个诚实的提醒：项目目前没装类型检查工具（没 mypy / pyright，CI 也不查类型），所以这版补的类型标注**眼下主要让编辑器更聪明**（写代码时能补全 / 报错），CI 不会因为「repository 用错了」而拦你。要真正让 CI 自动抓这类错误，得另外装类型检查工具——那是后续候选。版本从 3.8.4 升到 3.8.5（PATCH——纯类型标注，不改对外接口）。验证：后端全量 301 个单元测试全过（零回归）+ repository 契约测试 4 个全过 + ruff 旧债维持 37 不增 + PR 的 CI 全绿。**CQ-P1-002（repository 契约）至此全部完成**。详见 `CHANGELOG.md`。

## v3.8.4（2026-08-11）

这版把错误契约收口收尾（P1 第一项 CQ-P1-005 的最后两刀：B-6 + Slice C）。还是没有新功能，是把后端兜底分支的「业务码 / HTTP 码混用」和前端「靠错误文案猜认证失效」两个最后的缺口补上：

1. **后端兜底分支不再把 HTTP 码当业务码**：以前 `main.py` 的兜底错误处理里，没有业务码的错误（比如路由不存在的 404）会把 HTTP 状态码（404）直接塞进响应的 `code` 字段，跟有业务码的错误（`code: 4004`）语义打架。这版加了反向映射，HTTP 码自动转对应业务码（404→4004），`code` 字段从此只有一种含义。顺带把内部错误详情改成只进日志、不回传前端。
2. **前端不再靠文案猜「登录失效」**：以前前端拿到后端错误后只保留了错误文案（字符串），丢失了业务码，只能靠正则匹配文案（"invalid token" 之类）判断是不是登录失效——后端文案一改前端就坏。这版前端错误对象保留了业务码，登录失效改靠 `code === 4001` 判定，不再依赖文案。

> 这版是 CQ-P1-005 错误契约收口的最后一刀（版本从 3.8.3 升到 3.8.4，PATCH——纯重构不改对外接口语义）。验证：后端全量 297 个单元测试全过（零回归）+ ruff 旧债维持 37 不增 + 前端 tsc + vite build 全绿 + 2 个 PR 的 CI 全绿。**CQ-P1-005（错误契约收口）至此全部完成**——后端契约 + 兜底 + 前端解耦文案依赖全链路打通。详见 `CHANGELOG.md`。

## v3.8.3（2026-08-11）

这版把错误契约收口推进到后端最后几块（P1 第一项 CQ-P1-005 的 Slice B-5）。还是没有新功能，是把剩下 5 个域（space / auth / admin / space_members / users）的领域异常和 api 层的「状态码手工映射」也收进统一契约，并把 CI 单元测试门没覆盖到的集成测试断言一并清扫干净：

1. **5 个域的异常接入统一契约**：space（空间访问权限）、auth（登录 / 注册 / token 刷新 / 重置密码）、admin（后台用户管理）、space_members（空间成员）、users（用户）各自的异常统一继承 `ApiError`，自带业务码（4003/4220 等）。api 层不再每个端点手写「转 HTTP + 拼错误信息」，异常冒泡到统一网关自动转标准外壳 `{code,msg,data}`。
2. **删掉 api 层散落的「状态码映射」样板**：auth / admin / space_members / users 几处各自写的 `_status_for` / `_http_error`（把业务码翻译成 HTTP 码的小函数）一并删除，改走集中映射表——业务码到 HTTP 码的对应关系从此只有一处权威来源，不再各写各的。
3. **顺带清扫集成测试断言**：前几批改错误契约时，有些集成测试（CI 单元测试门不跑、只在本地跑）还在断言旧的错误响应写法。这版把它们一起改成断言新的统一契约，修了 7 处遗留。

> 这版是 B-5 全部做完后的聚合（版本从 3.8.2 升到 3.8.3，PATCH——纯重构不改对外接口语义，响应码和文案跟以前一致）。验证：全量 294 个后端单元测试全过（零回归）+ 47 个集成测试全过 + ruff 旧债维持 37 不增 + 4 个 PR 的 CI 全绿。**错误契约收口的「后端域迁移」至此全部完成**；下一刀 B-6 收 `main.py` 兜底分支的 code 二义，再下一刀 Slice C 才动前端错误处理。详见 `CHANGELOG.md`。

## v3.8.2（2026-08-11）

这版把 Slice A 打好的错误响应地基真正铺开（P1 第一项 CQ-P1-005 的 Slice B）。还是没有新功能，就是把后端散在各处的约 35 个领域异常全部改成统一继承「错误契约基类」，并删掉 api 层手工捕获、手工转 HTTP 的样板代码：

1. **约 35 个领域异常全部接入统一契约**：folder / tag / term / term_category / document / quick_entry / doc_links / timeline / search / rag / export / imports 各域的异常（如"没权限""没找到""参数不对""重名冲突"）统一继承 `ApiError`，自带业务码（4003/4004/4220/4090 等），抛异常时不再需要 api 层每个端点手写 `try/except + 转 HTTP + 拼错误信息`——异常一路冒泡到统一网关自动转成标准外壳 `{code,msg,data}`。
2. **顺手消掉了一处"猜文案"反模式**：导入（import）接口以前用一个异常同时承担"没权限(403)"和"参数不对(422)"两种错误，靠判断错误文案来区分。这版改成让"没权限"直接抛空间权限异常（4003），各归各的码，不再靠猜文案。
3. **api 层删掉了约 60 处样板 except**，错误响应从"每个端点各写各的"收敛成"异常自带语义 + 统一网关兜底"。

> 这版是 Slice B 全部做完后的聚合（版本从 3.8.1 升到 3.8.2，PATCH——纯重构不改对外接口语义，响应码和文案跟以前一致）。验证：7 批各域测试全过 + 全量 294 个后端测试全过（零回归）+ ruff 旧债从 41 降到 39（不增反降）+ 7 个 PR 的 CI（Linux 测试 / 前端构建 / 项目检查）全绿。下一刀 B-5 收 auth/admin/space 几个域 + 统一"状态码映射"；B-6 收 main.py 兜底分支；再下一刀 Slice C 才动前端错误处理。详见 `CHANGELOG.md`。

## v3.8.1（2026-08-11）

这版开始收拾前后端「错误响应」的契约（P1 第一项 CQ-P1-005 的 Slice A）。没有新功能，但为以后前后端能稳定处理错误打地基：

1. **后端错误响应有了统一规矩**（NFR-007）：以前各种错误响应写法比较乱——业务码 `code` 有时和 HTTP 状态码混着用、服务端没捕获到的异常会返回一个不带标准外壳的裸 500、错误文案有时直接把内部异常原文（`str(exc)`）透传出去可能泄露内部细节。这版建了集中错误码表（`error_codes.py`，对齐 API 文档 §1 的权威码表）+ 统一映射，并加了两个「错误网关」：领域异常 `ApiError` 自动转标准外壳 `{code,msg,data}`；任何漏网没处理的异常统一兜底成 `{code:5000, msg:"internal error"}`（不把堆栈 / 内部路径回传给前端，只写进日志）。

> 这版是契约地基（版本从 3.8.0 升到 3.8.1，PATCH——纯重构不改对外接口语义）。地基先行，下一刀（Slice B）才把现在散在各处的约 60 处 `str(exc)` 直传和约 40 个领域异常逐步迁过来；前端错误处理（Slice C）再下一刀。验证：新增 8 个单测 + 全量 294 个后端测试全过（零回归）+ ruff 旧债不增加 + CI 在 Linux 上 294 测试通过。详见 `CHANGELOG.md`。

## v3.8.0（2026-08-11）

这版做了一批「让代码更安全、让持续集成真正把关」的工程治理，没有新功能，但以后改代码会更安心；顺带修了两个 CI 才发现的潜伏 bug：

1. **集成测试不会再误清开发数据库了**（P0-1 / NFR-005）：之前跑数据库集成测试理论上存在把开发库（`lumen`）误清空的风险——测试和开发用同一个库。这版建了独立测试库 `lumen_test`，并加三重保险：只有「测试环境标记 + 库名以 `_test` 结尾 + 显式开启破坏性开关」三个条件同时满足才允许跑，缺一个直接报错（不跳过）。默认跑单元测试时数据库集成测试自动排除。
2. **CI 终于有真正的代码门了**（P0-2 / NFR-006）：以前 CI 只检查版本号和文档格式，不跑测试。这版加了三道门——后端单元测试、前端构建、后端代码 lint（ruff）。前两道设成「必须通过」，lint 暂作参考（记旧债基线 41 处，不阻断）。前端 lint（eslint）暂缓到下一阶段。
3. **顺带修了两个 CI 才发现的 bug**：① 漏声明 `python-multipart` 依赖（文档上传功能需要，以前本地碰巧装了没发现）；② **LLM 多通道切换在 Linux 上根本不工作**——配 `LLM_PROVIDERS=deepseek` 会因环境变量大小写问题（小写 name vs 大写 key）被读成空、退化成 mock，AI 功能失效。这个 bug 在 Windows 上巧合正常所以一直没发现，CI 改在 Linux 跑后才暴露。现在修了。

> 这版是工程治理（版本从 3.7.0 升到 3.8.0）。验证：guard 单测全过 + 真实测试库跑通 47 个集成测试；CI 三道门 5 轮验证全绿。详见 `CHANGELOG.md`。

## v3.7.0（2026-08-09）

这版把你之前留的两项立项做完了——管理员管成员空间、忘记密码 + 登录密码显隐：

1. **管理员可以配置每个成员能访问哪些空间了**（REQ-050）：以前管理员只能管「当前空间」里有谁，没办法管「某个用户」一共能进哪些空间。现在在「用户管理」页面点某个用户那一行，右边会弹出他「可访问的空间」清单——可以当场把他加到别的空间、改他在某个空间的角色（管理员/成员），或把他从某个空间移除。如果某个空间只剩他一个管理员，系统会拦着不让移除（避免空间没人管）。这个能力复用了已有的空间成员管理接口，后端只加了一个只读查询接口。
2. **登录页密码可以「小眼睛」切换显隐了**（REQ-051）：登录和注册的密码框右边多了「显示 / 隐藏」按钮，点一下能看到自己输的密码对不对。
3. **忘记密码可以自助重置了**（REQ-051）：登录页加了「忘记密码？」入口。点开输入注册邮箱申请——为了防止别人借此试探哪些邮箱注册过，不管邮箱存不存在，系统都回同一句"重置链接已发送"。因为这版没接邮件发送，重置 token 会写进后端日志（演示时从日志取 token），再在弹窗里填 token + 新密码就能重置。重置成功后这个账号在所有设备上的登录都会失效，要用新密码重新登录（更安全）。token 半小时过期、用一次就作废。

> 这版加了新能力（版本从 3.6.0 升到 3.7.0）。验证：后端测试 276 项全过（含忘记密码重置、成员空间配置的新用例）；前端构建绿；设计文档已补。真实的数据库迁移和浏览器自动化测试要启动 Docker / 演示环境后再跑，这版先标记为待补。

## v3.6.0（2026-08-08）

这版根据你试用反馈做了一批改进：

1. **本地挂载可以同时挂多个文件夹了**：以前只能挂一个目录，现在「挂载 vault」按钮可以点多次，每次加一个文件夹，所有挂载会在左边平铺成一棵树（每个挂载名是一级目录），方便管理分散在不同位置的知识库。
2. **右键菜单补了导入入口**：文件夹右键多了一个「导入全部挂载」，文件右键多了一个「导入此篇」——想导入单个文件不用先选中它了。
3. **「并排」视图修好了**：之前点并排，编辑框和预览框会被挤扁、错位。现在切并排会自动收起右侧的版本/标签栏，把空间让给编辑和预览，两边都正常。
4. **md 工具栏好看了**：按钮去掉了边框、按「格式/结构/插入」分组，鼠标移上去只让文字变蓝（不再出现深蓝色块）；而且点标题/列表这类按钮时，光标所在行的**原有文字会保留**（以前会被整行替换删掉）。
5. **编辑多了撤销（Ctrl+Z）**：网页编辑器里按 Ctrl+Z 之前没用（因为用了受控组件），现在能一步一步撤销了，最多回退 50 步。

> 这版加了新能力（版本从 3.5.0 升到 3.6.0）。验证：前端构建绿，你在浏览器里实测多挂载、导入入口、并排、工具栏、撤销都正常。

## v3.5.0（2026-08-08）

这版把你之前提的两个需求落地了：

1. **本地挂载的文件现在可以在网页里直接改了**（REQ-049）：以前挂载一个本地文件夹后只能看、只能搜、只能导入，现在可以——在左边目录里对文件夹右键或点「＋」新建文件、对某个文件右键重命名、点「×」删除（会先问你要不要确认），在主区预览时点「编辑」直接在网页里改正文再保存。**注意**：改的还是你本地那个文件，不会上传到服务器、不会进团队搜索，安全边界没变。

2. **写好了"开放给团队成员"的部署方案**（⑪）：之前 LUMEN 只能在你自己的电脑上跑。这版补了完整的打包和部署文件（后端、前端、数据库三件套的容器编排）加一份部署说明书 `docs/env/deploy-guide.md`——按里面步骤，找一台服务器执行一条命令，团队成员就能通过浏览器访问了。AI 能力（问答、润色）也一起装进容器。

另外还有两项立项（成员能看哪些空间、忘记密码+登录交互）还没做，等你确认。

> 这版加了新能力（版本从 3.4.0 升到 3.5.0）。验证：前端构建绿、部署编排配置校验通过；本地文件真实的增删改查需要你在浏览器里授权后实际点一遍确认。

## v3.4.0（2026-08-08）

这版根据你使用 LUMEN 的反馈，加了 3 个写文档 / 读文档时更方便的功能：

1. **长文档多了「目录」导航**：打开一篇很长的 Markdown 文档，正文左侧会出现「目录」栏，列出文档里的一二三级标题，点一下直接跳过去；本地挂载的文档预览也有。
2. **编辑界面多了 Markdown 工具栏**：新建 / 编辑文档时，输入框上方有一排快捷按钮（加粗、斜体、标题、列表、引用、代码块、链接、图片、分割线），点一下就在光标位置插入对应的语法，不用手敲。
3. **可以在文件夹里直接新建文档了**：在左侧目录树对某个文件夹点右键，菜单里多了「在此新建文档」，新建的文档会保存到那个文件夹下（以前不管在哪建都跑到根目录）。

另外把 3 项「需求变更」和 1 项「部署开放团队」的立项写进了 `docs/design/batch-maintenance-2026-08-08.md`（本地挂载文件可编辑 / 成员可看哪些空间 / 忘记密码 / 部署方案），等你确认后再动工。

> 这版加了新功能（版本从 3.3.3 升到 3.4.0）。验证：前端构建绿、后端 311 个测试通过、浏览器自动化验证全绿。

## v3.3.3（2026-08-08）

这版修了使用 LUMEN 时反馈的 4 个交互小问题：

1. **新手清单做完关不掉**：登录后弹出的 3 步引导右上角加了「×」关闭；首页的「新手清单」卡片也能关闭了，而且 3 步全做完会自动收成一行。
2. **预览完本地文档，再点「新建」看不到新建界面**：之前「新建」一直显示的是刚才预览的本地文档，现在点新建会先关掉本地预览，正常进入新建编辑界面。
3. **文档权限下拉里「外部只读」出现两次**：下拉里重复的那项是历史遗留的同一个权限写了两遍，现在只保留一项。
4. **右上角头像只显示 `#1`**：现在显示你的用户名了（注册时填的名字）；老的登录缓存没名字时暂时仍显示 `#数字`。

> 这版全是修 bug，没加新功能。验证：前端构建绿、后端全部 311 个测试通过。

## v3.3.2（2026-08-08）

这版修了一个**切换账号后 AI 助手残留上一位用户对话**的 bug：你在 AI 助手里发过消息后登出，换另一个账号登录，右下角 AI 抽屉里还留着上一个用户的问题、回答和来源。同一个人切空间是正常的（切空间会清空），但换账号不清空就不对了——上一位用户看到的文档来源可能被下一位用户看到，这属于跨用户隔离问题。现在改成：登录、登出、换账号的时候把 AI 助手对话一起清空。验证：前端构建绿（294 模块），你浏览器复测过（alice 发消息 → 登出 → kira 登录 → AI 助手对话已清空）。

## v3.3.1（2026-08-08）

这版修了一个真实数据库（PostgreSQL）模式下才暴露的 bug：**通过「导入」进来的文档删不掉**。之前对导入文档点删除，后端会报 500 错（数据库外键约束冲突，`lumen_imports` 表还引用着这篇文档，但没有自动级联清理），导致验证清单里「清理测试数据」这一步做不下去。现在改成：删文档前先把导入记录里对这篇文档的引用解绑，再删文档本身。修完跑过验证：PostgreSQL 集成测试 17 个全过（含新增的回归用例）、后端全部 311 个测试通过、真实数据库里导入再删除实测返回 200 成功。另外把这次验证新写的团队端到端操作手册（`docs/env/team-e2e-verification-runbook.md`）和验证记录一并登记进 `docs/09-verification.md`。

## v3.3.0（2026-08-08）

这版加了**右下角 AI 助手**：点悬浮图标 ✨ 弹出对话抽屉，可以多轮聊天；底下有个「基于知识库」勾选框——勾上就是基于知识库的检索问答（答案带来源、可点开原文），不勾就是通用聊天（纯对话）。`Ctrl+K` 命令面板里输入问题回车选「问 AI」，现在直接打开 AI 抽屉而不是跳页面。另外 **LLM 通道可以切换了**：在 AI 抽屉底部「通道」下拉选 deepseek / glm / gpt（配置在本地 `.env`，key 不泄露），哪个中转 token 用完或连不上就切另一个；新增 deepseek 通道（api.deepseek.com），演示默认走它，真实出文。后端问答接口扩展了多轮历史 / 开关 / 通道字段（向后兼容），并新增一个查通道列表的接口。另外修了一个切换通道后仍走旧通道的 bug。验证：后端 310 个测试全过（新增 13 个），前端构建绿（294 模块），浏览器自动化 smoke 过（切 deepseek 真实出文），你也验收过了。

## v3.2.0（2026-08-07）

这版是一批体验升级（维护态 UI 改进）：**术语管理升级成「领域树」**——左栏变成「全局术语固定区 + 空间领域树」，可以建领域（如「研发与开发流程」「软件测试验证」）、建子领域、重命名、排序、删除；术语挂在领域下面，还带「内容分类」（14 类候选）+「来源」字段；点术语进**阅读态**（看名称 / 状态 / 领域 / 分类 / 定义 / 别名 / 来源），点「编辑」才进编辑表单，不再像以前一点就进编辑框；全局术语固定顶部只读。**标签能改名字、加描述、换颜色、归档了**——标签行悬停出现「✎」，点开内联编辑表单，保存生效；归档会确认一次，归档后标签从列表消失但关联的文档还在。另外：顶栏图标换成更清晰的图形，左右栏边缘加了折叠箭头；`Ctrl+K`（Mac 是 `⌘K`）呼出全局搜索命令面板，即时搜索文档 / 跳转视图 / 新建 / 导入 / 问 AI，支持键盘上下选、回车跳转。验证：后端全量 298 个测试通过（2 个跳过），前端构建绿（291 模块），数据库新表 / 新字段已在本机 PostgreSQL 实测生效，浏览器验收通过。

## v3.1.0（2026-08-07）

这版把「账号」升级成「有管理员的分层账号」：用户表加了全局角色（admin / member，新注册默认 member，演示账号 alice 是管理员）。管理员可以在「用户管理」页看所有用户（姓名 / 邮箱 / 角色 / 状态 / 最后登录）、改角色、禁用 / 启用账号（被禁用的用户登录会被拒绝，文档保留不删）；团队空间加了成员管理——空间管理员可以按邮箱搜索用户并添加为成员、改空间角色、移除成员（移除后立刻失去该空间访问，文档归属不变）。普通成员看不到这些入口，直接调接口也会被拒绝（403）。安全细节：用户列表和搜索不返回密码哈希；空间不能把自己锁死——最后一个空间管理员不能降级或移除（返回 409）；所有管理操作都写审计日志。验证：后端全量 275 个测试通过（2 个跳过），前端构建绿，浏览器自动化 smoke（管理员 + 普通成员双视角 + API 越权矩阵）全部通过。

## v3.0.0（2026-08-07）

这版把「登录」从演示用的假登录升级成真实账号体系：可以注册新账号（邮箱 + 密码，密码 bcrypt 加密存库），用账号密码登录，退出登录，支持多设备同时登录、token 自动续期，以及把某个设备的会话踢下线（重复踢不报错，踢不存在的会话才报 404）。登录接口的入参变了——以前只传 external_id，现在必须传账号 + 密码——属于对外破坏性变更，所以版本直接升到 3.0。安全方面：登录失败 5 次锁 15 分钟、重复邮箱注册返回 409、密码太短返回 422、密码错误返回 401，而且不区分「账号不存在」和「密码错误」，避免被试探。演示环境仍保留 alice / kira 等快速登录（seed 账号密码统一为 demo-pass-1234），生产环境（PostgreSQL 仓储）强制真实凭证，用 demo 仓储启动生产模式会被拒绝。验证：后端 20 个账号测试 + 全量 222 个测试通过，前端构建 273 modules 绿，浏览器自动化 smoke（注册 → 登录 → 登出 → 刷新续期 → 多设备会话撤销）全部通过，demo（后端 18000 + 前端 5173）启动验证通过。

## v2.2.0（2026-08-06）

这版把「新用户怎么上手」这条链路补齐了：登录后第一次会弹一个 3 步引导（建一篇文档 → 保存后去搜索 → 去问答提问），每步可以点「去完成」直接跳到对应页面，也可以整体跳过（跳过或做完后，下次登录不再打扰）；首页欢迎页有「新手清单」进度和「示例文档未建索引」的提示（意思是：自带的示例文档不会被搜索和问答命中，先新建或导入才会有内容）；搜索、问答、标签、时间线这些空页面都加了「去新建文档 / 去导入」的下一步入口；右上角的「?」帮助现在是分类速查，能输入关键词过滤（比如输入「导入」直接看到相关条目），还有「查看完整手册」链接。顺手修了两个小毛病：点开帮助弹层后之前只能再点「?」才能关，现在点弹层外面、按 Esc、或点弹层右上角的「×」都能关；搜索和问答空页面里「去新建文档」「去导入」两个按钮之前被拉得特别高、占一大块地方，现在恢复正常小按钮。另外把帮助手册内容重组成按任务找答案的结构（从哪开始、怎么放资料、怎么找内容……），统一成唯一内容源。纯前端改动，没动后端接口和数据契约。

## v2.1.0（2026-08-06）

这版给「本地挂载」补了一个很顺手的操作：以前挂载本地笔记文件夹后，要么整库导入、要么一篇篇导；现在把鼠标停到任意子文件夹上，会浮现一个「导入」按钮，点它可以把这一整个子文件夹（连同里面的子目录和文件）按原目录结构导进 LUMEN。导入前如果文件多会先问一句（显示要导多少文件、会保留目录结构），导入中显示进度（正在导哪个文件夹、第几个），导完告诉你去向和成功 / 失败 / 跳过各多少，左侧文档列表也会马上刷新。单篇导入还是老样子、不打扰。顺手修了两个小毛病：左边本地挂载区和上面知识库区之间的拖动条方向反了（现在拖动方向符合直觉），以及首页有时左栏是空的（现在会根据当前视图正确显示）。纯前端改动，没动后端接口和数据契约。

## v2.0.0（2026-08-06）

这是一个大版本：LUMEN 终于能挂载本地知识库了（Phase2C 首个能力）。如果你有一堆 Markdown 笔记（比如 Obsidian 的 vault）想整理，但又有些内容比较私密、不想上传到服务器，现在可以把这个本地文件夹「挂载」进 LUMEN，直接在里面浏览目录、按关键词搜索正文、点开看内容——但所有这些操作都只在你自己的浏览器里进行，笔记原文不会上传到服务器、也不会进入团队的 AI 问答。刷新页面后挂载还在（不用重新选目录、重新授权）。看完觉得某篇或整个库想要 LUMEN 的完整能力（全文搜索、AI 引用、标签、版本、团队共享），可以一键「导入到 LUMEN」，这时才会按之前的方式上传入库。这是一条明确的边界：仅本地挂载 = 只自己看、不上传；导入 = 进数据库、能用 AI。

技术上纯前端实现（用浏览器的文件系统访问能力 + 浏览器自带的本地数据库存挂载信息），没动后端接口。顺手把本地访问地址从 127.0.0.1 改成了 localhost（浏览器对文件访问要求的安全上下文）。左下角的本地挂载区和上面的知识库区可以用中间的横条拖动调整各自高度；点本地文件时正文显示在中间主区（不再挤在左边小栏里）。硬限制没变：只本地挂载的内容没法用 AI 问答（浏览器读得到的，服务器读不到），要 AI 必须导入。经过自动脚本 + 人工两轮验证（包括确认全程零网络上传）。

## v1.7.5（2026-08-05）

这版是 Phase2B 收口后的一次界面打磨，没加新功能、也没动数据和接口。修了几处用起来别扭的地方：左右两栏的宽度以前是固定的、不能调，现在可以直接拖边框改宽，改完刷新也会记住；编辑文档点“保存”后以前还停在编辑框里，连续点保存容易存出一堆版本，现在保存完自动回到阅读模式；删文档以前只能进编辑区点“删除”，现在在左侧目录里对文档点右键就有“删除文档”；快速录入的抽屉以前只有 420 宽，现在放宽到 560；搜索页的关键词输入框以前和标题之间空一大块，现在紧贴标题。另外把编辑器的留白、编辑工具栏的样子、顶栏图标和帮助入口、左侧目录的图标和“展开 / 收起 / 定位当前文件”都收拾了一遍，去掉了界面上露出来的 REQ 编号角标（追溯信息保留在代码注释和文档里）。纯界面修复，没有新功能。

## v1.7.4（2026-08-05）

这版修了一个 Obsidian 笔记里常见的链接写法问题，并把之前的评估材料收进仓库。之前笔记里写 `[[目标|别名]]`（显示成别名）或 `[[目标#锚点]]`（跳到某节）时，程序会把整段 `目标|别名` / `目标#锚点` 当成目标名字去找文档，结果总是找不到、显示成断链；现在会先剥掉 `|别名` 和 `#锚点` 再按 `目标` 找，这两种写法都能正常连上，指向同一个目标的链接会合并成一条。另外把「Obsidian Vault 能不能下一步做」的评估报告登记进了调研索引，把验证用的导入脚本正式收进仓库，顺手补上了之前漏打的 v1.7.2 / v1.7.3 版本标签。纯修复和收尾，没有新功能。

## v1.7.3（2026-08-05）

这版把 Obsidian 笔记库导入功能补完整了，让导入真正好用。修了两个小问题：一是之前导入时笔记之间的 `[[内链]]` 按顺序一篇篇解析，先导入的笔记如果链接了还没导入的笔记，这条链接就会断掉、而且再也不会补上——现在导入完一批后会把整个知识库的链接重新捋一遍，断的接上（也包括分批上传时跨批的断链）；二是之前导入会把 Obsidian 的配置目录（`.obsidian`）、隐藏文件和非文本附件也一股脑传上去，后端不收就显示一堆「失败」，吵且没用——现在前端在选择 / 拖拽文件时就把这些滤掉，只传真正的 `.md/.txt` 笔记。另外加了个验证脚本，连真实数据库确认导入进去的笔记能搜到、能用来问答（之前在内存演示模式里搜不出，这次证明换成真数据库就正常了）。都是修 bug 和验证，没有新功能。

## v1.7.2（2026-08-04）

这版加了一个性能压测脚本，专门量「把文档批量导进真实 PostgreSQL（带向量化）到底要多久」。之前那个导入修复（v1.7.1）只在内存 demo 上验证过 multipart 上限问题，没压过真实数据库 + 向量化的耗时；这版补上：脚本连真实数据库、按每批 50 篇顺序导入、确认向量真的写进去了、跑完清理临时数据。实测 200 篇大约 41 秒、每篇约 0.2 秒；按这速度 1000 篇要几分钟，所以真要常导上千篇，以后还得做后台异步导入。结论也写进了验收记录。不是新功能，是测试 + 记录工具。

## v1.7.1（2026-08-04）

这版修了一个导入 bug，并把 Obsidian Vault 兼容的远期设计写进文档。之前一次导入超过约 1000 个文件的文件夹会直接失败——浏览器把整个文件夹塞进一个上传请求，撞到后端框架默认的 1000 个字段上限，根本没开始导入就报错；现在前端会自动拆成每批 50 个文件、按顺序上传，中途某批失败也不影响已成功的批次，最后给出「成功/失败/跳过」总数、失败的文件全部列出来方便重试。批大小定 50 是因为导入时还要在本机跑向量化，批太大会让单个请求超时。另外把「Obsidian Vault 兼容」的远期设想（同一文件夹可选「导入数据库」或「只在本地挂载、仅自己可见」）整理进了设计文档，作为后续技术验证的立项依据；这部分不是现在能用的功能，明确标了愿景、不进当前阶段。

## v1.7.0（2026-08-04）

这版把 PDF 导出最后一截补上了。之前点“导出 PDF”只是生成文件并提示服务器本机路径；现在会继续请求新的下载接口，把 PDF 当成浏览器下载文件发回来，用户能直接拿到 `.pdf`。后端下载时会重新查权限，确认这个导出任务属于当前空间、源文档当前仍可读、文件还在受控目录里；任务没完成会返回 4090，文件不存在或无权限按不存在处理。

## v1.6.0（2026-08-04）

这版把单篇文档导出 PDF 做成了可用功能。现在在文档详情里可以点“导出 PDF”，后端会按当前用户权限读取这篇文档的当前版本或指定版本，生成中文 PDF，并把导出任务记到数据库里。中文标题、正文、列表、表格、引用和页眉页脚都做了基础排版。导出库或字体不可用时不会吐坏文件，而是把任务标成失败并返回 5030。首版先同步生成，PDF 文件放在 `tmp/pdf_exports`；异步队列、下载端点、过期清理、水印、Word/PDF 解析和 zhparser 仍留后续。

## v1.5.2（2026-08-04）

这版不加新功能，专门把两个验证欠账补上：文件夹树现在有可重复的浏览器自动化脚本，会真的打开 Chrome/Edge、登录、进入文档视图、新建子文件夹、把文档移动进去，再从后端确认移动成功；时间线也补了真实 PostgreSQL 大数据性能 smoke，用 620 篇临时文档和 240 条链接跑真实服务路径，确认会进入降级窗口、只返回 200 条列表、密度统计正确，耗时约 2.7 秒。脚本跑完会清理临时数据。

## v1.5.1（2026-08-04）

这版不加新功能，只把上一个版本发布后的记录补干净：Sprint-20 已经推到 `main`，CI 已通过，`v1.5.0` tag 也已经打到对应提交。之前开发计划里还残留“本地未提交”这类旧说法，这版把它改成准确的发布状态，避免下次续接时误判。

## v1.5.0（2026-08-03）

这版把 Phase2B 的「主题时间线」做成了可用功能。你可以进入时间线视图，按关键词或标签看相关文档什么时候创建、更新、打标签、建立链接；上方密度热条会显示每天事件多少，点时间线里的文档可以回到文档阅读。实现上没有新建一张事件流水表，而是实时从现有文档、标签、链接和正文 chunk 聚合，保持轻量。还顺手把这几次浏览器 smoke 反复中断的 demo 启动根因修掉了：后台启动现在用独立 launcher，不会再因为 AI 执行器结束就把后端带走；备用端口启动前端也不再混入默认 5173 参数。

## v1.4.0（2026-08-03）

这版把上一版只有后端的「文件夹」补成前端真正能用的文件管理器。左侧目录树更接近 Obsidian：界面更简洁，文件夹和单篇文档都有右键菜单，菜单会在点外部、按 Esc 或滚动时自动收起；新建和重命名文件夹改成在左侧栏里直接编辑，不再弹浏览器输入框。批量导入文件夹时也会保留原目录结构，把文档放到对应文件夹里；单篇文档现在也可以通过右键菜单移动到其他文件夹或根目录。之前出现过的「右键能点移动，但实际没移动」问题，这版通过新增并接通单篇文档移动接口修掉了，并且已经做过后端测试、前端构建、运行态 API smoke 和用户浏览器 smoke。

## v1.3.0（2026-08-03）

这版给知识库加了「文件夹」：文档不再只能平铺在空间里，可以建嵌套文件夹（比如「项目A/子1」），把文档归到文件夹下，文件夹能新建、改名、移动、删除、排序。移动时会防环（不能把一个文件夹拖进它自己的子文件夹）和防跨空间；同层重名或文件夹非空（有子夹或有文档）时拒绝删除。文件夹本身不单独设权限，文件夹里的文档仍按原来的私有/团队/外部可见性过滤，不会因为放进文件夹就越权泄露。这版只做后端接口和数据层（能通过 API 操作文件夹，还没接数据库真实跑递归查询），前端文件管理器界面留下一个版本。现有文档默认归到空间根，不受影响。

## v1.2.0（2026-08-02）

这版把工作台整体从「一堆常驻面板的工具台」改成「以文档内容为中心、工具收起来按需呼出」的阅读器范式（参考 Obsidian 那种简洁感），即 Sprint-21 Doc-First UX 收口。主要变化：文档默认是「阅读」单列（只看正文），点「编辑」才出编辑框，点「并排」才是编辑+预览双列；导入从原来的常驻区改成 toolbar「导入」按钮弹窗；快速录入和用户头像挪到顶栏右侧；右侧文档栏改成「版本 / 链接 / 标签 / AI」几个 tab。还修了几个 smoke 反馈：中文标题文档下载 .md 报错（后端文件名编码）、快速录入默认改成直接转文档而不是存草稿、标签在文档侧栏可以直接新建并打标签（不用再跳到标签视图）。后端接口和数据库没动。

## v1.1.0（2026-07-31）

这版交付 Phase2B 的第一个完整功能：AI 润色和写作引用。写文档时选中一段话，可以让 AI 改写得更通顺（润色），或基于知识库里已有的相关内容来续写并标注来源（引用）；满意就应用回正文（自动留一个历史版本可回退），不满意就丢弃。后端（新接口、草稿存储、权限过滤和降级）与前端（右侧润色侧边栏）都完成了，把文档片段发给公司内网 AI 这个数据外发风险也正式通过评审（RG-008 升 Go）。还差最后一步浏览器人工验收，等数据库和 AI 环境起来再补。

## v1.0.0（2026-07-30）

这版是阶段跨越：从 Phase2A（个人知识组织）正式进入 Phase2B（团队 MVP）。本身不新增可演示功能，主要是把阶段指针切过去、把 AI 润色和时间轴从"以后再说"挪到"现在可以做"，并落锤 AI 润色要把文档片段发给公司内网 AI 这个数据外发风险（有条件通过，等第一个功能跑通再正式放行）。是为后续 AI 润色 / 时间轴铺路。

## v0.2.7（2026-07-30）

这版是前端代码结构整理：把原本挤在 App.tsx 里的一大坨视图切换逻辑抽到单独的 WorkspaceMain 文件，主应用文件从 305 行降到 231 行（低于约定的膨胀阈值）。纯粹结构搬运，不改任何业务逻辑，为后面 AI 润色侧边栏腾地方。

## v0.2.6（2026-07-30）

这版补了 Phase2B AI 润色 / 时间轴的前端交互设计（侧边栏长什么样、怎么点击、各状态怎么提示），并修了一个前端构建崩溃的坑——根因是本机 Node 版本太老（≤16）和 Vite 5 不兼容，用 Volta 锁定到 Node 22 后修好。纯文档和配置，没写功能代码，也没切阶段。

## v0.2.5（2026-07-30）

这版确认了 Phase2B 要先做什么、并落锤 AI 润色的数据外发风险。范围定为：REQ-014 AI 润色 / 写作引用做首批核心，REQ-013 / 024 时间轴紧随第二 slice。同时把"允许 AI 润色把真实文档片段经公司内网中转 LLM 外发"这个风险正式接受并配上护栏（只发你有权看的内容、草稿只存哈希和摘要、不做敏感字段自动过滤、AI 不可用时降级不编造），补了 RG-008、相关设计文档和 Sprint-19 / 20 规划。纯文档和决策留痕，没切阶段、没编码。

## v0.2.4（2026-07-21）

这版主要是文档状态校准。把 2026-07-20 输入材料、另一 AI 的评估和仓库事实核对整理成研究记录，并把 Phase2A 相关文档里已经完成的能力状态对齐。它不增加新功能，也不改后端、前端、接口或数据库，只是让文档更准确地反映已有实现。

## v0.2.3（2026-07-20）

这版把 Phase2A 的验收状态收口。快速录入、标签、内链 / 反链等个人知识组织能力已经有实现和测试证据，因此文档不再把 Phase2A 写成待确认或未完成。Phase2B、PDF / Word 解析、zhparser 等仍留到后续确认。

## v0.2.2（2026-07-19）

这版新增面向最终用户的操作手册，覆盖登录空间、文档、批量导入、版本、双向链接、标签、快速录入、搜索、问答、术语、导出和权限等日常使用路径。它是纯文档版本，不新增可演示能力，也不改 API 契约。

## v0.2.1（2026-07-19）

这版重构了前端主应用文件，把 `App.tsx` 里的业务逻辑拆到多个领域 hook 和 helper 文件里，降低主文件体量并修复切换空间后保存 UI 不刷新的问题。用户看到的功能范围不变，主要收益是代码更容易维护。

## v0.2.0（2026-07-19）

这版交付 Phase2A 的“快速录入”能力。用户可以快速记录标题、来源、摘要和标签，选择保留为私有草稿、转成新私有文档，或追加到已有文档；后端、前端、API、数据库迁移和追溯文档都完成了对应闭环。

## v0.1.0（2026-07-18）

这版不是业务功能发布，而是把 LUMEN-DEMO 从沿用母模板版本号切换为项目自有版本机制。此后 `VERSION` 表示 LUMEN-DEMO 自身版本，继承到哪个 `ai-project-template` 版本由 `TEMPLATE-BASE.md` 记录。
