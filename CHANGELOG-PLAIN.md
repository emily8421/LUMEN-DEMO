# CHANGELOG-PLAIN

本文是 LUMEN-DEMO 项目自身 `CHANGELOG.md` 的大白话同步版，按项目版本解释“这版实际改变了什么”。

- 项目版本入口：`VERSION`
- 正式项目 changelog：`CHANGELOG.md`
- 继承的母模板版本：`TEMPLATE-BASE.md`
- 母模板发布说明：`upstream/CHANGELOG.md` / `upstream/CHANGELOG-PLAIN.md`

历史上随模板同步带入的 `v1.x` 记录不再在本文重复展开；需要审计时看 `CHANGELOG.md` 的“历史模板同步记录”和 `sync-records/template-sync/`。

## v3.6.0（2026-08-08）

这版根据你试用反馈做了一批改进：

1. **本地挂载可以同时挂多个文件夹了**：以前只能挂一个目录，现在「挂载 vault」按钮可以点多次，每次加一个文件夹，所有挂载会在左边平铺成一棵树（每个挂载名是一级目录），方便管理分散在不同位置的知识库。
2. **右键菜单补了导入入口**：文件夹右键多了一个「导入全部挂载」，文件右键多了一个「导入此篇」——想导入单个文件不用先选中它了。
3. **「并排」视图修好了**：之前点并排，编辑框和预览框会被挤扁、错位。现在切并排会自动收起右侧的版本/标签栏，把空间让给编辑和预览，两边都正常。
4. **md 工具栏好看了**：按钮去掉了边框、按「格式/结构/插入」分组，鼠标移上去只让文字变蓝（不再出现深蓝色块）；而且点标题/列表这类按钮时，光标所在行的**原有文字会保留**（以前会被整行替换删掉）。
5. **编辑多了撤销（Ctrl+Z）**：网页编辑器里按 Ctrl+Z 之前没用（因为用了受控组件），现在能一步一步撤销了，最多回退 50 步。

> 这版加了新能力（版本从 3.5.0 升到 3.6.0）。验证：前端构建绿，你在浏览器里实测多挂载、导入入口、并排、工具栏、撤销都正常。

## v3.5.0（2026-08-08）

这版把你之前提的两个需求落地了：

1. **本地挂载的文件现在可以在网页里直接改了**（REQ-049）：以前挂载一个本地文件夹后只能看、只能搜、只能导入，现在可以——在左边目录里对文件夹右键或点「＋」新建文件、对某个文件右键重命名、点「×」删除（会先问你要不要确认），在主区预览时点「编辑」直接在网页里改正文再保存。**注意**：改的还是你本地那个文件，不会上传到服务器、不会进团队搜索，安全边界没变。

2. **写好了"开放给团队成员"的部署方案**（⑪）：之前 LUMEN 只能在你自己的电脑上跑。这版补了完整的打包和部署文件（后端、前端、数据库三件套的容器编排）加一份部署说明书 `docs/env/deploy-guide.md`——按里面步骤，找一台服务器执行一条命令，团队成员就能通过浏览器访问了。AI 能力（问答、润色）也一起装进容器。

另外还有两项立项（成员能看哪些空间、忘记密码+登录交互）还没做，等你确认。

> 这版加了新能力（版本从 3.4.0 升到 3.5.0）。验证：前端构建绿、部署编排配置校验通过；本地文件真实的增删改查需要你在浏览器里授权后实际点一遍确认。

## v3.4.0（2026-08-08）

这版根据你使用 LUMEN 的反馈，加了 3 个写文档 / 读文档时更方便的功能：

1. **长文档多了「目录」导航**：打开一篇很长的 Markdown 文档，正文左侧会出现「目录」栏，列出文档里的一二三级标题，点一下直接跳过去；本地挂载的文档预览也有。
2. **编辑界面多了 Markdown 工具栏**：新建 / 编辑文档时，输入框上方有一排快捷按钮（加粗、斜体、标题、列表、引用、代码块、链接、图片、分割线），点一下就在光标位置插入对应的语法，不用手敲。
3. **可以在文件夹里直接新建文档了**：在左侧目录树对某个文件夹点右键，菜单里多了「在此新建文档」，新建的文档会保存到那个文件夹下（以前不管在哪建都跑到根目录）。

另外把 3 项「需求变更」和 1 项「部署开放团队」的立项写进了 `docs/design/batch-maintenance-2026-08-08.md`（本地挂载文件可编辑 / 成员可看哪些空间 / 忘记密码 / 部署方案），等你确认后再动工。

> 这版加了新功能（版本从 3.3.3 升到 3.4.0）。验证：前端构建绿、后端 311 个测试通过、浏览器自动化验证全绿。

## v3.3.3（2026-08-08）

这版修了使用 LUMEN 时反馈的 4 个交互小问题：

1. **新手清单做完关不掉**：登录后弹出的 3 步引导右上角加了「×」关闭；首页的「新手清单」卡片也能关闭了，而且 3 步全做完会自动收成一行。
2. **预览完本地文档，再点「新建」看不到新建界面**：之前「新建」一直显示的是刚才预览的本地文档，现在点新建会先关掉本地预览，正常进入新建编辑界面。
3. **文档权限下拉里「外部只读」出现两次**：下拉里重复的那项是历史遗留的同一个权限写了两遍，现在只保留一项。
4. **右上角头像只显示 `#1`**：现在显示你的用户名了（注册时填的名字）；老的登录缓存没名字时暂时仍显示 `#数字`。

> 这版全是修 bug，没加新功能。验证：前端构建绿、后端全部 311 个测试通过。

## v3.3.2（2026-08-08）

这版修了一个**切换账号后 AI 助手残留上一位用户对话**的 bug：你在 AI 助手里发过消息后登出，换另一个账号登录，右下角 AI 抽屉里还留着上一个用户的问题、回答和来源。同一个人切空间是正常的（切空间会清空），但换账号不清空就不对了——上一位用户看到的文档来源可能被下一位用户看到，这属于跨用户隔离问题。现在改成：登录、登出、换账号的时候把 AI 助手对话一起清空。验证：前端构建绿（294 模块），你浏览器复测过（alice 发消息 → 登出 → kira 登录 → AI 助手对话已清空）。

## v3.3.1（2026-08-08）

这版修了一个真实数据库（PostgreSQL）模式下才暴露的 bug：**通过「导入」进来的文档删不掉**。之前对导入文档点删除，后端会报 500 错（数据库外键约束冲突，`lumen_imports` 表还引用着这篇文档，但没有自动级联清理），导致验证清单里「清理测试数据」这一步做不下去。现在改成：删文档前先把导入记录里对这篇文档的引用解绑，再删文档本身。修完跑过验证：PostgreSQL 集成测试 17 个全过（含新增的回归用例）、后端全部 311 个测试通过、真实数据库里导入再删除实测返回 200 成功。另外把这次验证新写的团队端到端操作手册（`docs/env/team-e2e-verification-runbook.md`）和验证记录一并登记进 `docs/09-verification.md`。

## v3.3.0（2026-08-08）

这版加了**右下角 AI 助手**：点悬浮图标 ✨ 弹出对话抽屉，可以多轮聊天；底下有个「基于知识库」勾选框——勾上就是基于知识库的检索问答（答案带来源、可点开原文），不勾就是通用聊天（纯对话）。`Ctrl+K` 命令面板里输入问题回车选「问 AI」，现在直接打开 AI 抽屉而不是跳页面。另外 **LLM 通道可以切换了**：在 AI 抽屉底部「通道」下拉选 deepseek / glm / gpt（配置在本地 `.env`，key 不泄露），哪个中转 token 用完或连不上就切另一个；新增 deepseek 通道（api.deepseek.com），演示默认走它，真实出文。后端问答接口扩展了多轮历史 / 开关 / 通道字段（向后兼容），并新增一个查通道列表的接口。另外修了一个切换通道后仍走旧通道的 bug。验证：后端 310 个测试全过（新增 13 个），前端构建绿（294 模块），浏览器自动化 smoke 过（切 deepseek 真实出文），你也验收过了。

## v3.2.0（2026-08-07）

这版是一批体验升级（维护态 UI 改进）：**术语管理升级成「领域树」**——左栏变成「全局术语固定区 + 空间领域树」，可以建领域（如「研发与开发流程」「软件测试验证」）、建子领域、重命名、排序、删除；术语挂在领域下面，还带「内容分类」（14 类候选）+「来源」字段；点术语进**阅读态**（看名称 / 状态 / 领域 / 分类 / 定义 / 别名 / 来源），点「编辑」才进编辑表单，不再像以前一点就进编辑框；全局术语固定顶部只读。**标签能改名字、加描述、换颜色、归档了**——标签行悬停出现「✎」，点开内联编辑表单，保存生效；归档会确认一次，归档后标签从列表消失但关联的文档还在。另外：顶栏图标换成更清晰的图形，左右栏边缘加了折叠箭头；`Ctrl+K`（Mac 是 `⌘K`）呼出全局搜索命令面板，即时搜索文档 / 跳转视图 / 新建 / 导入 / 问 AI，支持键盘上下选、回车跳转。验证：后端全量 298 个测试通过（2 个跳过），前端构建绿（291 模块），数据库新表 / 新字段已在本机 PostgreSQL 实测生效，浏览器验收通过。

## v3.1.0（2026-08-07）

这版把「账号」升级成「有管理员的分层账号」：用户表加了全局角色（admin / member，新注册默认 member，演示账号 alice 是管理员）。管理员可以在「用户管理」页看所有用户（姓名 / 邮箱 / 角色 / 状态 / 最后登录）、改角色、禁用 / 启用账号（被禁用的用户登录会被拒绝，文档保留不删）；团队空间加了成员管理——空间管理员可以按邮箱搜索用户并添加为成员、改空间角色、移除成员（移除后立刻失去该空间访问，文档归属不变）。普通成员看不到这些入口，直接调接口也会被拒绝（403）。安全细节：用户列表和搜索不返回密码哈希；空间不能把自己锁死——最后一个空间管理员不能降级或移除（返回 409）；所有管理操作都写审计日志。验证：后端全量 275 个测试通过（2 个跳过），前端构建绿，浏览器自动化 smoke（管理员 + 普通成员双视角 + API 越权矩阵）全部通过。

## v3.0.0（2026-08-07）

这版把「登录」从演示用的假登录升级成真实账号体系：可以注册新账号（邮箱 + 密码，密码 bcrypt 加密存库），用账号密码登录，退出登录，支持多设备同时登录、token 自动续期，以及把某个设备的会话踢下线（重复踢不报错，踢不存在的会话才报 404）。登录接口的入参变了——以前只传 external_id，现在必须传账号 + 密码——属于对外破坏性变更，所以版本直接升到 3.0。安全方面：登录失败 5 次锁 15 分钟、重复邮箱注册返回 409、密码太短返回 422、密码错误返回 401，而且不区分「账号不存在」和「密码错误」，避免被试探。演示环境仍保留 alice / kira 等快速登录（seed 账号密码统一为 demo-pass-1234），生产环境（PostgreSQL 仓储）强制真实凭证，用 demo 仓储启动生产模式会被拒绝。验证：后端 20 个账号测试 + 全量 222 个测试通过，前端构建 273 modules 绿，浏览器自动化 smoke（注册 → 登录 → 登出 → 刷新续期 → 多设备会话撤销）全部通过，demo（后端 18000 + 前端 5173）启动验证通过。

## v2.2.0（2026-08-06）

这版把「新用户怎么上手」这条链路补齐了：登录后第一次会弹一个 3 步引导（建一篇文档 → 保存后去搜索 → 去问答提问），每步可以点「去完成」直接跳到对应页面，也可以整体跳过（跳过或做完后，下次登录不再打扰）；首页欢迎页有「新手清单」进度和「示例文档未建索引」的提示（意思是：自带的示例文档不会被搜索和问答命中，先新建或导入才会有内容）；搜索、问答、标签、时间线这些空页面都加了「去新建文档 / 去导入」的下一步入口；右上角的「?」帮助现在是分类速查，能输入关键词过滤（比如输入「导入」直接看到相关条目），还有「查看完整手册」链接。顺手修了两个小毛病：点开帮助弹层后之前只能再点「?」才能关，现在点弹层外面、按 Esc、或点弹层右上角的「×」都能关；搜索和问答空页面里「去新建文档」「去导入」两个按钮之前被拉得特别高、占一大块地方，现在恢复正常小按钮。另外把帮助手册内容重组成按任务找答案的结构（从哪开始、怎么放资料、怎么找内容……），统一成唯一内容源。纯前端改动，没动后端接口和数据契约。

## v2.1.0（2026-08-06）

这版给「本地挂载」补了一个很顺手的操作：以前挂载本地笔记文件夹后，要么整库导入、要么一篇篇导；现在把鼠标停到任意子文件夹上，会浮现一个「导入」按钮，点它可以把这一整个子文件夹（连同里面的子目录和文件）按原目录结构导进 LUMEN。导入前如果文件多会先问一句（显示要导多少文件、会保留目录结构），导入中显示进度（正在导哪个文件夹、第几个），导完告诉你去向和成功 / 失败 / 跳过各多少，左侧文档列表也会马上刷新。单篇导入还是老样子、不打扰。顺手修了两个小毛病：左边本地挂载区和上面知识库区之间的拖动条方向反了（现在拖动方向符合直觉），以及首页有时左栏是空的（现在会根据当前视图正确显示）。纯前端改动，没动后端接口和数据契约。

## v2.0.0（2026-08-06）

这是一个大版本：LUMEN 终于能挂载本地知识库了（Phase2C 首个能力）。如果你有一堆 Markdown 笔记（比如 Obsidian 的 vault）想整理，但又有些内容比较私密、不想上传到服务器，现在可以把这个本地文件夹「挂载」进 LUMEN，直接在里面浏览目录、按关键词搜索正文、点开看内容——但所有这些操作都只在你自己的浏览器里进行，笔记原文不会上传到服务器、也不会进入团队的 AI 问答。刷新页面后挂载还在（不用重新选目录、重新授权）。看完觉得某篇或整个库想要 LUMEN 的完整能力（全文搜索、AI 引用、标签、版本、团队共享），可以一键「导入到 LUMEN」，这时才会按之前的方式上传入库。这是一条明确的边界：仅本地挂载 = 只自己看、不上传；导入 = 进数据库、能用 AI。

技术上纯前端实现（用浏览器的文件系统访问能力 + 浏览器自带的本地数据库存挂载信息），没动后端接口。顺手把本地访问地址从 127.0.0.1 改成了 localhost（浏览器对文件访问要求的安全上下文）。左下角的本地挂载区和上面的知识库区可以用中间的横条拖动调整各自高度；点本地文件时正文显示在中间主区（不再挤在左边小栏里）。硬限制没变：只本地挂载的内容没法用 AI 问答（浏览器读得到的，服务器读不到），要 AI 必须导入。经过自动脚本 + 人工两轮验证（包括确认全程零网络上传）。

## v1.7.5（2026-08-05）

这版是 Phase2B 收口后的一次界面打磨，没加新功能、也没动数据和接口。修了几处用起来别扭的地方：左右两栏的宽度以前是固定的、不能调，现在可以直接拖边框改宽，改完刷新也会记住；编辑文档点“保存”后以前还停在编辑框里，连续点保存容易存出一堆版本，现在保存完自动回到阅读模式；删文档以前只能进编辑区点“删除”，现在在左侧目录里对文档点右键就有“删除文档”；快速录入的抽屉以前只有 420 宽，现在放宽到 560；搜索页的关键词输入框以前和标题之间空一大块，现在紧贴标题。另外把编辑器的留白、编辑工具栏的样子、顶栏图标和帮助入口、左侧目录的图标和“展开 / 收起 / 定位当前文件”都收拾了一遍，去掉了界面上露出来的 REQ 编号角标（追溯信息保留在代码注释和文档里）。纯界面修复，没有新功能。

## v1.7.4（2026-08-05）

这版修了一个 Obsidian 笔记里常见的链接写法问题，并把之前的评估材料收进仓库。之前笔记里写 `[[目标|别名]]`（显示成别名）或 `[[目标#锚点]]`（跳到某节）时，程序会把整段 `目标|别名` / `目标#锚点` 当成目标名字去找文档，结果总是找不到、显示成断链；现在会先剥掉 `|别名` 和 `#锚点` 再按 `目标` 找，这两种写法都能正常连上，指向同一个目标的链接会合并成一条。另外把「Obsidian Vault 能不能下一步做」的评估报告登记进了调研索引，把验证用的导入脚本正式收进仓库，顺手补上了之前漏打的 v1.7.2 / v1.7.3 版本标签。纯修复和收尾，没有新功能。

## v1.7.3（2026-08-05）

这版把 Obsidian 笔记库导入功能补完整了，让导入真正好用。修了两个小问题：一是之前导入时笔记之间的 `[[内链]]` 按顺序一篇篇解析，先导入的笔记如果链接了还没导入的笔记，这条链接就会断掉、而且再也不会补上——现在导入完一批后会把整个知识库的链接重新捋一遍，断的接上（也包括分批上传时跨批的断链）；二是之前导入会把 Obsidian 的配置目录（`.obsidian`）、隐藏文件和非文本附件也一股脑传上去，后端不收就显示一堆「失败」，吵且没用——现在前端在选择 / 拖拽文件时就把这些滤掉，只传真正的 `.md/.txt` 笔记。另外加了个验证脚本，连真实数据库确认导入进去的笔记能搜到、能用来问答（之前在内存演示模式里搜不出，这次证明换成真数据库就正常了）。都是修 bug 和验证，没有新功能。

## v1.7.2（2026-08-04）

这版加了一个性能压测脚本，专门量「把文档批量导进真实 PostgreSQL（带向量化）到底要多久」。之前那个导入修复（v1.7.1）只在内存 demo 上验证过 multipart 上限问题，没压过真实数据库 + 向量化的耗时；这版补上：脚本连真实数据库、按每批 50 篇顺序导入、确认向量真的写进去了、跑完清理临时数据。实测 200 篇大约 41 秒、每篇约 0.2 秒；按这速度 1000 篇要几分钟，所以真要常导上千篇，以后还得做后台异步导入。结论也写进了验收记录。不是新功能，是测试 + 记录工具。

## v1.7.1（2026-08-04）

这版修了一个导入 bug，并把 Obsidian Vault 兼容的远期设计写进文档。之前一次导入超过约 1000 个文件的文件夹会直接失败——浏览器把整个文件夹塞进一个上传请求，撞到后端框架默认的 1000 个字段上限，根本没开始导入就报错；现在前端会自动拆成每批 50 个文件、按顺序上传，中途某批失败也不影响已成功的批次，最后给出「成功/失败/跳过」总数、失败的文件全部列出来方便重试。批大小定 50 是因为导入时还要在本机跑向量化，批太大会让单个请求超时。另外把「Obsidian Vault 兼容」的远期设想（同一文件夹可选「导入数据库」或「只在本地挂载、仅自己可见」）整理进了设计文档，作为后续技术验证的立项依据；这部分不是现在能用的功能，明确标了愿景、不进当前阶段。

## v1.7.0（2026-08-04）

这版把 PDF 导出最后一截补上了。之前点“导出 PDF”只是生成文件并提示服务器本机路径；现在会继续请求新的下载接口，把 PDF 当成浏览器下载文件发回来，用户能直接拿到 `.pdf`。后端下载时会重新查权限，确认这个导出任务属于当前空间、源文档当前仍可读、文件还在受控目录里；任务没完成会返回 4090，文件不存在或无权限按不存在处理。

## v1.6.0（2026-08-04）

这版把单篇文档导出 PDF 做成了可用功能。现在在文档详情里可以点“导出 PDF”，后端会按当前用户权限读取这篇文档的当前版本或指定版本，生成中文 PDF，并把导出任务记到数据库里。中文标题、正文、列表、表格、引用和页眉页脚都做了基础排版。导出库或字体不可用时不会吐坏文件，而是把任务标成失败并返回 5030。首版先同步生成，PDF 文件放在 `tmp/pdf_exports`；异步队列、下载端点、过期清理、水印、Word/PDF 解析和 zhparser 仍留后续。

## v1.5.2（2026-08-04）

这版不加新功能，专门把两个验证欠账补上：文件夹树现在有可重复的浏览器自动化脚本，会真的打开 Chrome/Edge、登录、进入文档视图、新建子文件夹、把文档移动进去，再从后端确认移动成功；时间线也补了真实 PostgreSQL 大数据性能 smoke，用 620 篇临时文档和 240 条链接跑真实服务路径，确认会进入降级窗口、只返回 200 条列表、密度统计正确，耗时约 2.7 秒。脚本跑完会清理临时数据。

## v1.5.1（2026-08-04）

这版不加新功能，只把上一个版本发布后的记录补干净：Sprint-20 已经推到 `main`，CI 已通过，`v1.5.0` tag 也已经打到对应提交。之前开发计划里还残留“本地未提交”这类旧说法，这版把它改成准确的发布状态，避免下次续接时误判。

## v1.5.0（2026-08-03）

这版把 Phase2B 的「主题时间线」做成了可用功能。你可以进入时间线视图，按关键词或标签看相关文档什么时候创建、更新、打标签、建立链接；上方密度热条会显示每天事件多少，点时间线里的文档可以回到文档阅读。实现上没有新建一张事件流水表，而是实时从现有文档、标签、链接和正文 chunk 聚合，保持轻量。还顺手把这几次浏览器 smoke 反复中断的 demo 启动根因修掉了：后台启动现在用独立 launcher，不会再因为 AI 执行器结束就把后端带走；备用端口启动前端也不再混入默认 5173 参数。

## v1.4.0（2026-08-03）

这版把上一版只有后端的「文件夹」补成前端真正能用的文件管理器。左侧目录树更接近 Obsidian：界面更简洁，文件夹和单篇文档都有右键菜单，菜单会在点外部、按 Esc 或滚动时自动收起；新建和重命名文件夹改成在左侧栏里直接编辑，不再弹浏览器输入框。批量导入文件夹时也会保留原目录结构，把文档放到对应文件夹里；单篇文档现在也可以通过右键菜单移动到其他文件夹或根目录。之前出现过的「右键能点移动，但实际没移动」问题，这版通过新增并接通单篇文档移动接口修掉了，并且已经做过后端测试、前端构建、运行态 API smoke 和用户浏览器 smoke。

## v1.3.0（2026-08-03）

这版给知识库加了「文件夹」：文档不再只能平铺在空间里，可以建嵌套文件夹（比如「项目A/子1」），把文档归到文件夹下，文件夹能新建、改名、移动、删除、排序。移动时会防环（不能把一个文件夹拖进它自己的子文件夹）和防跨空间；同层重名或文件夹非空（有子夹或有文档）时拒绝删除。文件夹本身不单独设权限，文件夹里的文档仍按原来的私有/团队/外部可见性过滤，不会因为放进文件夹就越权泄露。这版只做后端接口和数据层（能通过 API 操作文件夹，还没接数据库真实跑递归查询），前端文件管理器界面留下一个版本。现有文档默认归到空间根，不受影响。

## v1.2.0（2026-08-02）

这版把工作台整体从「一堆常驻面板的工具台」改成「以文档内容为中心、工具收起来按需呼出」的阅读器范式（参考 Obsidian 那种简洁感），即 Sprint-21 Doc-First UX 收口。主要变化：文档默认是「阅读」单列（只看正文），点「编辑」才出编辑框，点「并排」才是编辑+预览双列；导入从原来的常驻区改成 toolbar「导入」按钮弹窗；快速录入和用户头像挪到顶栏右侧；右侧文档栏改成「版本 / 链接 / 标签 / AI」几个 tab。还修了几个 smoke 反馈：中文标题文档下载 .md 报错（后端文件名编码）、快速录入默认改成直接转文档而不是存草稿、标签在文档侧栏可以直接新建并打标签（不用再跳到标签视图）。后端接口和数据库没动。

## v1.1.0（2026-07-31）

这版交付 Phase2B 的第一个完整功能：AI 润色和写作引用。写文档时选中一段话，可以让 AI 改写得更通顺（润色），或基于知识库里已有的相关内容来续写并标注来源（引用）；满意就应用回正文（自动留一个历史版本可回退），不满意就丢弃。后端（新接口、草稿存储、权限过滤和降级）与前端（右侧润色侧边栏）都完成了，把文档片段发给公司内网 AI 这个数据外发风险也正式通过评审（RG-008 升 Go）。还差最后一步浏览器人工验收，等数据库和 AI 环境起来再补。

## v1.0.0（2026-07-30）

这版是阶段跨越：从 Phase2A（个人知识组织）正式进入 Phase2B（团队 MVP）。本身不新增可演示功能，主要是把阶段指针切过去、把 AI 润色和时间轴从"以后再说"挪到"现在可以做"，并落锤 AI 润色要把文档片段发给公司内网 AI 这个数据外发风险（有条件通过，等第一个功能跑通再正式放行）。是为后续 AI 润色 / 时间轴铺路。

## v0.2.7（2026-07-30）

这版是前端代码结构整理：把原本挤在 App.tsx 里的一大坨视图切换逻辑抽到单独的 WorkspaceMain 文件，主应用文件从 305 行降到 231 行（低于约定的膨胀阈值）。纯粹结构搬运，不改任何业务逻辑，为后面 AI 润色侧边栏腾地方。

## v0.2.6（2026-07-30）

这版补了 Phase2B AI 润色 / 时间轴的前端交互设计（侧边栏长什么样、怎么点击、各状态怎么提示），并修了一个前端构建崩溃的坑——根因是本机 Node 版本太老（≤16）和 Vite 5 不兼容，用 Volta 锁定到 Node 22 后修好。纯文档和配置，没写功能代码，也没切阶段。

## v0.2.5（2026-07-30）

这版确认了 Phase2B 要先做什么、并落锤 AI 润色的数据外发风险。范围定为：REQ-014 AI 润色 / 写作引用做首批核心，REQ-013 / 024 时间轴紧随第二 slice。同时把"允许 AI 润色把真实文档片段经公司内网中转 LLM 外发"这个风险正式接受并配上护栏（只发你有权看的内容、草稿只存哈希和摘要、不做敏感字段自动过滤、AI 不可用时降级不编造），补了 RG-008、相关设计文档和 Sprint-19 / 20 规划。纯文档和决策留痕，没切阶段、没编码。

## v0.2.4（2026-07-21）

这版主要是文档状态校准。把 2026-07-20 输入材料、另一 AI 的评估和仓库事实核对整理成研究记录，并把 Phase2A 相关文档里已经完成的能力状态对齐。它不增加新功能，也不改后端、前端、接口或数据库，只是让文档更准确地反映已有实现。

## v0.2.3（2026-07-20）

这版把 Phase2A 的验收状态收口。快速录入、标签、内链 / 反链等个人知识组织能力已经有实现和测试证据，因此文档不再把 Phase2A 写成待确认或未完成。Phase2B、PDF / Word 解析、zhparser 等仍留到后续确认。

## v0.2.2（2026-07-19）

这版新增面向最终用户的操作手册，覆盖登录空间、文档、批量导入、版本、双向链接、标签、快速录入、搜索、问答、术语、导出和权限等日常使用路径。它是纯文档版本，不新增可演示能力，也不改 API 契约。

## v0.2.1（2026-07-19）

这版重构了前端主应用文件，把 `App.tsx` 里的业务逻辑拆到多个领域 hook 和 helper 文件里，降低主文件体量并修复切换空间后保存 UI 不刷新的问题。用户看到的功能范围不变，主要收益是代码更容易维护。

## v0.2.0（2026-07-19）

这版交付 Phase2A 的“快速录入”能力。用户可以快速记录标题、来源、摘要和标签，选择保留为私有草稿、转成新私有文档，或追加到已有文档；后端、前端、API、数据库迁移和追溯文档都完成了对应闭环。

## v0.1.0（2026-07-18）

这版不是业务功能发布，而是把 LUMEN-DEMO 从沿用母模板版本号切换为项目自有版本机制。此后 `VERSION` 表示 LUMEN-DEMO 自身版本，继承到哪个 `ai-project-template` 版本由 `TEMPLATE-BASE.md` 记录。
