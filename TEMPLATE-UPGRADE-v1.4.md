# 模板升级 v1.4（执行版）

> 决策已锁定，本文是可直接应用到**模板仓库**的执行规格——每项给出具体改动内容（diff/文本）。
> 试点参考：LUMEN 项目（A 路径已验证）。当前模板版本 v1.3 → 目标 v1.4。
> 边界：只提升**方法论**；LUMEN 专属内容（代号/技术栈/具体 REQ/表/接口/design-* 实际内容/愿景叙事）不入模板。

---

## 0. 决策（已锁定）

| # | 决策 | 锁定结果 |
|---|---|---|
| D1 | verification 形态 | **升编号 `09-verification`**（四支柱：需求 00-03 / 总体 04-05 / 详细 06-07 / 计划 08 / 验证 09） |
| D2 | 积累规则位置 | **进 `global-rules §8`**；`project-rules` 模板**不另设 §6**（每项目自动继承，免重复维护） |
| D3 | vision-start `_example` | **后置 v1.5**（v1.4 不做，控体积） |

---

## 1. `ai/global-rules.md`（核心，触发 v1.3 → v1.4）

### 1.1 版本号
```diff
-> **模板版本：v1.3（2026-06-17）**。
+> **模板版本：v1.4（2026-06-18）**。
```

### 1.2 §5 目录标准（D1：00-08 → 00-09；加两类语义命名约定）
```diff
-`docs/` 核心文档固定编号 `00-08`，编号本身不因项目而变；其中 `00-05`、`08` 对所有项目必备，
-`06-db-design`、`07-api-spec` 按项目形态决定（无持久化 / 无对外接口的项目可省略，
-并在 `ai/project-rules.md` §3 声明）。额外的项目专属文档用语义化命名直接追加在 `docs/` 下，
-不占用、不挪动 `00-08` 编号。
+`docs/` 核心文档固定编号 `00-09`，编号本身不因项目而变；其中 `00-05`、`08`、`09` 对所有项目必备，
+`06-db-design`、`07-api-spec` 按项目形态决定（无持久化 / 无对外接口的项目可省略，
+并在 `ai/project-rules.md` §3 声明）。额外的项目专属文档用语义化命名直接追加在 `docs/` 下，
+不占用、不挪动 `00-09` 编号。
+
+两类常见的语义命名约定：
+- **产品愿景/叙事类源文档**：放 `docs/vision/product-vision.md`，头部带"产品愿景叙事·不直接驱动
+  开发"定位声明；它是工程文档的**输入**，与 00-09 物理分离。从愿景起步的项目用 `INIT-PROMPT.md` §0
+  一次性生成完整文档体系。
+- **子系统详细设计**：非平凡子系统用 `docs/design-<子系统>.md`，一个子系统一份，不编号；
+  与 04（总体）/ 06-07（数据·接口）互补，承载子系统内部逻辑。
```

### 1.3 新增 §8 文档演进规则（D2：从 LUMEN `project-rules §6` 提升，已去项目化）
在文末追加整节：

```markdown
## 8. 文档演进规则（积累式）

> 设计类文档（03–09、design-\*）按「完整骨架 + 阶段增量」演进，**只增不删**。
> 框架一次铺满，细节随阶段在原位完善。

### 8.1 两枚标签（每个设计要素必带）
- **阶段标签**：`[P1]` / `[P2]` / `[愿景]`，取自 `docs/03-prd.md` §3 已确认的路线图
- **状态标签**：`骨架` → `P{N}-已设计` → `P{N}-已实现`

### 8.2 演进方式
1. **初次生成**：按完整愿景铺出全部要素并打标签；只把 `[P1]` 写细，其余留 `骨架·待细化`
2. **阶段执行**：在原位补本阶段要素的详细设计，状态推进到 `P{N}-已设计` → `已实现`
3. **升阶段**：当前阶段指针（`project-rules.md` §1）后移；在原位补新阶段要素细节，前阶段内容原封不动
4. **最终态**：愿景实现完毕时，每份文档即完整系统设计

### 8.3 禁止
- 不得为不同阶段建独立文档（如 `06-db-p1.md`），一切在原文件原位演进
- 不得删除既有阶段内容、只保留当前阶段
- 不得把 `[P2]` / `[愿景]` 要素的详细设计提前写死
- 需求层（00-02）跨阶段稳定；阶段调整只动 03 §3 与 `project-rules.md` §1 指针
```

---

## 2. `INIT-PROMPT.md`

### 2.1 新增 §0「从产品愿景文档生成完整文档体系」
**整段拷贝**自 LUMEN `INIT-PROMPT.md` §0（vision → 00-09 + design-\* + verification + 阶段建议的主 prompt）。该 prompt 已在 LUMEN 验证可用，内容通用，无需改动。

### 2.2 §1 / §10 扩至 03-09（D1 连带）
```diff
-## 1. 新项目初始化（生成03-08）
+## 1. 新项目初始化（生成03-09）
```
- §1「依次生成」清单末尾加 `- docs/09-verification.md`（09 对所有项目必备，无条件生成）
- §10 checklist §B 加一项：`- [ ] 09-verification：REQ→用例追溯矩阵覆盖当前阶段全部 REQ`
- §10 §C「08 Sprint ⊆ 05-07」扩为「08-09 ⊆ 05-07」

---

## 3. `ai/project-rules.md`（模板骨架）

### 3.1 §5.2 禁区补一条
```diff
 - 不得自行实现的功能：（原内容）
+- 愿景 / 01 中的功能点不等于已批准实现；阶段归属以 `docs/03-prd.md` §3 路线图为准，编码以 §1 当前阶段为准
```
### 3.2 不设 §6
积累规则已在 `global-rules §8`，本文件**不重复**。（LUMEN 现有 §6 在同步后删除，见 §7）

---

## 4. `docs/` 模板骨架

### 4.1 00-08 模板补写法指引（在各模板抬头注释处加一行）
| 模板 | 追加指引 |
|---|---|
| 00-scenario | 「极简：背景/目标用户/典型场景 + 指针行；**不放功能表/Phase 表/非目标**（那些进 01/03）。从愿景抽取时去叙事化」 |
| 01-user-requirements | 「完整功能清单，带阶段标签 `[P1]/[P2]/[愿景]` + 状态；从愿景**全量**抽取，不按阶段裁剪」 |
| 02-srs | 「完整 REQ；当前阶段 REQ 写清可验证口径，远期粗粒度标'待该阶段细化'」 |
| 03-prd | 「§3 阶段路线图 = **阶段标签唯一来源**；AI 在此提议路线图，人工确认」 |
| 04-architecture | 「完整框架：架构图 + 子系统表（每行带 阶段/状态/指向 design-\*）」 |
| 05-tech-spec | 「技术栈 + 关键决策；版本可标'待确认'，不虚构」 |
| 06-db-design | 「完整表清单 + 阶段标签/状态；当前阶段表写全字段/索引，其余骨架」 |
| 07-api-spec | 「完整接口清单 + 阶段标签/状态；当前阶段写请求/响应，其余骨架」 |
| 08-dev-plan | 「当前阶段拆 Sprint；升阶段在原位追加，不删旧 Sprint」 |

> 各模板抬头追加一句：「按 `global-rules.md` §8 积累式演进」。

### 4.2 新增 `docs/09-verification.md` 模板（D1）
```markdown
# 09 验证计划（Verification）

> AI生成初稿，人工确认。测试策略 + REQ→用例追溯矩阵 + 分阶段验证范围。
> 与 08（"何时做"）正交：本文回答"怎么算对"。按积累式演进（global-rules §8）。
>
> 适用条件：所有项目必备（每个项目都需验证）。无条件保留，不按项目形态省略。

## 1. 测试策略
（单元 / 集成 / 验收；数据夹具；与环境）

## 2. REQ → 用例追溯矩阵
（逐 REQ：验收用例 + 阶段标签 + 状态；当前阶段 REQ 必须有用例）

## 3. 分阶段验证范围
- 当前阶段：覆盖哪些 REQ（对应 02-srs 当前阶段段）
- 升阶段：在原位追加新阶段 REQ 的用例（global-rules §8）
```

---

## 5. `README.md`

### 5.1 快速开始加愿景起步分支
**整段拷贝**自 LUMEN `README.md` 快速开始末尾的「若起点是一份产品愿景文档」引用块（指向 INIT-PROMPT §0 + `docs/vision/product-vision.md`）。

### 5.2 版本记录加 v1.4 条目
```markdown
- v1.4（2026-06-18）：`ai/global-rules.md` 新增 §8 文档演进规则（积累式：完整骨架+阶段标签+状态，只增不删）；§5 目录标准扩为 00-09（新增 09-verification 验证支柱）、新增 `docs/vision/` 源文档与 `design-*.md` 子系统设计两类语义命名约定。同期非 global-rules 改动：`INIT-PROMPT.md` 新增 §0 愿景→完整文档体系主 prompt、§1/§10 扩至 03-09；`ai/project-rules.md` §5.2 禁区补阶段归属条；`docs/` 00-08 模板补完整需求+阶段标签写法指引、新增 09-verification 模板；`README` 快速开始加愿景起步分支
```

---

## 6. 执行步骤（按 README「global-rules 更新同步」流程）

1. [ ] 模板仓库：按 §1 改 `ai/global-rules.md`（版本号 v1.4 + §5 + §8）
2. [ ] 按 §2 改 `INIT-PROMPT.md`（§0 + §1/§10 扩 09）
3. [ ] 按 §3 改 `ai/project-rules.md`（§5.2 补条；不设 §6）
4. [ ] 按 §4 改 `docs/` 模板（00-08 指引 + 新建 09-verification）
5. [ ] 按 §5 改 `README.md`（愿景分支 + v1.4 记录）
6. [ ] 覆盖复制到各进行中项目的对应文件
7. [ ] 各项目提交注明 `sync template v1.4`
8. [ ] 审计：`grep 模板版本` 比对，落后者补同步

---

## 7. LUMEN（试点）同步后收尾

- [ ] 删除 LUMEN `ai/project-rules.md` §6（规则已在 global-rules §8），或改为「见 global-rules §8」一行指针
- [ ] LUMEN `docs/verification-plan.md` → 重命名/迁移为 `docs/09-verification.md`（D1 升编号后，语义档归位），更新引用
- [ ] 其余 LUMEN 专属内容（代号/技术栈/REQ/表/接口/design-*/vision）**不动**
- [ ] 确认 LUMEN 跑 §0 端到端仍正常（回归）

---

## 8. 验收（v1.4 发布后）

- [ ] 新建空项目复制模板 → 放愿景到 `docs/vision/product-vision.md` → 跑 INIT-PROMPT §0 → 应一次性产出 00-09 + design-\* + 阶段建议
- [ ] 人工确认 03 §3 阶段 → 进 Sprint1，全程无需手写 00-02
- [ ] `grep 模板版本` 各项目均显示 v1.4
- [ ] 抽查某设计文档含 `[P2]`/`[愿景]` 骨架要素（验证积累式写法已进模板）
