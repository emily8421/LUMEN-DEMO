# RESEARCH 文档格式转换输入评审报告（2026-07-21）

> 定位：本文是当前项目 AI 对 `docs/inputs/2026-07-21-doc-format-conversion-requirements.md` 的输入评审，不是权威规格，不直接更新 `01-user-requirements.md`、`02-srs.md`、`05-tech-spec.md` 或 `08-dev-plan.md`。正式采纳需经人工确认后再回填下游文档。

## 1. 文档元信息

| 项 | 内容 |
|---|---|
| 文档类型 | 输入材料评审 / 需求采纳建议 |
| 评估对象 | `docs/inputs/2026-07-21-doc-format-conversion-requirements.md` |
| 评估结论 | Conditional Go |
| 当前状态 | 已评审，待人工确认后回填 01/02/05/08/09 |
| 最后更新 | 2026-07-22（修正版输入材料复核） |
| 复核说明 | 输入材料 2026-07-22 修正后逐条复核，FC-01~04 范围 / 阶段 / 编号占位与初版一致，Conditional Go 结论不变 |

## 2. 总体判断

该输入材料可作为有价值的需求与技术约束输入，但不能直接视为正式需求、架构结论或当前项目 AI 的已采纳研究结论。文内的 FC 编号、U/REQ 编号和阶段建议只能作为候选素材。

本轮不直接改动 `01-user-requirements.md`、`02-srs.md`、`05-tech-spec.md`、`08-dev-plan.md`、`09-verification.md`。原因是格式范围、Excel 解析依赖、源格式检索数据形态、水印参数、独立转换工具权限和临时文件生命周期仍需确认；同时当前 Phase2A 已完成，格式转换相关新增项应作为 Phase1.5B / 后续候选，先走需求确认与 RG / 技术环境评估，再进入编码。

## 3. 采纳建议

| FC-ID | 建议 | 评估 |
|---|---|---|
| FC-01 | 部分采纳 | 建议将范围收敛为 Office-compatible `.docx` / `.xlsx` 到 MD 的候选能力；`.pptx`、旧版 `.doc/.xls/.ppt`、WPS 私有 `.wps/.et/.dps` 暂不纳入。`.docx` 可延展 RG-007；`.xlsx` 需要新增 RG 或技术评估，重点验证 `openpyxl` / `pandas`、多 sheet、合并单元格、公式取值、超大表格截断规则。暂不扩大现有 REQ-009 验收，待确认后可作为候选 REQ-039。 |
| FC-02 | 原则采纳，待确认 | “用于搜索 / RAG 的文本抽取”和“生成可编辑 MD 的结构化转换”应拆成两层。保留源格式的文件只要文本抽取成功，就应参与全文检索与 embedding；是否生成可编辑 MD 是另一个产品选项。正式采纳前需在 `04-architecture.md` / `06-db-design.md` 明确数据形态与字段。 |
| FC-03 | 作为 REQ-027 子验收候选采纳 | 不建议单独开 REQ。建议只做文字水印、默认关闭，随单文档 PDF 导出能力一起受 RG-006 约束；RG-006 未验证前不进入编码。 |
| FC-04 | 暂不直接采纳，待产品边界确认 | 独立格式转换工具是邻接工具能力，不一定属于知识库核心流。若采纳，建议在入口、权限 / 配额、临时文件 TTL、是否绕开知识库文档权限模型确认后，作为候选 REQ-040；能力依赖 REQ-039 与 REQ-027。 |

## 4. 文档影响建议

本次只完成输入评审，不回填正式文档。

若用户确认采纳范围，建议按以下顺序推进：

1. 确认 FC-01 到 FC-04 的产品范围和非目标。
2. 修订 `01-user-requirements.md` 与 `02-srs.md`，再分配正式 U-ID / REQ-ID / AC-ID。
3. 修订 `04-architecture.md`、`05-tech-spec.md`、`06-db-design.md`、`07-api-spec.md`，补齐转换管线、依赖 RG、数据字段和接口。
4. 修订 `08-dev-plan.md` 与 `09-verification.md`，形成排期和验收测试。
5. 在实现前补做 RG / tech-env 评估，尤其是 Excel 解析与 PDF 水印能力。

## 5. 待确认事项

| ID | 待确认项 | AI 建议 | 阻塞点 |
|---|---|---|---|
| FCR-C-001 | Word / Excel 转 MD 范围 | `.docx` 优先，`.xlsx` 条件纳入；排除 PPT、旧版 Office 二进制格式和 WPS 私有格式 | Excel 表格降级规则与依赖库未验证 |
| FCR-C-002 | 保留源格式是否参与搜索 / RAG | 参与，前提是文本抽取成功；同时区分可编辑 MD 与源格式存储 | 需要确认数据模型与 UI 状态表达 |
| FCR-C-003 | PDF 水印模板 | 文本水印、默认关闭；先不做图片水印 | 依赖 RG-006 对最终 PDF 库的验证 |
| FCR-C-004 | 独立转换工具产品边界 | 延后到核心转换与 PDF 导出能力确认后再纳入 | 入口、权限、配额、临时文件 TTL 未定 |
| FCR-C-005 | REQ-039 / REQ-040 编号 | 暂不分配正式编号，确认范围后再写入权威文档 | 需避免输入材料建议编号污染正式编号体系 |

## 6. 建议结论

建议将该输入材料保留为 `docs/inputs/` 原始输入，并以本评审报告作为当前 AI 的采纳判断记录。下一步不应直接编码，应先让用户确认 FCR-C-001 到 FCR-C-005，再回填正式文档并安排 RG / 技术环境评估。
