# 总体设计文档评估（04 架构 / 05 技术方案）

> 定位：总体设计阶段评估留痕（Phase2 前置 §10 步骤 0）。对照 `ai/doc-standards/04-architecture.md`、`ai/doc-standards/05-tech-spec.md`。**只读评估**，不改 Phase 指针，不替代 `04/05` 正式修订。落盘于 `docs/research/`。

## 0. 元信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-07-11 |
| 评估对象 | `docs/04-architecture.md`、`docs/05-tech-spec.md` |
| 评估维度 | (A) doc-standards 字段一致性；(B) Phase2 设计就绪度 |
| 结论 | **(A) Go（minor gaps，可顺手补）；(B) No-Go（Phase2 设计未就绪，不足以驱动 Phase2 tech-env-eval 与 06/07 契约）** |
| 依据 | `ai/doc-standards/04-architecture.md`、`ai/doc-standards/05-tech-spec.md`；`docs/04` §1–§7、`docs/05` §1–§6；`docs/research/2026-07-11-phase2-upgrade-evaluation.md` |
| 不做 | 不改 Phase 指针、不启动 Phase2 编码、不新增需求 |

## 1. 结论速览

| 维度 | 结论 | 说明 |
|---|---|---|
| (A) 04 字段一致性 | Go（minor） | 缺独立「架构目标与约束」节；ADR 矩阵缺「理由」列（理由在 §3 散文）；模块表 P2 / 愿景行 IO / 边界为空 |
| (A) 05 字段一致性 | Go（minor-moderate） | 缺「依赖与配置矩阵」（含密钥 / 敏感性列）；缺独立「安全隐私合规」结构化节；关键技术决策无决策 ID |
| (B) 04 Phase2 就绪度 | **No-Go** | MOD-006/007 纯占位（无 IO / 边界 / 流程 / 组件 / ADR）；§5 无 Phase2 Flow；§6 P2 行无 Flow-ID / 下游设计 |
| (B) 05 Phase2 就绪度 | **No-Go** | 无 Phase2 技术决策（实时协作 / 移动端 / PDF / 推送）；§5.1 无 Phase2 readiness gate；无 Phase2 tech-env-eval |

## 2. (A) doc-standards 字段一致性核对

### 04
- ✅ 文档元信息、系统上下文图（§1.1）、容器视图（§1.2 COMP-001..004）、模块划分（§2）、关键流程（§5 Flow-001/002）、部署拓扑（§4）、ADR 矩阵（§3.1 ADR-001..005）、REQ → 模块追溯（§6）、待确认项（§7）均存在。
- ⚠️ 缺独立「架构目标与约束」节：当前 Phase / 交付物形态 / 禁止项 / 权威源 / 下游影响 散见 §0 / §4 / §1，建议补一节集中表达。
- ⚠️ ADR 矩阵缺「理由」列（理由在 §3 散文），可补列对齐 doc-standards §5。
- ⚠️ 模块表 P2 / 愿景行（MOD-006..010）输入 → 输出 / 边界为「—」，属 (B) 问题。

### 05
- ✅ 技术栈与版本（§1）、关键技术决策（§2）、运行环境与资源（§5）、技术环境评估结论引用（§5.1）、Phase 技术约束（§3）、Readiness gate（§5.1 RG-001..005）、待确认项（§6）均存在。
- ⚠️ 缺「依赖与配置矩阵」（类型 / 名称 / 用途 / 启用阶段 / 当前状态 / 配置来源 / 密钥敏感性 / 验证方式）——依赖散见 §1 技术栈表与 §5.1；建议补一张含密钥 / 敏感性列的矩阵（doc-standards §3）。
- ⚠️ 缺独立「安全、隐私与合规」结构化节（散见 §5 数据边界 + `04` §1.1 外发边界）。
- ⚠️ 关键技术决策无决策 ID（如 TCD-001），不便追溯。

> (A) 不阻塞 Phase2；可在后续 `04/05` 补强 PR 顺手对齐（与刚完成的 00-03 规范化一致）。

## 3. (B) Phase2 设计就绪度核对（重点）

### 04
- MOD-006 标签与视图 [P2]：职责有，但**输入 → 输出 / 边界 =「—」**，无关键流程、无新增组件、无 ADR，详细设计「待 P2 建」。
- MOD-007 协作与推送 [P2]：同上，纯占位。
- §5 关键流程仅 Flow-001/002（P1）；**无实时协作 / 跨空间推送 / 移动端流程**。
- §1.2 仅 COMP-001..004（P1）；**无 Phase2 新组件**（如实时协作服务、推送服务、移动端）。
- §6 REQ-012..017/024..027 行：Flow-ID =「—」，下游设计 =「升 Phase2 时细化」。
- §3 无 Phase2 架构决策（实时并发 OT/CRDT、移动端、跨空间副本模型均无）。

### 05
- §1 仅锁 Phase1 技术基线；**无 Phase2 技术决策**（实时协作栈、移动端、PDF 导出库、副本同步）。
- §5.1 仅 RG-001..005（Phase1）；**无 Phase2 readiness gate**（RG-006.. 待评估：实时协作、移动端、PDF、跨空间推送）。
- §3 Phase 约束仅 P1 允许 / 禁止；无 Phase2 口径。
- `docs/research/` 仅 Phase1 tech-env-eval，无 Phase2。

→ **(B) 结论：`04/05` 当前不足以驱动 Phase2 tech-env-eval 与 `06/07` 契约。** 评估对象（Phase2 模块 / 技术 / 门禁）基本缺失。

## 4. 建议的补强项（AI 建议，待确认）

先补 `04/05` 的 Phase2 设计骨架（再跑 tech-env-eval 与 `06/07` 才有依据）：

**04 补强（P2-已设计骨架级）：**
- MOD-006/007 补「输入 → 输出 / 边界（不负责）/ 关联组件 / 详细设计入口」。
- §5 新增 Phase2 关键 Flow（实时协作、跨空间推送、移动端访问）的成功 / 异常 / 降级 / 权限路径（骨架）。
- §1.2 补 Phase2 新增组件（如协作服务、推送服务、移动端）。
- §3 补 Phase2 关键 ADR（协作并发模型 OT/CRDT 候选、移动端方案、跨空间副本模型）——状态标「候选 / 待技术验证」。

**05 补强：**
- §1 / §2 补 Phase2 关键技术决策（候选状态）：实时协作栈、移动端、PDF 导出、跨空间副本同步。
- §5.1 补 Phase2 readiness gate（RG-006..：实时协作、移动端、PDF、推送），状态标「待评估」，解锁条件指向 Phase2 tech-env-eval。
- 顺手补 (A) 的「依赖与配置矩阵」「安全隐私合规」节、决策 ID。

## 5. 与 Phase2 升级评估的关系 / 修订前置顺序

本评估细化 `docs/research/2026-07-11-phase2-upgrade-evaluation.md` §10 的步骤 0：`04/05` 字段一致性可接受，但 **Phase2 设计就绪度未过**。修订后的 Phase2 前置顺序：

0. ~~`04/05` 评估~~（本报告，已完成）
1. **人工确认 Phase2 MVP 范围**（REQ-012..017/024..027 取舍）← 真正的第一阻塞，决定补强范围
2. **`04/05` Phase2 设计补强**（按确认范围补 MOD-006/007 骨架 + Phase2 ADR + `05` Phase2 技术决策 / RG-006..）
3. Phase2 tech-env-evaluation（对真实依赖给 Go / Conditional-Go）
4. `06/07` MVP 级契约
5. 复跑 Phase2 升级评估 → 阻塞关闭后再切 Phase1 → Phase2 指针

每步独立小 PR；未关闭前不切指针、不启动 Phase2 编码。

## 6. 待人工确认项

| ID | 待确认项 | AI 建议 | 建议依据 | 阻塞关系 |
|---|---|---|---|---|
| OD-C-001 | 是否先做 `04/05` Phase2 设计补强（步骤 2） | 建议在确认 Phase2 范围（步骤 1）后做 | (B) 未就绪；tech-env-eval / `06/07` 依赖 `04/05` Phase2 设计 | 阻塞 Phase2 tech-env-eval 与 `06/07` |
| OD-C-002 | Phase2 MVP 范围 | 待用户确认 12 个 P2 REQ 取舍 | `03` §3 Phase2 退出标准 = 待细化 | 阻塞 `04/05` 补强范围；真正的第一阻塞 |
| OD-C-003 | (A) 字段一致性 minor gaps 是否顺手补 | 建议在 `04/05` 补强 PR 一并补 | doc-standards 对齐 00-03 规范化 | 不阻塞 |
