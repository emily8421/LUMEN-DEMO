# 设计说明模版

> 转换自 `../Doc_ref/` 同名原件（原名含已脱敏的项目前缀）。Word 二进制流解析，无样式：标题按原文目录映射重建，表格按分隔符重建，页眉页脚与目录条目已清理，图表类内容不可见。项目编号/单位/人名已替换为占位符。原文为准；本 md 供快速阅读与 AI 检索。

设计说明

## 1. 概述

### 1.1. 编写目的

本文档的编写目的是：详细定义×××××软件的总体功能；给出系统的结构设计和过程设计，作为程序编写的依据。

### 1.2. 参考资料

【包括：
a. 项目来源；
b. 本文档中引用到的规范和资料等；
c. 列出这些规范和资料的作者、编号、标题、发表日期、出版单位或资料来源。可包括：项目经核准的计划任务书、合同或上级机关的批文；项目开发计划；需求规格说明书；测试计划（初稿）；用户操作手册（初稿）；文档所引用的资料、采用的标准或规范。】

### 1.3. 术语和缩写词*

列出本文档中用到的专门术语的定义和缩写词，缩写词要给出中文译名和英文全称，常用的不需要定义。

## 2. 需求概述

概述系统的特性和需求，扩充软件需求说明中的信息，给出增加的细节，详尽地指出对软件需求说明中有关特性和需求作出的变更。（详细说明见《需求说明》）。

## 3. 总体设计

### 3.1. 结构设计

【用一览表及框图的形式说明本系统的系统元素（各层模块、子程序、公用程序等）的划分，扼要说明每个系统元素的标识符和功能，分层次地给出各元素之间的控制与被控制关系。
如果采用结构化设计方法，需绘制软件结构图（Software Chart）。
如果采用面向对象设计方法，需绘制顺序图（Sequence Diagram）等进行描述。
对系统总体结构设计和所绘制图形加以文字说明。】

### 3.2. 功能分配

说明《需求分析》文档当中各项功能同总体结构中各模块的对应关系。

### 3.3. 接口设计

#### 3.3.1. 外部接口设计

【此处填写外部接口设计的目的、依据】

##### 3.3.1.1. 用户界面

【此处填写用户界面设计的规则，以及使用 visio 设计的用户界面图。如果图形过多，请使用附件】

##### 3.3.1.2. 软件接口

【参考：运行于 Windows2000 及更高版本具有 WIN32 API 的操作系统之上。】

##### 3.3.1.3. 硬件接口

【参考：支持一般 x86 系列微机和 Windows2000。】

#### 3.3.2. 内部接口设计

对系统各构件（模块）之间的接口进行说明。可参考使用表格：

| 模块及标号 | 被调用模块 | 被调用触发消息 | 调用模块 | 调用触发消息 |
|---|---|---|---|---|
| | | | | |

### 3.4. 数据结构设计

#### 3.4.1. 类图设计

【此处摘录 Rose 中的相关类图。】

#### 3.4.2. 公共数据结构设计

对程序当中所定义的全局变量、全局数据结构或类当中 Public 数据结构进行定义和说明。

#### 3.4.3. 数据结构同程序的关系

数据结构影响最后的构件（模块）过程设计。因此，需要说明数据结构如何影响程序的数据组织、存取方式、结合程度和处理方法。

### 3.5. 数据库设计*

对程序当中所采用的数据库相关的名称和标识符、在数据库当中的位置、定义、度量单位、格式和值域、敏感程度、数据项名、缩写词和代码，包括用于数据库的规格说明等进行定义。建议给出数据库表结构。

### 3.6. 出错处理设计*

包括对出错输出信息、出错处理对策的说明。

### 3.7. 其他*

说明设计当中对于安全保密、维护等情况的设计考虑。

## 4. 构件（过程）设计

对系统当中主要的构件（模块）逐一进行说明。

### 4.1. 构件（模块）1设计

- **4.1.1. 功能说明**：用文字形式简单的描述本构件（模块）完成的主要功能和输入输出接口。
- **4.1.2. 算法**：详细描述构件（模块）的实现算法，可以采用用流程图、盒(N－S)图、PAD图、HIPO图、判定表及PDL语言等手段进行描述。
- **4.1.3. 输入**：说明输入。
- **4.1.4. 输出**：说明输出。

### 4.2. 构件（模块）2设计

……其它模块的设计要求同上说明。
