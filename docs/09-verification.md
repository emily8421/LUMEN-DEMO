# 验证计划（Verification Plan）

> 测试策略与 REQ → 用例追溯。按阶段增量：本期覆盖 `[P1]` 全部 REQ；升阶段在原位追加（global-rules §8）。
> 与 `08-dev-plan`（"何时做"）正交：本文回答"怎么算对"。
>
> 定位：本项目「验证」支柱，对应 global-rules §5 编号档 `09-verification`（由语义命名 `verification-plan.md` 升格而来）。

## 0. 文档元信息

| 项 | 内容 |
|---|---|
| 当前 Phase | **Phase2D（账户与多人权限）已完成（2026-08-07 收口）：Sprint-26 账号体系基础（TC-P2-AUTH-001）+ Sprint-27 权限多人化（TC-P2-ACC-001）+ Sprint-28 角色分层 + 用户管理 + 团队空间加入（TC-P2-ACC-002）三 slice 全部验收通过、退出标准达成；Sprint-27 P2 两项 + Sprint-28 偏差经用户确认全接受、留后续；不升 Phase，下一阶段范围待用户定义**；Phase2B（团队 MVP）已完成（2026-08-05 收口）；Phase2C（本地知识源接入）已完成（2026-08-06） |
| 交付物形态 | Demo / 个人可用 Alpha / 个人知识组织 |
| 覆盖 REQ | Phase1：REQ-001..REQ-011、REQ-036；Phase1.5A：REQ-037/038；Phase1.5B：REQ-027；Phase2A：REQ-026/012/025；**Phase2B：REQ-014 首批核心（后端已实现，RG-008 升 Go，2026-07-30）+ REQ-013a 主题时间线 / REQ-024 第二 slice（已完成，运行态 API smoke + Edge headless 浏览器 smoke 通过）+ REQ-039 文档目录树第三 slice**；**Phase2C：REQ-018 模式 B 本地知识源接入（RG-009 Go 2026-08-05，TC-P2-VAULT-001）**；**Phase2D：REQ-040/041/042 账号体系基础（Sprint-26 已完成，TC-P2-AUTH-001 自动化通过 2026-08-07）+ REQ-043/044 权限多人化（Sprint-27 已完成，TC-P2-ACC-001 通过 2026-08-07）+ REQ-045..047 角色分层 / 用户管理 / 团队空间加入（Sprint-28 已完成 2026-08-07，TC-P2-ACC-002 通过）**；其余愿景验证项待升阶段 |
| 当前状态 | **维护态**（2026-08-07 收口）。Phase2D（账户与多人权限）三 slice 验收全通过（TC-P2-AUTH-001 / ACC-001 / ACC-002），下一阶段范围待用户定义；无未完成验证项。各 TC 状态见 §2 追溯矩阵与 §5 验收记录。 |
| 最后更新 | 2026-08-07（Phase2D 收口：Sprint-26/27/28 三 slice 全部验收通过、退出标准达成；Sprint-27 P2 两项 + Sprint-28 偏差经用户确认全接受、留后续；不升 Phase，下一阶段范围待用户定义）；前次 2026-08-07（Sprint-26/27/28 实现 + 验收） |

> **阶段验收线**（历史指针；各 TC 详情见 §2 矩阵 / §5 验收记录，不在此重复）：
> - **Phase1 Demo**：全量验收 Conditional Go（TC-P1-001..017）。
> - **Phase1.5A**：TC-P1-015 / 016。
> - **Phase1.5B**：TC-P1-017（Sprint-18 PDF 导出；v1.7.0 下载闭环）。
> - **Phase2A**：TC-P2-LINK-001 / TAG-001 / QUICK-001。
> - **Phase2B（团队 MVP）**：2026-08-05 收口。TC-P2-AI-001（REQ-014 / v1.1.0）+ TC-P2-TL-001（REQ-013a/024 / v1.5.0）+ TC-P2-FOLDER-001（REQ-039）；数据外发风险已接受（RG-008 Go 2026-07-30）。
> - **Phase2C（本地知识源接入）**：2026-08-06 完成。TC-P2-VAULT-001（REQ-018 模式 B / RG-009 Go 2026-08-05 / PR#108 / v2.0.0）。
> - **Phase2D（账户与多人权限）**：2026-08-07 收口。TC-P2-AUTH-001（Sprint-26 / RG-011/012/013 Go / PR#112 / v3.0.0）+ TC-P2-ACC-001（Sprint-27 / PR#114）+ TC-P2-ACC-002（Sprint-28 / PR#117 / v3.1.0）。Sprint-27 P2 两项 + Sprint-28 偏差经用户确认接受、留后续。

## 1. 测试策略

- **单元测试**：service 层——权限过滤、切块、检索排序、导入解析
- **集成测试**：导入 → 检索 → 问答 端到端闭环（双空间、三级权限场景）
- **验收测试**：逐条 REQ 的可验证口径（见 §2 矩阵）
- **数据夹具**：`nova-internal` / `brightlite-team` 两空间 + 私有 / 团队共享 / 外部只读 各若干文档

## 2. REQ → 用例追溯矩阵

### Phase1（功能范围 `[P1]` · 交付物形态 **Demo**）

| TC-ID | REQ | 验收用例 | 阶段 | 状态 |
|---|---|---|---|---|
| TC-P1-001 | REQ-001 隔离 | brightlite 账号查询 nova-internal 文档 = 0 命中 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-002 | REQ-002 切换 | 切换后搜索 / 问答只反映目标空间 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-003 | REQ-003 权限 | 作者私有文档，同空间他人搜不到、问答不引用 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-004 | REQ-004 CRUD | 创建 / 读 / 改 / 删均成功，删后不可检索 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-005 | REQ-005 行内编辑 | 编辑保存后内容持久、再打开一致 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-006 | REQ-006 版本 | 3 次修改 → 3 版本 → 能恢复指定版本 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-007 | REQ-007 搜索 | 已知关键词 / 语义相近问题返回正确文档 | [P1] | 条件通过（hybrid：关键词 + `ts_vector` SQL 候选 + pgvector 语义召回；zhparser 可选回退 `simple`） |
| TC-P1-008 | REQ-008 RAG | 库内问答正确 + 标来源；库外回复"未找到" | [P1] | 通过（真实 LLM·glm-5.2 + 向量召回·bge-small-zh pgvector ANN threshold 0.6；RG-001/002 Go） |
| TC-P1-009 | REQ-009 导入 | 导入 `.md` / `.txt` 已提取文本后可搜、可问答引用 | [P1] | 条件通过（真实 Word / PDF 解析未验证，留后续） |
| TC-P1-010 | REQ-010 OCR | 中文白板照片 OCR 后可搜 | [P1] | 后续阶段（OCR 未实现，降级移出 P1 必过） |
| TC-P1-011 | REQ-011 桌面端 | Chrome / Edge 完成上述全部 | [P1] | 条件通过（降级口径·Edge Headless + Chrome 人工 smoke） |
| TC-P1-012 | REQ-036 术语管理 | 新建空间术语后，文档识别该词，问答优先使用空间定义且不被同名全局术语覆盖 | [P1] | 条件通过（PG 仓储·Sprint-8 起真实化） |
| TC-P1-013 | REQ-011 P1A 结构聚焦 | 文档 / 搜索 / 问答 / 术语一级视图切换；900px 桌面宽度不横向破版；P0 能力不回退 | [P1] | 通过（构建 + Chrome / Edge 900px smoke） |
| TC-P1-014 | REQ-011 P1B 工作台重设计 | TopBar + Nav Rail + Context Pane + Workspace 三层布局；Context Pane 随视图变化；900px 桌面宽度不横向破版；信息密度达标；P0/P1A 能力不回退 | [P1] | 通过（构建 + Chrome / Edge 900px smoke）；**Sprint-21 slice 3a/3b/3c/3d 回归通过**（栏默认收起 + 首页默认落地 + documents 空态/返回 + 导入入口迁移 + 3b 单列阅读/编辑/并排 + 顶栏快速录入/头像 + inspector 版本/链接/标签/AI tabs / 900px 不回退，2026-08-01 用户本地 smoke 确认） |
| TC-P1-015 | REQ-037 批量导入 | 拖入文件夹 / 多文件后全部 `.md`/`.txt` 入库可搜可问答；标题保留路径前缀；同名跳过（**Phase2B folder-tree 扩展**：`preserve_structure=true` 保留真实目录结构，见 TC-P2-FOLDER-001） | [P1] | 通过（Sprint-16 自动化 + Chrome headless drop-zone smoke）；**Sprint-21 slice 3d 导入弹窗入口形态回归通过**（2026-08-01 用户 smoke 确认）；**Phase2B `preserve_structure` 后端/API 扩展通过**（task-028，2026-08-03）；前端文件管理器基础能力已实现并通过用户浏览器 smoke（2026-08-03），浏览器自动化 smoke 已补（2026-08-04，见 TC-P2-FOLDER-001） |
| TC-P1-016 | REQ-038 导出备份 | 文档详情下载 `.md`；空间导出 ZIP 含可见文档、权限过滤 | [P1] | 通过（Sprint-17 后端 tests + 端到端 HTTP smoke） |
| TC-P1-017 | REQ-027 单文档 PDF | 选型 + 中文 PDF 导出验证（RG-006）；文档详情导出 PDF 并直接下载，中文正常 | [P1] | 通过（Sprint-18：API-019 生成 + 前端入口 + 中文产品样例；v1.7.0 下载闭环补充通过） |

> 状态说明：Sprint-2~6 按**降级口径**验收（原内存 `demo_repository`）；Sprint-7/8 真实化后 RAG 已走真实 LLM（GLM-5.2）+ 向量召回（pgvector），存储切到 PostgreSQL（见 §5 Sprint-7/8 记录）。仍降级的：真实 PDF/Word 解析、OCR（RG-003，后续阶段）。search 已在 task-009 升级为 hybrid（关键词 / ts_vector / pgvector 语义召回），zhparser 为可选回退。「条件通过」= 当前实现满足 Demo 级别验收；详见 §6 与 `docs/05-tech-spec.md §5.1`。 TC-P1-013 / TC-P1-014 为 REQ-011 体验收口增量，不改变 Phase1 Demo closure 结论。**2026-07-15 标注校准**：TC-P1-001~006/012 原标「降级口径·内存」，因运行时仓储自 Sprint-8 起为 `repository = PgRepository()`（内存降级已解除），已校准为「PG 仓储·Sprint-8 起真实化」；是否进一步将这些 TC 升级为「通过」待人工确认（见 `08` 待确认项）。

#### Phase1 TC 用例详情

> P2-E 回梳新增（C-4：引入稳定 TC-ID）。`TC-P1-NNN` 与 REQ 一一映射；自动化位置指向 `tests/backend/`，证据见 §5。

| TC-ID | REQ | 前置 | 步骤 | 期望 | 自动化 / 证据 | 状态 |
|---|---|---|---|---|---|---|
| TC-P1-001 | REQ-001 | nova / brightlite 双空间 + 成员 | brightlite 账号搜索 nova 文档 | 0 命中 | `tests/backend/test_permission.py`、`test_search.py` | 条件通过 |
| TC-P1-002 | REQ-002 | 用户属多空间 | 切换空间后搜索 / 问答 | 仅目标空间结果 | `tests/backend/test_space.py`、`test_api_routes.py` | 条件通过 |
| TC-P1-003 | REQ-003 | 作者私有文档 + 同空间他成员 | 他成员搜索 / 问答该私有文档 | 不命中、不引用 | `tests/backend/test_permission.py` | 条件通过 |
| TC-P1-004 | REQ-004 | 已登录 + 当前空间 | 创建 / 读 / 改 / 删文档 | 四项成功，删后不可检索 | `tests/backend/test_document.py` | 条件通过 |
| TC-P1-005 | REQ-005 | 已有文档 | 行内编辑保存后重开 | 内容持久、一致 | `tests/backend/test_document.py` | 条件通过 |
| TC-P1-006 | REQ-006 | 已有文档 | 改 3 次 → 查版本 → 恢复 | 3 版本可见、能恢复指定版本 | `tests/backend/test_document.py` | 条件通过 |
| TC-P1-007 | REQ-007 | 已索引文档 | 关键词搜索 / 语义搜索 | 返回正确文档 + 摘要；无直接关键词重叠时可通过向量召回 | `tests/backend/test_search.py`（+ `test_api_routes.py::test_search_api_returns_vector_semantic_hits` PG 集成） | 条件通过（hybrid search） |
| TC-P1-008 | REQ-008 | 已索引文档 | 库内问答 / 库外问答 | 库内正确 + 来源；库外「未找到」 | `tests/backend/test_rag.py`（+ `test_api_routes.py` PG 集成）+ GLM-5.2 真实问答 | 通过（真实 LLM + 向量召回） |
| TC-P1-009 | REQ-009 | `.md`/`.txt` 文件 | 导入后搜索 / 问答 | 可命中 | `tests/backend/test_imports.py` | 条件通过（仅 .md/.txt） |
| TC-P1-010 | REQ-010 | 中文白板照片 | OCR 后搜索 | 可搜 | — | 后续阶段（OCR 未实现） |
| TC-P1-011 | REQ-011 | 桌面端浏览器 | 走查全部 P1 功能 | 全部可用 | Edge Headless + Chrome 人工 smoke（§5） | 条件通过 |
| TC-P1-012 | REQ-036 | brightlite 空间 | 新建术语 → 文档识别 → 问答 | 识别 + 空间术语优先于全局 | `tests/backend/test_term.py`、`test_rag.py` | 条件通过 |
| TC-P1-013 | REQ-011 | P1A 前端实现 + Chrome / Edge 桌面 | ① 登录并切换文档 / 搜索 / 问答 / 术语视图；② 在 900px 宽度完成文档、搜索、问答、术语主流程；③ 回归 Markdown 渲染、来源点击、删除 / 恢复二次确认 | 视图切换保持当前空间上下文；各视图主区不被无关面板挤压；900px 无全局横向滚动；P0 能力不回退 | `npm.cmd run build` 通过；Chrome / Edge headless smoke（900px）通过，覆盖登录、四视图切换、文档新建 / 编辑 / 版本恢复 / 删除、Markdown 预览、搜索来源打开、问答、术语新建 / 删除确认 | 通过 |
| TC-P1-014 | REQ-011 | P1B 工作台重设计实现 + Chrome / Edge 桌面；Sprint-21 slice 3a/3c/3d 回归覆盖栏显隐、首页默认落地、documents 减框、空态引导与返回、导入入口迁移后栏显隐 / 900px 不回退 | ① 登录后检查 TopBar + Nav Rail + Context Pane + Workspace 三层布局；② 切换文档 / 搜索 / 问答 / 术语并确认 Context Pane 随视图变化；③ 在 900px 宽度完成文档、搜索、问答、术语主流程；④ 检查搜索首屏 ≥5 条结果、术语首屏 ≥8 条、编辑区 ≥12 行；⑤ 回归 Markdown 渲染、来源点击、删除 / 恢复二次确认；⑥ Sprint-21 追加确认首页非空、左目录展开后主区不消失、空态「展开左目录 / 新建文档」可用、文档页 / 新建态「返回」回到空态；⑦ 3d 追加确认导入入口迁移不破坏栏显隐与 900px 布局 | 工作台布局与 `frontend-workspace-redesign` 原型一致；任务聚焦，不显示无关大卡片；900px 无全局横向滚动；P0/P1A 能力不回退；Sprint-21 追加的默认落地、空态与导入入口迁移不造成主体空白或三栏消失 | `npm.cmd run build` 通过；Chrome / Edge headless smoke（900px）通过，覆盖登录、四视图切换、文档新建 / 编辑 / 版本恢复 / 删除、Markdown 预览、搜索来源打开、问答、术语新建 / 删除确认；Sprint-21：用户本地 Chrome/Edge smoke + TC-P1-014 回归通过（2026-08-01）；3d：`volta run --node 22.17.1 npm run build` 通过 + 用户 smoke 确认导入迁移后回归通过（2026-08-01） | 通过（含 Sprint-21 3a/3c/3d 回归） |
| TC-P1-015 | REQ-037 | 多个 `.md` / `.txt` 文件或文件夹；Sprint-21 slice 3d 入口形态改为 Documents toolbar「导入」→ 居中 modal | 拖入文件夹 / 多文件后批量导入；3d 追加检查 modal 打开 / 关闭 / 文件选择 / 权限 / 逐条结果 / 导入成功刷新；Phase2B 扩展追加检查 `preserve_structure=true/false` | 全部入库可搜可问答；P1.5A 平铺模式标题保留路径前缀；同名跳过；入口迁移不改变 API-029；Phase2B `preserve_structure=true` 时建/复用真实 folder 并回填 `folder_id`，`false` 保持标题前缀兼容 | `tests/backend/test_imports.py`（批量成功、失败隔离、同名跳过、可搜可问答、preserve_structure 默认保留结构、同名文件跨 folder 共存）；`tests/backend/test_import_api.py`（API-029 响应契约 + folder_id）；`npm.cmd run build`；Chrome headless drop-zone smoke（历史）；Sprint-21 3d：`volta run --node 22.17.1 npm run build` 通过 + 用户 smoke 确认 modal 入口形态回归通过（2026-08-01）；task-028：临时 PG import smoke 通过（2026-08-03）；task-029：用户浏览器 smoke 确认文件管理器基础能力通过（2026-08-03）；v1.5.2：`scripts/smoke-folder-tree-browser.mjs` 通过（2026-08-04） | 通过（含 3d 入口形态回归 + Phase2B 后端/API 扩展 + 前端文件管理器用户 smoke + 浏览器自动化 smoke） |
| TC-P1-016 | REQ-038 | 当前空间存在多篇可见 / 不可见文档 | 文档详情下载 `.md`；空间导出 ZIP | 单文档 `.md` 内容正确；ZIP 只含可见文档 | `tests/backend/test_export.py`（13 tests：单文档可见 / 不可见 / 指定版本、ZIP 只含可见 / 空空间 / 路径前缀 / 防穿越 / 非成员）+ FastAPI TestClient 端到端 HTTP smoke（路由注册 / 异常 handler / 二进制 Response / 不可见 4004 / 非法 format 4220 / 无 token 4001） | 通过 |
| TC-P1-017 | REQ-027 | RG-006 已 Go；当前用户可读文档 | 对可读文档指定版本发起 PDF 导出；验证中文、失败态、权限态和产物下载 | 导出任务与版本绑定；中文正常；产物继承源文档权限；导出库不可用返回 5030；下载端点未就绪返回 4090，artifact 不存在 / 越界不泄露 | RG-006 前置已通过：`scripts/smoke-pdf-rg006.py` 生成 2 页中文 PDF，Poppler 渲染 PNG 非空，pdfplumber 抽取关键中文文本命中，人工视觉检查正常；Sprint-18 产品验证：`tests.backend.test_export` 20/20 OK、backend discover 196 OK（skipped=2）、frontend build 通过；样例 `tmp/pdf_exports/Sprint18 产品 PDF 验证-v1-export-1.pdf` 经 `pdftoppm` 渲染、人工 PNG 检查、`pdfplumber` 抽文本与 PIL 非白像素检查通过；v1.7.0 下载闭环补充：`.venv\Scripts\python.exe -m unittest tests.backend.test_export` 27/27 OK，backend discover 203 OK（skipped=2），`volta run --node 22.17.1 npm run build` 通过（259 modules），运行态 demo API smoke 下载 prefix `%PDF` | 通过 |

> TC-P1-010 暂为「后续阶段」，待 OCR 落地后补步骤与证据；TC-P1-013 已随 PR #76 合并后完成前端构建与 Chrome / Edge 900px smoke；TC-P1-014 已随 Sprint-10（P1B）完成前端构建与 Chrome / Edge 900px smoke，并随 Sprint-21 slice 3a/3c/3d 完成本地回归（2026-08-01 用户确认）；TC-P1-015 已随 Sprint-16 完成自动化验证与 Chrome headless drop-zone smoke，并随 Sprint-21 slice 3d 补入口形态回归（2026-08-01 用户确认）；TC-P1-016 已随 Sprint-17 完成后端 tests + 端到端 HTTP smoke；TC-P1-017 已随 Sprint-18 完成 API-019 产品验收；其余 TC 自动化均含于后端测试与既有 smoke 证据（见 §5）。

### Phase2（功能范围 `[P2]` · Phase2A **个人知识组织** / Phase2B **团队 MVP**）

> Phase2A 已完成 REQ-026 / REQ-012 / REQ-025 三个 vertical slice；**Phase2B 已完成并收口（2026-08-05）：REQ-014 首批核心（TC-P2-AI-001）、REQ-013a/024 第二 slice（TC-P2-TL-001）、REQ-039 第三 slice（TC-P2-FOLDER-001）全部验收通过；前端 half 已闭环（2026-07-31 live UI smoke）**。**Phase2D 账号体系基础已完成（Sprint-26，2026-08-07：TC-P2-AUTH-001 自动化通过，RG-011/012/013 Go）**。

| TC ID | 覆盖对象 | 前置条件 | 验证步骤 | 预期结果 | 证据要求 | 状态 |
|---|---|---|---|---|---|---|
| TC-P2-WSG-001 | WSG-001..006；P2-UI-G-001..006；P2 Web vertical slice | `04` WSG 矩阵、`05 §4.1`、`08` Sprint-11 草案、`frontend-interaction §9.3` 均已回填；用户确认执行门禁评审 | 逐项检查 App Shell、目录边界、vertical slice、文件膨胀阈值、验证入口、UI 链路是否有文档锚点；Phase2B 启动前确认是否沿用 / 调整 | WSG-001..006 均有明确证据位置；Phase2A 已由 REQ-026 / 012 / 025 三个 slice 完成 build / smoke；不得跳过 WSG 直接改 `frontend/` | `04/05/08/09/frontend-interaction` 锚点 + 2026-07-14 静态评审记录 + Phase2A closure | 静态评审条件通过；Phase2B 启动前重跑 |
| TC-P2-UI-001 | PG-P2-001/002/003、P2-UI-G-001/006；REQ-012/025/026 页面入口候选 | 少容器清爽稿暂定按当前稿继续；首个 slice 聚焦 REQ-012/026 | 静态检查 `docs/research/prototypes/2026-07-14-frontend-ui-reference-absorbed-prototype.html` 首页、经典目录、文档工作区的信息层级、首屏入口和视觉密度 | 首屏 2-3 个主信息区；少圆角、少边框、少胶囊、少阴影；层级主要靠留白、细分隔线、文字标签和左侧选中线表达；无卡片墙回退 | 原型路径 + 后续截图 / smoke 记录 | 静态评审条件通过·待实现 smoke |
| TC-P2-UI-002 | PG-P2-009、CMP-P2-BREADCRUMB、CMP-P2-LAYER-SEARCH；REQ-012/026 候选 | 使用当前空间 mock 数据；首个 slice 聚焦标签与内链 / 反链 | 静态检查面包屑、当前层级搜索、经典目录 / 列表过滤路径 | 路径定位清楚；搜索只过滤当前空间 / 当前层级可见内容；无结果不泄露跨空间或无权限内容 | 原型路径 + 后续 UI smoke | 静态评审条件通过·待实现 smoke |
| TC-P2-UI-003 | PG-P2-003、CMP-P2-DOC-MODE；REQ-014/026 候选 | 打开示例文档；首个 slice 只用 REQ-026 相关文档工作区能力 | 静态检查预览、编辑、编辑+预览并排、版本 / 来源入口和可调宽说明 | 阅读与预览合并；编辑不破坏阅读；并排预览可用；主要区域可调宽；来源 / 版本可追溯 | 原型路径 + 后续 Chrome / Edge smoke | 静态评审条件通过·待实现 smoke |
| TC-P2-UI-004 | PG-P2-004/005、CMP-P2-QA-SOURCE；REQ-014 候选 | 当前空间已有搜索与问答 mock；完整 AI 润色暂避 | 静态检查搜索 / 问答入口、推荐问题、来源文档 / 术语入口 | 搜索结果高密度但可读；答案限定当前空间并带来源列表行；来源可回看；库外问题与 AI 降级不编造 | 原型路径 + 后续 UI smoke | 静态评审条件通过·非首个 slice |
| TC-P2-UI-005 | PG-P2-007、P2-UI-G-003/004；REQ-013 候选 | 使用小 / 中 / 大集合 mock；关系图不进首个 slice | 静态检查局部关系图候选与大集合降级、无权限、空结果、AI 降级、>80 文档先筛选文案 | 局部关系图仍为候选 / 静态说明，不进入默认实现；大集合先筛选或聚合；不新增真实图谱算法、冲突检测或权限模型 | 原型说明 + 后续设计评审记录 | 静态评审条件通过·后续候选 |
| TC-P2-TAG-001 | REQ-012；`lumen_tags` / `lumen_tag_links`；API-014 / API-027 / API-031 / API-032 | Phase2A 最小版已细化（扁平标签 + 独立标签视图 + 单标签筛选 + 文档详情打标签）；首个 slice REQ-026 已完成 | 创建 / 更新 / 归档标签（API-014/027）；给可见文档打标签 / 移除（API-031）；按标签筛选文档（API-032 `GET /api/tags/{id}/documents`）；跨空间 / 无权限文档不进入 document_count 与筛选结果 | 标签只在当前空间可见；同空间 normalized_name 重名返回 4090；document_count 只统计当前用户可见文档；归档标签不破坏既有 tag_links 历史关联；打标签需文档可写 + 标签同空间 | `tests/backend/test_tags.py` 15 通过 + 浏览器 smoke 通过（Task A `1e4cf48` + Task B `d07688b`） | 通过（Task A `1e4cf48` + Task B `d07688b`） |
| TC-P2-LINK-001 | REQ-026；`lumen_doc_links`；API-018 | Batch B 契约已回填；首个 vertical slice | 在文档内容中登记 `[[wikilink]]`；查询出链 / 反链；target 缺失、无权限、跨空间分别验证 | resolved / unresolved / no_access 状态正确；无权限目标不泄露标题 / 摘要；反链仅返回当前用户可见文档 | `tests/backend/test_doc_links.py`（9 tests）+ TestClient 路由 smoke 5/5；前端 UI smoke 2026-07-16 通过（resolved 跳转 / unresolved 占位 / 反链面板 / pending，alice space10） | 通过（Task A fc2b869 + Task B 6228f3f；UI smoke 4/4） |
| TC-P2-QUICK-001 | REQ-025；`lumen_quick_entries`；API-017 | Phase2A 已实现 | 快速录入 draft；转为新文档；追加到已有文档；关联 tag_ids；丢弃 draft | draft 默认 owner 私有；转换后继承文档权限；非法 tag_ids / document_id 返回 4220；不绕过权限 | `tests/backend/test_quick_entry.py` 17/17 + service 回归 53 + API smoke（draft/create/append + discard + 4220）+ 浏览器 smoke（Task B `bad8fe5`） | 通过（Task A `f771e02` + Task B `bad8fe5`）；**Sprint-21 slice 3b（2026-08-01）前端收敛**：默认 create_document + 移除 draft radio（`useQuickEntry`/`QuickEntryFeature`），后端 draft capture/discard 契约 + 17 tests 保留，draft 改 API 级验证 |
| TC-P2-AI-001 | REQ-014；`lumen_ai_drafts`；API-028；RG-004；**RG-008** | LLM adapter 可用或 Mock 降级；**数据外发风险已接受（RG-008 Go）** | 对可写文档选中文本执行 polish / citation；验证 sources 仅来自可见 chunks；草稿只存 hash + 摘要；LLM 不可用时降级；D-C-001 量化 citation 同步延迟 | 输出保存为 draft；引用可追溯到 chunk / document；库外或无权限来源不进入 prompt / 不返回；草稿不含 API key 与完整敏感原文；5030 / Mock 降级不编造；citation 同步耗时在 MVP 预算内 | 后端 tests（test_ai_polish）+ prompt 边界人工审查 + Chrome/Edge UI smoke（Sprint-19）+ `scripts/smoke-ai-citation-latency.py` | 通过（后端 service 9/9 + 全量 125 OK；RG-008 升 Go；**前端 UI smoke 2026-07-31 live 实跑通过**：真 GLM polish/citation + 应用→版本 + 越权 4004 + 交互点击流 alice 实测；**D-C-001 2026-08-04 量化通过**：真 PG + 真 GLM 3 次 citation，sources=3，p50=5041.79ms，max=5914.06ms，当前不做异步 job；面板可见性缺陷已修，见 §5.1） |
| TC-P2-TL-001 | REQ-013a；REQ-024；API-033；migration 012 | Sprint-19 已落地；folder-tree 基础能力已完成；候选 A 不建 timeline 事件表；API-033 已实现 | 调用主题时间线：`q` 标题 / chunk 命中、`tag_ids` 标签入口、不传 `q` 空间总览、4 类事件、actor、密度热条、零命中 / 非法参数 / 大集合降级；前端进入时间线视图并加载结果；真实 PG 大数据下服务路径可在预算内返回降级结果 | 仅当前用户可见文档事件返回；越权事件零泄露；`created/updated/tagged/linked` 事件与 actor 规则正确（linked=`null`）；density 返回 `event_count/level/ratio/window`；大集合 `degraded=true` 且列表有上限 | `tests/backend/test_timeline.py`（7/7 OK）+ `tests/backend.test_timeline tests.backend.test_document tests.backend.test_tags tests.backend.test_doc_links` 回归（38/38 OK）+ backend discover 190 OK（skipped=2，embedding torch DLL 警告按 text-only fallback 继续）+ `npm run build`（frontend，259 modules）+ 运行态 API smoke 通过（OpenAPI 含 API-033，创建文档/标签，关键词 timeline、tag timeline、空 q 422）+ Edge headless CDP 浏览器 smoke 通过（登录、切到时间线、搜索 Phoenix、事件列表命中，density=1）+ `scripts/smoke-timeline-pg-performance.py` 真实 PG smoke 通过（620 docs + 240 links，density_events=2100，returned=200，degraded=True，window=week，elapsed_ms=2677.61） | **通过**（task-030，2026-08-03；v1.5.2 补真实 PG 大数据性能 smoke，2026-08-04） |
| TC-P2-FOLDER-001 | REQ-039；`lumen_folders` / `lumen_documents.folder_id`；API-034..038；API-029 `preserve_structure` | folder-tree 设计 FT-C-* 确认 + 06/07 契约回填；migration 011 已落地；API-034..038 已实现 | 新建 / 移动 / 排序 / 删除文件夹（API-034..037）；单文档移动到 folder / 根目录（API-038）；导入文件夹后 `preserve_structure=true` 保留目录结构、`=false` 退回标题前缀；防环 / 跨空间 / 重名 / 删非空 folder 分别验证；文档可见性不因 folder 归属泄露；浏览器自动化覆盖目录树渲染、UI 新建子文件夹、UI 单文档移动 | folder CRUD / 移动 / 排序正确；单文档移动只更新 `folder_id`、不新增版本；`UNIQUE(space_id,parent_id,name)` 重名→4090；防环 / 跨空间→4220；删非空→4090；folder **不独立设权限**，文档可见性仍按 `permission` + `space_id` 过滤；现有文档 `folder_id=null` 向后兼容；`preserve_structure=true` 按路径段建/复用 folder，`false` 保持旧平铺标题 | `tests/backend/test_folder.py`（19/19 OK，task-027）+ `tests/backend/test_document.py`（API-038 service/API 覆盖，task-029）+ `tests/backend/test_imports.py` / `test_import_api.py`（11/11 OK，task-028）+ 临时 PG folder/import smoke（2026-08-03）+ `volta run --node 22.17.1 npm run build`（task-029）+ 运行态 API smoke（API-038 出现在 OpenAPI；临时文档移动到 folder 后 `folder_id` 读回一致并已清理）+ 用户浏览器 smoke（2026-08-03）+ `scripts/smoke-folder-tree-browser.mjs` 通过（2026-08-04：登录 → Documents → 目录树渲染 → UI 新建子文件夹 → UI 单文档移动 → API `folder_id` 后验） | **通过**（task-027 + task-028 + task-029，2026-08-03；v1.5.2 补浏览器自动化 smoke，2026-08-04） |
| TC-P2-VAULT-001 | REQ-018 模式 B；浏览器 File System Access + IndexedDB；CMP-P2-TREE 左侧分区；API-029 按需导入 | RG-009 PoC Go（2026-08-05）；`lumen_vault_mounts` 仅元数据；模式 B 后端零新 API | ①授权+刷新恢复 granted；②1000+ 文件本地树；③IndexedDB 本地关键词搜索；④左侧分区视觉隔离 + 本地条目不进团队 RAG；⑤按需单篇/子树导入走 API-029；⑥隐私红线不上传（Network 零请求） | 句柄持久化刷新恢复；本地索引命中；分区徽标；不上传；导入获完整能力 | RG-009 PoC 报告 `docs/research/2026-08-05-rg009-vault-local-mount-poc.md`（8 能力 + 5 场景，CDP 自动实测 + 用户人工 smoke）+ PoC 原型 `docs/research/prototypes/2026-08-05-rg009-vault-local-mount-poc.html` | **通过**（2026-08-06，Sprint-23C：CDP 前提 smoke + 用户人工 6 口径全过；验收期修复布局/单字搜索/主区预览/按钮色） |
| TC-P2-VAULT-002 | REQ-018 模式 B 增强·子树导入 UI；API-029 + `preserveStructure` | task-033 留后续（2026-08-06）；Sprint-24 规划已落 08 | 本地挂载目录树选中任意子文件夹 → 「导入到 LUMEN」→ 上层 DB 出现且目录结构保留；单篇 / 整库入口回归 | 子树导入保留目录；导入后可搜；单篇 / 整库不回归 | build + 浏览器人工 smoke（Sprint-24） | 通过（2026-08-06 人工 smoke，4 项全过） |
| TC-P2-HELP-001 | 帮助体系 L0+L1（user-guide 内容源 + 首次引导 / 空状态）；REQ-011 可用性收口 | design/help-onboarding（新增 2026-08-06）；Sprint-25 规划已落 08 | 新用户路径：登录 → 首次引导 3 步 → 各视图空状态有下一步 → 首页提示「示例文档未建索引」；帮助可检索到「导入」；内容源单一来源核对 | 3 分钟内可完成首篇可搜文档；引导可跳过；不破既有主流程 | build + 浏览器人工 smoke（Sprint-25）+ 内容走查 | 通过（2026-08-06，Sprint-25：build 273 modules 绿 + scripts/smoke-help-onboarding-browser.mjs 自动化通过；人工浏览器 smoke 待用户确认后补记） |
| TC-P2-VAULT-003 | REQ-018 模式 B 增强·FileSystemObserver 自动监听 | RG-010 Go（2026-08-06，Edge 139 实测） | 本地挂载文件夹新增 / 修改 / 删除文件后自动重扫生效（无需手动） | 自动反映变更；手动重扫保留 | 浏览器 smoke（Wave 3 实现时执行） | 已 Go（2026-08-06）；Wave 3 可立项 |
| TC-P2-AUTH-001 | 账号体系基础（注册 / 凭证登录 bcrypt / 登出会话·不透明 token + `lumen_sessions`）；REQ-040/041/042 | Phase2D 立项（2026-08-07）；design/accounts-auth；task-038 | 注册 → 凭证登录 → 鉴权访问；错误凭证失败+锁定+不枚举账号；登出撤销+TTL 过期+续期轮换+多设备会话；demo 开关 PG 强制/内存允许；跨用户隔离不泄露（私有文档仅 owner） | 凭证错误也能登入；登出后仍可用；跨用户泄露 | 后端 tests（`tests/backend/test_auth.py` 20/20 + 全量 222 OK）+ 前端 build 273 modules 绿 + RG-011/012/013 Go；浏览器 smoke PASS（Sprint-26） | **通过（自动化 + 浏览器 smoke，2026-08-07）** |
| TC-P2-ACC-001 | 权限多人化（owner_id 跨用户过滤 + 私有按 owner 过滤 + external 仅 owner 可写 + 跨用户隔离回归）；REQ-001/002/003 扩展（REQ-043/044） | Sprint-27 立项（2026-08-07）；design/accounts-auth §17；task-039 | 两个真实用户同空间：A 私有文档 B 列表 / 搜索 / 问答零命中；external 文档 B 只读不可写（4003）；注册多用户全路径（列表 / 搜索 / 问答 / 时间线 / 目录树 / 标签 / 导出 ZIP / 链接 / 快速录入）跨用户零泄露；空间隔离与切换回归（用户仅能访问所属空间） | 任一路径泄露他人私有文档；外部只读可被非 owner 修改 | 后端 tests（`tests/backend/test_permission.py` 扩展 + 新增多用户隔离用例）+ 既有 TC 回归（TC-P1-001/002/003）+ 浏览器 smoke | **通过（2026-08-07）**：P0 doc-links 源文档可见性修复 + 多用户全路径隔离回归（`test_permission.py` MultiUserIsolationTest + `test_doc_links.py` 4004 用例）→ backend 229 OK（skipped=2）+ 双用户浏览器 smoke PASS |
| TC-P2-ACC-002 | 角色分层 + 用户管理 + 团队空间加入（全局角色 admin/member + admin 域用户管理后台 + space 域成员管理）；REQ-045/046/047 | Sprint-28 编码（2026-08-07）；design/accounts-auth §18；task-040 | 管理接口仅 admin（member 4030）+ 新注册默认 member；admin 用户列表（姓名 / email / 角色 / 状态 / 最后登录）可过滤、改角色、禁用启用（禁用后登录被拒且会话失效、接口不返回 password_hash）；空间 admin 按 email 搜索添加成员 / 改空间角色 / 移除（移除后失去空间访问、非空间 admin 4030）；seed alice=admin / kira·brightlite-member=member；demo 仓储不旁路管理鉴权 | 普通用户可管理他人 / 用户列表泄露敏感字段 / 成员可自行加入任意空间 / 管理鉴权被仓储类型旁路 | 后端 tests（`tests/backend/test_role.py` / `test_admin_users.py` / `test_space_members.py` / `test_api_sprint28.py`）+ 既有 auth / permission 回归 + 浏览器 smoke（`scripts/smoke-sprint28-role-admin-browser.mjs`） | **通过（2026-08-07）**：backend discover 275 OK（skipped=2）；浏览器 smoke PASS（admin/member 双视角 + API 矩阵：member 4030 / 改角色 / 禁用登录 403 / 重复添加 409 / 最后一个 admin 4090 / 搜索 4030 / 移除后失权 4003） |
| TC-P2-TERM-001 | 术语领域树（REQ-048，REQ-036 增强）；`lumen_term_categories` / `lumen_terms` 扩字段；API-051..053 + API-012/013 扩字段 | 维护态增强立项（2026-08-07）；research/2026-08-07-term-domain-tree-analysis（TM-C-001..007 已确认）；migration 017 已落地 | 领域树 CRUD / 移动（防环 / 跨空间 4220）/ 排序 / 删非空 4090 / 同 parent 重名 4090 / 空间隔离（API-051..053）；术语挂领域叶子 + 内容分类 + 来源（API-012/013 扩字段，`category_id` 跨空间→4220）；全局术语只读；前端左栏领域树（全局固定区 + 空间领域树）+ 主区单详情面板 + 阅读/编辑态分离 + 领域下拉 / 分类 datalist / 来源字段；标签 CRUD 前端接线（API-027：hover 编辑 → 重命名 / 描述 / 颜色；归档确认后移除、关联文档保留） | 领域树重名 / 防环 / 删非空 / 跨空间拒绝；术语挂领域 + 分类 / 来源正确；全局术语只读（编辑入口隐藏 + 后端 4003）；主区无冗余列表栏；标签归档不破坏关联 | 后端 tests（`tests/backend/test_term_category.py` 18 例 + `test_term.py` 扩字段回归 2 例）+ 后端全量 298 OK + 真 PG migration 017 应用（`init_db()` schema 校验）+ `volta run --node 22.17.1 npm run build`（291 modules 绿）+ 运行时 API smoke（领域建树 / 术语挂领域 / term_count）+ 用户浏览器验收 | **通过（2026-08-07）**：步 A 后端 + 步 B 前端全绿；用户浏览器验收通过（含 3 轮验收反馈迭代：新建领域入口 / 右键新建术语 / 主区去列表 / 左栏分区调高）；批2b 步2 标签 CRUD 用户验收通过 |
| TC-P2-ASSIST-001 | AI 助手抽屉 + 「基于知识库」开关 + 多轮对话 + LLM 多通道切换（REQ-008 扩展）；`docs/design/ai-assistant.md` | 维护态批3 立项（2026-08-07/08）；API-010 请求体扩 `history` / `use_knowledge_base` / `llm_provider` + 新增 `GET /api/llm-configs`；`run-sprint16-demo.ps1` 加载 `.env` 注入后端 | 右下角 ✨ 悬浮图标 → 抽屉：默认「基于知识库」勾选 → 提问（RAG 带来源可点开）→ 取消勾选通用对话（不检索、无来源）→ 多轮 history 拼 prompt → LLM 通道下拉切换（deepseek 真实出文）→ 命令面板「问 AI」开抽屉预填 → 空间切换清空对话 | 通用对话编造知识库内容；来源泄露不可见文档；显式切换通道后仍走旧通道（闭包丢失） | 后端 tests（`test_rag.py` +7 + `test_llm_adapter.py` +6）+ 后端全量 310 OK + `volta run --node 22.17.1 npm run build`（294 modules 绿）+ `scripts/smoke-ai-assistant-browser.mjs` PASS（含切 deepseek 真实出文）+ 用户浏览器验收 | **通过（2026-08-08）**：AI 抽屉 / 基于知识库开关 / 多轮 / LLM 多通道切换（deepseek 真实出文）用户验收通过；REQ-008 扩展不另编号 |
| TC-P2-VAULT-004 | REQ-018 模式 B 增强·跨设备 vault 元数据 `/api/vault-mounts` | 依赖账户体系（Sprint-26）；migration 015 待落地（014 已用于账户 `lumen_sessions`，vault_mounts 顺延） | 设备 A 挂载 → 元数据入库 → 设备 B 登录可见挂载点列表 | 仅元数据同步，正文不上传 | 后端 tests + API smoke + 浏览器 smoke | 已拍板·待 Wave 3 启动 |
| TEAM-E2E-001 | 真实团队端到端（PG 模式）：账户 / 角色 / 空间成员 / 文档权限全链路（alice 管理员 + kira 成员 + brightlite-member 客户视角）；依据 `docs/env/team-e2e-verification-runbook.md` | v3.3.2（main `766ec09`，PR #118/#119）；PG Demo 运行态 18000/5173 预检通过；历史遗留 E2E 数据已清理 | 全部 18 项：E2E-01（alice 登录 + 管理员入口）/ E2E-02..16（登录 / 空间切换 / 私有·团队·外部文档权限隔离 / 无权访问 4004 / 版本历史与恢复 / 文件夹树移动防环 / 导入保留结构 / 搜索·RAG 权限隔离 / 管理员接口 / password_hash 不泄露）+ E2E-17 持久化（建带唯一标记文档+文件夹 → `-Stop` → `-UsePostgres` 重启 → 全保持）+ E2E-18 清理（缺陷修复后回归通过） | 18 项全部 PASS；kira 跨用户全路径零泄露；导入文档保留 subdir 嵌套且可搜索；LLM 3 通道可用 + RAG 库内带来源 / 库外不编造；E2E-17 持久化（文档重启后 title/team/folder/version/搜索命中完全一致）；E2E-01 浏览器登录 + 管理员入口用户确认通过；E2E-14 浏览器交互（基于知识库开关 / 多轮 / 通道切换 / 空间·用户切换清空）用户确认通过 | 证据：本会话 API 验证输出 + 用户浏览器复测记录（E2E-01/14）；`%TEMP%\lumen-sprint16-demo\runtime.json` + `check-runtime-openapi.ps1` PASS（localhost:5173，见 §5.1 IPv6 说明）；E2E-17 重启前后 API 对照记录 | **PASS**（2026-08-08 完整验证通过）：验证中发现 2 个缺陷均已在验证内修复闭环——导入文档删除 500（§5.1，PR #118 / v3.3.1）+ 切换用户 AI 助手对话未清空（§5.1，PR #119 / v3.3.2）；E2E-18 清理回归通过 |
| TC-P2-ACC-003 | 成员空间可见性配置（REQ-050）；API-054 admin 用户空间查询 + space 域成员 API 复用；`GET /api/spaces` admin 全空间分支 | 维护态批5 立项（2026-08-08）；design/batch-maintenance §3；Sprint-30；无新 migration（复用 `lumen_space_members`） | admin 在用户管理页点用户 → 抽屉显示已加入空间清单 + 各角色；授予新空间 → 该用户空间下拉出现 → 访问文档 / 权限正确；撤销成员后立即不可见；改空间角色即时生效；最后一个空间 admin 保护（4090）；非 admin 调 API-054 → 4030 | 普通成员可查 / 改他人空间归属；授予后用户空间列表未更新；撤销后仍可访问 | 后端 tests（admin 用户空间查询 + 4030 / 4004 + admin 全空间分支回归）+ 浏览器 smoke（用户详情抽屉授予 / 撤销 / 改角色） | **通过（2026-08-09）**：后端 276 tests OK（REQ-050 空间查询 3 用例：member 4030 / 不存在 4004 / joined·available 互补）+ batch5 API smoke PASS（API-054 端到端：admin 200 / member 403 / 不存在 404）+ 前端 build 301 modules 绿；用户详情抽屉即时操作复用 space 成员 API（commit 2），浏览器 UI smoke 留候选（build 绿 + 单测覆盖逻辑） |
| TC-P2-AUTH-002 | 登录密码小眼睛 + 忘记密码自助重置（REQ-051）；API-055 / 056 + migration 018 `lumen_users` reset 字段 | 维护态批5 立项（2026-08-08）；design/batch-maintenance §4 + accounts-auth §19（编码时写）；Sprint-30 | 密码框小眼睛切换显隐；忘记密码 → 输 email → 恒响应"若已注册则已发送"（防枚举，存在 / 不存在响应一致）→ demo 从后端日志取 reset token → 输 token + 新密码 → 重置成功 → 新密码可登录、旧密码失败；token 过期 / 二次使用 → 4010；密码 <8 → 4220；重置后该用户全部活跃 session 吊销（其他设备登出） | 不存在邮箱响应异于存在（枚举）；过期 / 已用 token 仍可重置；重置后旧 session 仍有效；旧密码仍可登录 | 后端 tests（reset request 恒响应 + 防枚举 + 日志 token + DB 存 hash；confirm 成功 / 过期 / 二次 / 密码长度 / session 全吊销）+ 前端 build + 浏览器 smoke（小眼睛 + 忘记密码全流程） | **通过（2026-08-09）**：后端 276 tests OK（REQ-051 reset 9 用例：恒响应防枚举 / 日志 token / DB hash 非明文 / confirm 成功+过期+二次+短密码+session 全吊销）+ batch5 API smoke PASS（API-055 注册 vs 未注册响应一致 / API-056 invalid token 401·短密码 422）+ 真 PG migration 018 应用（reset 3 列 + 稀疏索引 `idx_lumen_users_reset_token`）+ 前端 build 301 modules 绿；**浏览器 UI smoke PASS（2026-08-10，`scripts/smoke-req051-password-ui-browser.mjs`）**：小眼睛双向 toggle（登录 + 注册）+ 忘记密码 modal 两步（email 恒响应提示 → confirm invalid token 失败提示 → 短密码 submit 禁用 → 关闭）；confirm 成功路径 + 防枚举由后端 reset 单测 + batch5 API smoke 兜底（token 障碍：demo 无 SMTP，valid token 只在后端日志） |
| TC-P2-GOV-001 | 测试数据库安全 guard（NFR-005，P0-1）；`tests/backend/pg_test_support.py` 三重 fail-closed + 独立 `lumen_test` + 4 PG 测试面 | 维护态批6 立项（2026-08-11）；rollout §3 P0-1；task-041 | guard 三条件（`LUMEN_ENV=test` + 库名 `_test` 结尾 + `ALLOW_DESTRUCTIVE_TEST_DB=1`）全满足才放行；缺任一 → `UnsafeTestDatabaseError` 不降级 skip；指向开发库 `lumen` 时 PG 可达也拒；默认 `pytest -m "not integration"` 排除 4 PG 面；显式配 `lumen_test` 跑通 4 面；开发库未被 reset | guard 被 skip 吞；开发库被清；默认 unit 误跑 PG 集成 | guard 单测 + 负向 smoke + 默认 unit + 真实 PG integration + 开发库保护复核（评估 §4.4） | **通过（2026-08-11）**：guard 单测 10/10；负向 smoke 指向 `lumen` 被 guard 拒（17 ERROR 非 skip）；默认 unit 排除 4 PG 面；真实 PG integration 47 passed（`lumen_test`）；开发库 `lumen` 保护复核未 reset。PR #124 合并 main `c26bb63` |
| TC-P2-GOV-002 | CI 最小回归门（NFR-006，P0-2）；`backend-test`/`frontend-build`/`backend-lint` 三 job | 维护态批6 立项（2026-08-11）；rollout §3 P0-2（A1/B1）；task-042 | CI 跑后端 unit（`pytest -m "not integration" --strict-markers`）+ 前端 build（含 `tsc -b`）+ ruff（`E4/E7/E9/F`）；A1：test/build advisory→同 PR 升 required、ruff 恒 advisory；B1：eslint 暂缓 P1 | unit/build 永久 advisory；integration 在 default 跑；CI 仍零代码门 | CI workflow 三 job + 首次基线 + 升 required 时机 | **通过（2026-08-11）**：CI 三 job（backend-test required 286 passed / frontend-build required 301 modules / backend-lint advisory 基线 41）；CI 首跑暴露并修复 2 潜伏 bug（`python-multipart` 漏声明 + `llm_adapter` env key 大小写 Linux case-sensitive）；A1 advisory→required 闭环。PR #124 合并 main `c26bb63` |
| TC-P2-GOV-003 | 错误响应契约地基（NFR-007，CQ-P1-005 Slice A）；`model/error_codes.py` ErrorCode IntEnum + ApiError 基类 + `main.py` ApiError handler + Exception 兜底 | 维护态批7 立项（2026-08-11）；rollout §4 轨道3；assessment §4.8；task-043 | 业务码 `code` 单一含义（不与 HTTP 码混用）；未捕获异常走兜底 handler 返回 `{code:5000,msg:"internal error",data:null}` envelope（非 FastAPI 默认裸 500）；ApiError 领域异常→envelope；生产不回传堆栈 / 内部路径 | 未捕获异常返回非 envelope；`code` 字段混 HTTP 码；堆栈 / 内部路径泄露 | 后端单测（`tests/backend/test_error_contract.py`：ApiError 构造 + code→HTTP 映射完整性 + ApiError handler / Exception 兜底 TestClient 断言响应体 `{code,msg,data}`）+ 现有 286 零回归 | **通过（2026-08-11）**：新单测 8/8 + 全量 294 passed（286→294 零回归）/ 47 deselected + ruff 不恶化（advisory 41 基线）+ CI Linux backend-test 294 passed；PR #125 squash 合并 main `176f3a9` |
| TC-P2-GOV-005 | 权限查询边界 scoped query（CQ-P1-004）；`RepositoryProtocol.list_visible_documents(user_id, space_id)` 安全默认查询 + 全量方法 internal 标注 + 用户态路径收口 | 维护态批15 立项（2026-08-12）；rollout §4 轨道3；assessment §4.7；task-047 | repository 层 `list_visible_documents` 与 `can_view_document` 语义一致（space + 成员 + private/owner + team/external）；非成员跨空间返回空；用户态路径（列表 / 搜索 / RAG / 时间线 / 标签 / 目录树 / 导出 / 导入）全部收口到安全默认 API；`ensure_documents_indexed` 等系统内部路径保留 internal 用法 | 任一用户态路径仍全量读取再过滤；新 endpoint 漏调过滤形成越权 | repository 契约测试（双实现方法面 + 行为）+ cross-space / cross-user 负向单测 + 现有权限回归（TC-P2-ACC-001）+ 全量零回归 | **通过（2026-08-13）**：contract 方法面 + demo scope 行为 + pg cross-space / cross-user 负向单测全过；多用户隔离回归（TC-P2-ACC-001）过；默认 pytest **304 passed / 4 skipped** 零回归；mypy **0 error** + ruff passed；CI PR #151 **7 job 全绿**（含 backend-integration 48 用例）；PR #151 squash merge main `945bf8f`，bump v3.8.12 |
| TC-P2-GOV-006 | API 响应契约 response_model·codegen（CQ-P1-006）；62 个 JSON 端点 `response_model=ApiEnvelope[X]` + OpenAPI 快照固定 + CI schema diff 防漂移 | 维护态批16 立项（2026-08-13）；rollout §4 轨道3；assessment §4.9；task-048 | 全部 JSON 端点响应序列化按 Pydantic 模型校验 / 过滤（`backend/model/schemas.py`）；OpenAPI 快照 `openapi/openapi.json` 携带精确字段 / 可空性；`schema-diff` CI required——契约变更未同步快照即失败；二进制端点（md/zip/pdf 下载）明确排除 | 响应模型与既有 HTTP 输出漂移（字段 / 类型 / 可空性失配 → 运行时 500）；前端类型继续手工双写无机器契约 | 本地路由全量 smoke（62 JSON + 3 二进制端点 TestClient + DemoRepository 实测，response_model 零失配）+ 全量单测零回归 + 快照再生成 `git diff --exit-code` 绿 + CI schema-diff required / integration 兜底 | **通过（2026-08-13）**：路由全量 smoke **60/60**；mypy **0 error**（56 files）+ ruff passed + 默认 pytest **304 passed / 4 skipped** 零回归；快照无漂移；`schema-diff` required；integration 48 用例由 CI 跑（本地 docker 不可用未跑）；bump v3.8.13 |
| TC-P2-GOV-007 | 强依赖 fail-fast / readiness（CQ-P1-001）；lifespan PG 模式 init 失败 raise + `/api/health/live` `/ready` 探针 + `DB_NOT_READY=5031` | 维护态批17 立项（2026-08-13）；rollout §4 轨道3 / §8.1；assessment §4.4；task-049 | PG 模式启动 `init_db` 失败 → lifespan re-raise（TestClient 启动 RuntimeError）；demo 模式 init 失败 → 仍启动（容忍）；`/live` 恒 200；`/ready` demo 200 / PG `db.ping()` 失败 503 + code 5031；5031 与 5030 语义分离 | PG 模式带病运行（init 失败仍启动）；ready 复用 5030 与 AI/OCR 语义混淆 | `tests/backend/test_health.py`（live / ready demo / ready PG 503 / PG 启动 RuntimeError / demo 容忍）+ `test_error_contract` 码集+5031 + 全量零回归 | **通过（2026-08-13）**：health 5/5 + 码集断言；mypy **0 error**（57 files）+ ruff passed + 默认 pytest **313 passed / 49 deselected** 零回归；CI PR #154 **8 job 全绿**（含 backend-integration 48 / schema-diff required）；PR #154 squash merge main `dd75329`；bump v3.8.14 |
| TC-P2-GOV-008 | 日志统一（CQ-P2-002）；`print`→`logging` + 静默吞 except 补 `logger.warning` + ruff `T20` 禁 print | 维护态批18 立项（2026-08-13）；rollout §4 轨道3 / §8.1；assessment CQ-P2-002；task-050 | 后端生产代码零 `print`（ruff T20 + grep 复核）；降级 / 静默吞 except 必有 `logger.warning` 记原因（document 索引回填 / rag LLM 降级 ×2 / auth SpaceAccessError）；`scripts/` smoke / CLI stdout print 豁免 | 新代码再引入 `print`；降级路径继续静默吞无诊断证据 | ruff T20 检查 + `grep '^\s*print\s*\(' backend/` 零命中 + 全量单测 / mypy 零回归 | **通过（2026-08-13）**：ruff passed（含 T20）+ mypy **0 error**（57 files）+ 默认 pytest **313 passed / 49 deselected** 零回归 + grep 复核 backend/ 无 print 残留 + T20 负向探针确认可抓 print；CI PR #155 **8 job 全绿**；PR #155 squash merge main `edb5df1`；bump v3.8.15 |
| TC-P2-GOV-009 | 配置集中 + secret 校验（CQ-P2-003）；`backend/config.py`（pydantic-settings）+ 生产弱默认 signing key fail-closed | 维护态批19 立项（2026-08-13）；rollout §4 轨道3 / §8.1；docs/05 §4.2.4 env 散落债；task-051 | `LUMEN_ENV=production` 且 `TOKEN_SIGNING_KEY` 为弱默认 / 空 → 启动拒绝（RuntimeError）；静态 env（DATABASE_URL / LUMEN_ENV / LUMEN_DEMO_TOKEN_KEY）收敛 `config.py` 单点；test / 本地开发 / demo 豁免 | 生产带弱默认 key 运行（token 可预测）；env 读取继续散落各模块 | test_config 单测（env 读取 + secret 校验正/负向/豁免）+ 全量单测 / mypy 零回归 | **通过（2026-08-13）**：test_config 8/8；mypy **0 error**（58 files）+ ruff passed + 默认 pytest **321 passed / 49 deselected** 零回归；CI PR #156 **8 job 全绿**；PR #156 squash merge main `7388ff6`；bump v3.8.16 |
| TC-P2-GOV-010 | 前端文件膨胀 ratchet + App 减压（CQ-P1-008）；`App.tsx` 拆三 shell + `check-frontend-file-size.mjs` 棘轮 | 维护态批20 立项（2026-08-13）；rollout §4 轨道3 / §8.1；docs/05 §4.1 阈值；task-052 | `App.tsx` 行数显著下降且职责分离（auth / workspace / overlay shell）；`scripts/check-frontend-file-size.mjs` 拦新增超限（>250 行非基线文件）与既有超限膨胀（>基线行数）；CI project-check 跑 ratchet | 新 PR 引入超限文件；已超限文件继续膨胀 | `npm run lint` + `npm run build` + `npm run check:file-size` + 负向探针（临时 260 行文件 fail）+ CI | **通过（2026-08-13）**：lint 0 problem + build **304 modules**（+3 shell）+ file-size OK + 负向探针（260 行临时文件 fail，删除后 OK）；CI PR #157 **8 job 全绿**（project-check 含新 file-size step）；PR #157 squash merge main `7b5444f`；bump v3.8.17 |
| TC-P2-GOV-011 | 前端文件膨胀拆分 Slice A——App 减压收口 + ratchet 分层阈值（CQ-P1-008 候选 E4）；`App.tsx` 拆 useAppState/useAppShellState + `check-frontend-file-size.mjs` 分层阈值 | 维护态批21 立项（2026-08-13）；rollout §4 轨道3 / §8.1；docs/05 §4.1 阈值；task-053 | `App.tsx` 降到阈值内（359→90，App.* 阈值 300）；ratchet 按分层阈值工作（.css / App.tsx→300，.ts/.tsx→250，对齐 docs/05 §4.1）；baseline 收窄（19→18：App.tsx / ai-assistant.css 出基线，useAppState.ts 339 进） | ratchet 阈值口径与 docs/05 §4.1 不一致（原统一 250）；App 继续超限；新拆分文件再超限 | `npm run lint` + `npm run build` + `npm run check:file-size` + 负向探针 + 浏览器 smoke + CI | **通过（2026-08-13）**：lint 0 problem + build **306 modules**（+2 hook）+ file-size OK + 负向探针（280 行临时文件 fail，删除后 OK）+ 浏览器 smoke（demo + headless Edge 渲染登录页，无运行时错误）；CI PR #158 **8 job 全绿**；PR #158 squash merge main `ee95523`；bump v3.8.18 |
| TC-P2-GOV-012 | 前端文件膨胀拆分 Slice B——hooks/工具拆分（CQ-P1-008 候选 E4）；4 个超限 .ts 拆纯工具/hook | 维护态批22 立项（2026-08-13）；rollout §4 轨道3 / §8.1；docs/05 §4.1 阈值；task-054 | `local-vault-fs`/`useFolders`/`useDocuments`/`useLocalVaultMount` 四文件降到阈值内（登记例外除外）；拆出文件不新增超限；baseline 收窄（18→15） | 拆分引入行为回归（挂载 / 编辑 / 移动逻辑）；新文件再超限 | `npm run lint` + `tsc` + `npm run build` + `npm run check:file-size` + 负向探针 + 浏览器 smoke + CI | **通过（2026-08-13）**：lint 0 + tsc 0 + build **314 modules**（+9 文件）+ file-size OK（15 基线）+ 负向探针（280 行 fail，删除后 OK）+ 浏览器 smoke（headless Edge 登录页无错误）；CI PR #159 **8 job 全绿**；PR #159 squash merge main `bad7e4c`；bump v3.8.19 |
 - REQ-027 PDF 导出已提前到 Phase1.5B 并完成，验证口径见 TC-P1-017。
- **REQ-013a 主题时间线 / REQ-024 密度热条**：TC-P2-TL-001 已列入上表；task-030 已通过自动化验证、运行态 API smoke、Edge headless 浏览器 smoke 与真实 PG 大数据性能 smoke；详见 `docs/design/timeline.md`。
- **REQ-039 文档目录树（Phase2B 第三 slice）**：TC-P2-FOLDER-001——文件夹树 CRUD / 移动 / 排序 + 单文档移动 + 导入保留目录结构（`preserve_structure`）后端/API、前端 build、运行态 API smoke、用户浏览器 smoke 与浏览器自动化 smoke 已通过；folder 不独立设权限，文档可见性仍按 permission；防环 / 跨空间 / 重名 / 删非空 folder 拒绝；详见 `docs/design/folder-tree.md`。
- **REQ-018 Vault 兼容模式 B（Phase2C）**：TC-P2-VAULT-001——浏览器 File System Access 句柄 + IndexedDB 持久化 + 本地索引搜索 + 左侧上下分区（CMP-P2-TREE）+ 按需导入走 API-029；RG-009 PoC Go（2026-08-05，8 能力 + 5 场景，刷新自动恢复 granted、无上传）；硬天花板：仅本地挂载内容不进服务端 RAG；详见 `docs/research/2026-08-05-rg009-vault-local-mount-poc.md` + `docs/design/ingestion.md` Flow-D-014。
- **Phase2C 增强（Wave 1，2026-08-06 拍板）**：子树导入 UI（TC-P2-VAULT-002）、帮助体系 L0+L1（TC-P2-HELP-001）；FileSystemObserver 自动监听（TC-P2-VAULT-003）已 RG-010 Go（2026-08-06）。
- **Wave 2 账户（Phase2D）**：账号体系基础 TC-P2-AUTH-001（Sprint-26 已完成，PR#112 v3.0.0）；权限多人化 TC-P2-ACC-001（Sprint-27，2026-08-07 完成：owner_id 过滤 + 私有按 owner 过滤 + 跨用户隔离回归，P0 doc-links 泄露修复）；角色分层 + 用户管理 + 团队空间加入 Sprint-28 已完成（2026-08-07，TC-P2-ACC-002 通过，REQ-045..047）。
- **Wave 3（已拍板）**：跨设备 vault 元数据（TC-P2-VAULT-004，migration 015 + `/api/vault-mounts`，vault_mounts 顺延——014 已用于账户 `lumen_sessions`）；FileSystemObserver 自动监听（TC-P2-VAULT-003）已 RG-010 Go。
- REQ-015 / 016 / 017 其余 P2 后续用例（推送 / 协作 / 移动端）——不进 Phase2B 首批，待后续 Phase 细化。

### 远期愿景（不承诺）
- REQ-018 已升 Phase2C（TC-P2-VAULT-001，见 §2 矩阵 + 上方详述）；REQ-019..023、REQ-028..035 用例——待 05 技术验证可行后补

## 3. 分阶段验证范围

- **Phase1（Demo）**：覆盖 REQ-001..011、REQ-036（上表）——可演示 + 守产品红线；REQ-009/010 按 `.md` / `.txt` 已提取文本降级验收，真实 Word / PDF 解析与 OCR 不作为 Phase1 Demo 必过。P1A 的 TC-P1-013 是既有 REQ-011 的前端结构体验收口，不新增业务范围
- **Phase1.5A（个人可用 Alpha）**：覆盖 REQ-037 / REQ-038，对应 TC-P1-015 / 016；目标是批量入库 + 导出备份，优先于 PDF 和 Phase2 编码。
- **Phase1.5B（个人增强 Beta）**：覆盖 REQ-027，对应 TC-P1-017；RG-006 已 Go，Sprint-18 PDF 导出产品验收已完成。
- **Phase2A / Phase2B**：REQ-026 / 012 / 025 已作为 Phase2A 个人知识组织完成并通过 TC-P2-LINK/TAG/QUICK-001；**Phase2B 团队 MVP 已完成并收口（2026-08-05）**——REQ-014（TC-P2-AI-001）、REQ-013a/024（TC-P2-TL-001）、REQ-039（TC-P2-FOLDER-001）；P2 UI 门禁 TC-P2-UI-001~005 已由各 slice 的 build + 浏览器 smoke 覆盖；REQ-015/016/017 及愿景用例待后续阶段确认后继续细化。
- **Phase2C（本地知识源接入）**：覆盖 REQ-018 模式 B（TC-P2-VAULT-001，Sprint-23C 已完成 2026-08-06）；Wave 1 进行中（Sprint-24 子树导入 UI 已完成 2026-08-06 / TC-P2-VAULT-002 通过；Sprint-25 帮助 L0+L1 已完成（2026-08-06，TC-P2-HELP-001 自动化 + 人工确认通过））；RG-010 FileSystemObserver 已 Go（2026-08-06），Wave 1 收口。
- **Phase2D（账户与多人权限）**：Sprint-26 账号体系 TC-P2-AUTH-001 已完成（2026-08-07，PR#112 v3.0.0）；Sprint-27 权限多人化 TC-P2-ACC-001 已完成（2026-08-07，PR#114）；Sprint-28 角色分层 / 用户管理 / 团队空间加入 TC-P2-ACC-002 已完成（2026-08-07，REQ-045..047）。
- **愿景（产品）**：REQ-019..023、REQ-028..035 待技术验证后再补用例。

## 4. 本机资源验证

> 验证 Demo 在本机资源范围内可运行（受 `docs/env/local-env.md` 与 `ai/project-rules.md` §2.1 约束）。

- 本机起库与依赖：Docker Compose 起 PostgreSQL+pgvector，确认本机内存 / 磁盘足够。
- Demo 运行资源占用：峰值内存 < 8GB、显存 < 4GB、磁盘 < 20GB；Embedding 必须使用本机 `bge-small-zh`（512 维），不依赖外部 Embedding API。
- 数据边界验证：默认夹具使用已标注的虚构 Demo 数据；如导入真实团队文档，需检查来源 / 敏感级别标注，并验证敏感片段不默认发送到外部模型。
- 依赖 / 镜像验证：仅安装项目所需依赖与镜像；新增依赖必须进入依赖文件并说明用途。
- 资源超限的验证口径：超过软上限或本机 Embedding 在导入规模、响应时间、检索质量上无法满足要求时，先优化批处理、增量索引与 chunk 策略；仍不足则触发公司内网 Embedding / reranker 服务预案。

## 5. 验收记录

| 日期 | 范围 | 结果 | 记录 |
|---|---|---|---|
| 2026-07-03 | Sprint-2 前端文档编辑器 | 通过（降级口径） | 提交 `83fb782`；前端文档编辑器 Demo UI |
| 2026-07-03 | Sprint-3 降级文本导入 | 通过（降级口径） | 提交 `0fe169b`；仅 `.md`/`.txt` 已提取文本，不接真实 PDF/OCR |
| 2026-07-04 | Sprint-4A/B/C 搜索 + RAG API + 前端 UI | 通过（降级口径） | 提交 `da9f6e5`/`5144f2a`/`bc03839`；内存搜索 + 降级 RAG（不调 LLM）+ 前端搜索 / 问答 UI |
| 2026-07-04 | 缺陷修复：编辑文档索引 | 通过 | 提交 `c5c177e`；修复新建 / 编辑文档无法被搜索 / 问答命中（见 §5.1） |
| 2026-07-05 | Sprint-5 术语管理 | 通过（降级口径） | 提交 `5b78f0a`；空间术语 CRUD + 问答口径对齐 |
| 2026-07-03~07 | 后端单元 / 集成测试 | 通过（53 tests） | `.venv\Scripts\python.exe -m unittest discover -s tests/backend -v` 全通过；`compileall backend tests/backend` 通过 |
| 2026-07-06 | Sprint-6 桌面端集成 smoke（Edge Headless + FastAPI + Vite） | 部分通过（降级口径） | 已通过登录、空间切换、文档新建 / 编辑 / 版本恢复、`.md` 降级导入、搜索、RAG 问答、术语创建与术语来源、跨空间搜索隔离；Chrome 人工点击、真实 PDF 解析、图片 OCR 未验证。 |
| 2026-07-06 | Sprint-6 Chrome 人工 smoke | 通过（降级口径） | 用户反馈已通过 Chrome 桌面端 14 步 smoke：登录、空间切换、文档 CRUD / 版本恢复、`.md` 降级导入、标题 / 正文搜索、RAG 问答、术语创建与术语来源、跨空间搜索隔离；真实 PDF 解析、图片 OCR 未验证。 |
| 2026-07-09 | Sprint-7 LLM adapter（RG-004） | 通过（GLM-5.2 真实验证） | `llm_adapter.py` 多 provider + rag 接入；55 tests + 本机 GLM `glm-5.2` 真实问答验证通过（answer 带来源标注） |
| 2026-07-10 | Sprint-8 pgvector 接入（task-008 T1–T7） | 通过（RG-001/002 Go） | T1–T5 后端切 PostgreSQL（`docker/compose.yml` lumen-pg + ORM/PgRepository + 单例切换 + demo seed）；T6 RAG 向量召回（bge-small-zh 写 `lumen_chunks.embedding` + pgvector ANN，加法式叠加关键词，threshold 0.6）；74 后端 tests（含 PG 集成 + embedding）+ uvicorn 冒烟（相关问答 / 未找到红线 / 纯语义探针）；T7 文档回写 |
| 2026-07-10 | Phase1 Sprint / 全量验收 | Conditional Go（Demo closure） | TC-P1-001~012 均有验收口径与证据链：Sprint-1~6 按 Demo 降级口径通过；Sprint-7/8 真实 LLM、PostgreSQL+pgvector、Embedding 与 RAG 向量召回通过（RG-001/002/004/005 Go）；REQ-009 以 `.md` / `.txt` 已提取文本降级通过；REQ-010 OCR 明确移出 Phase1 必过（RG-003 No-Go，后续阶段）。遗留项：真实 Word / PDF 解析、OCR 真实化；不阻塞 Phase1 Demo closure，但阻塞无条件生产级 MVP 结论。 |
| 2026-07-10 | task-009 search 向量化 + 可选 zhparser | 通过 | `/api/search` 升级为 substring + `ts_vector` SQL 候选 + pgvector 语义召回；`init_db()` 两次验证通过；当前 `pgvector/pgvector:pg16` 无 zhparser 时回退 `simple`；76 后端 tests 通过（含 `tests.backend.test_search` 与 `test_search_api_returns_vector_semantic_hits`）。 |
| 2026-07-11 | RG-004 LLM 中转迁移复测 | 通过 | 旧中转 `47.107.134.2:7777` 的 key 被停用 → `.env` 迁至 `192.168.15.190:7777/v1`（`glm-5.2`）；`llm_adapter.chat()` + RAG 真实问答复测通过（答案带来源），RG-004 维持 Go。 |
| 2026-07-11 | Sprint-9（P1A）前端结构聚焦实现 | 通过 | PR #76 合并提交 `9ede5b6`；已实现本地 `activeView` 四视图切换、右栏拆分与桌面响应式 CSS；`npm.cmd run build` 通过；Chrome / Edge headless smoke（900px）通过，覆盖登录、四视图切换、文档新建 / 编辑 / 版本恢复 / 删除、Markdown 预览、搜索来源打开、问答、术语新建 / 删除确认；无全局横向滚动。 |
| 2026-07-12 | Sprint-10（P1B）前端工作台系统化重设计 | 通过 | 已实现 TopBar + Nav Rail + Context Pane + Workspace 三层工作台；Context Pane 随文档 / 搜索 / 问答 / 术语视图变化；CSS token + pane / toolbar / list-row / inspector 分层；新增 `frontend-workspace-redesign` 设计文档与 HTML 原型；`npm.cmd run build` 通过；Chrome / Edge headless smoke（900px）通过，覆盖登录、四视图切换、文档新建 / 编辑 / 版本恢复 / 删除、Markdown 预览、搜索来源打开、问答、术语新建 / 删除确认；无全局横向滚动。 |

| 2026-07-14 | Sprint-11（P2-UI-Gate 候选）实现前 UI 门禁草案 | 草案·未执行 | 已根据少容器清爽稿回填 TC-P2-UI-001~005；当前仅作为进入编码前的验证草案，不代表 Phase2 已启动或可直接编码。 |
| 2026-07-14 | UI / WSG 门禁静态评审 + 首个 vertical slice 确认 | Conditional Go（实现前） | WSG-001..006 与 P2-UI-G-001..006 均有文档锚点；静态检查少容器清爽稿原型关键入口、面包屑 / 层级搜索、文档预览 / 编辑并排、搜索 / 问答来源和关系图降级说明；首个 Phase2 vertical slice 静态评审建议为 `REQ-012 + REQ-026`（标签视图 + 内链 / 反向链接最小闭环）。仍未启动 Phase2、不改代码；进入实现前需补具体任务、迁移 / API / UI 实现计划和 smoke 证据。 |
| 2026-07-15 | Sprint-16（P1.5A）批量 / 文件夹导入 | 通过 | 已实现 API-029 `POST /api/import/batch` 与前端 drop zone / 多文件 / 文件夹选择；同名默认 skipped，失败隔离，标题保留相对路径前缀。验证：`git diff --check` 通过；`python -m unittest tests.backend.test_imports tests.backend.test_import_api` 9 tests 通过；非 PG/embedding 后端业务测试 47 tests 通过；`npm.cmd run build` 通过；Chrome headless drop-zone smoke 通过（登录 → drop 两个带 `webkitRelativePath` 文件 `smoke-folder/readme.md` / `smoke-folder/notes.txt` → 导入成功 2 → 文档列表保留路径标题 → 搜索命中 `smoke-folder/readme` → 问答来源命中 `smoke-folder/notes`）。未完成：全量 `python -m unittest discover -s tests/backend` 因本地 PG 测试超时未完成，embedding 模块在沙箱内触发 `torch_python.dll` 权限错误。 |
| 2026-07-16 | Sprint-17（P1.5A）单文档 `.md` + 空间 ZIP 导出 | 通过 | 已实现 API-030 `GET /api/documents/{id}/export`（`text/markdown`）+ `GET /api/export/space`（标准库 `zipfile`，按权限过滤，不可见文档不进 ZIP、不泄露数量）；前端文档详情"下载 `.md`"按钮 + TopBar"导出空间 ZIP"入口 + `api.ts` blob 下载。验证：`git diff --check` 通过；`python -m unittest tests.backend.test_export` 13 tests 通过（service + API 级）；回归 `test_imports` + `test_import_api` 9 tests 通过；`npm.cmd run build` 通过；FastAPI TestClient 端到端 HTTP smoke 通过（路由注册 + 全局异常 handler + 二进制 Response：单文档 markdown、空间 ZIP 含可见文档、不可见 4004、非法 format 4220、无 token 4001）。未完成：全量 PG/embedding 测试受本机环境限制未完成；浏览器按钮点击下载落盘的人工 smoke 可用 `scripts/run-sprint16-demo.ps1` 起服务后补充。 |
| 2026-07-16 | Sprint-12① 登录态持久化（REQ-011 可用性收口） | 通过（build；刷新 smoke 待人工） | `App.tsx` 将 `{token,userId,currentSpaceId}` 存 localStorage，启动恢复、切空间同步、token 失效（invalid token / 401）自动清除登出；`npm.cmd run build` 通过。刷新不掉线端到端 smoke 建议用 `scripts/run-sprint16-demo.ps1`（内存后端，不需 Docker）人工验证（登录→F5→仍在线→切空间→F5→仍在新空间）。seed 自索引（Sprint-12②）待做。 |
| 2026-07-16 | Sprint-12② seed 自索引（REQ-011 可用性收口） | 通过（单测；embedding 集成待本机 PG） | 根因：`migrations/005_sprint8_seed_demo.sql` 直接 INSERT 文档不经服务层，seed demo 文档开箱无 chunks / embedding。修复：`backend/service/document.py` `ensure_documents_indexed` 启动时幂等回填无 chunks 文档（`sync_document_chunks` → `replace_document_chunks`），`backend/main.py` lifespan `init_db` 后调用。验证：`python -m unittest tests.backend.test_seed_index` 4 tests 通过（回填 / 幂等 / 跳过已索引 / seed 初始无 chunks）；回归 `test_document` 5 tests 通过。embedding 向量集成验证（语义召回）待本机起 Docker+PG（沙箱 `torch_python.dll` 受限）；`_safe_embed` guarded 降级时 chunks 仍写入、关键词 / 标题搜索可用。 |
| 2026-07-16 | Sprint-13 外部只读真实化（REQ-003 权限收口，口径 B） | 通过（单测） | `permission.py` 加 `can_write_document`（external 文档仅 owner 可写，team/private 维持可见即可写）；`document.py` `update_document`/`delete_document`/`restore_version` 加 `_ensure_can_write` 校验（抛 `DocumentAccessError`）；`documents.py` 改/删/恢复端点捕获 → 403/4003。验证：`test_permission` 3 个 `can_write` 用例 + `test_external_write` 5 个 service 级用例（非 owner 改/删/恢复被拒、owner 可改、team 维持现状）通过；回归 44 tests 通过。设计回写：`permissions.md §3/§7` 补写口径（原仅定义读）。 |
| 2026-07-16 | Phase2A·REQ-026 内链/反链后端（Task A） | 通过（后端单测 + TestClient smoke；前端待 Task B） | 迁移 007 建 `lumen_doc_links`（resolved/unresolved/no_access + 防自链 CHECK + 索引）；`service/document.py` `sync_document_wikilinks` 文档保存时解析 `[[target]]` 按标题匹配（resolved/unresolved，自链跳过）；`service/doc_links.py` `list_links` 出链 target 不可见→`no_access` 不泄露标题、反链来源不可见过滤、`upsert_link` 仅 manual（wikilink 拒手动 POST）；API-018 `GET/POST /api/doc-links`。验证：`test_doc_links` 8 tests（解析 / 同步 / no_access / 反链过滤 / manual / wikilink 拒收 / API）+ 全回归 52 tests；TestClient 路由 smoke 5/5。前端 `[[wikilink]]` 渲染 + 反链面板见下行 Task B 验收。 |
| 2026-07-16 | Phase2A·REQ-026 内链/反链前端（Task B） | 通过（build + SSR + 浏览器 UI smoke 4/4） | `api.ts` 加 DocLink client（`listDocLinks`/`createDocLink`）；`MarkdownBlock` 扩展可选 `docLinks`/`onOpenDocument`，正则 `[[target]]`（同后端）注入伪链接 `lumen-wikilink:`，`components.a` 拦截按 status 渲染四态（resolved 可点跳转 / unresolved 虚线占位 / no_access 隐藏锚文本显「无权访问的链接」/ pending 编辑未同步）；自定义 `urlTransform` 放行 wikilink scheme（react-markdown v10 `defaultUrlTransform` 的 `safeProtocol` 会清空自定义 scheme，须放行）；`DocumentsFeature` 加反链面板（来源标题取自 `documents` 按 `source_document_id` 查）；`App.tsx` `loadDocLinks` + 出链/反链 state + effect；`panels.css` 三态 + 反链样式。验证：`npm.cmd run build`（tsc -b + vite，209 modules）；react-dom/server SSR 五态渲染验证；浏览器 UI smoke（`scripts/run-sprint16-demo.ps1` 内存后端，alice space10）4/4 通过：resolved `[[Nova Sprint Notes]]` 可点跳转、unresolved `[[不存在的文档]]` 虚线占位、反链面板显示引用方、编辑未保存 pending 占位。no_access 单用户单空间场景未构造（SSR 已验证隐藏锚文本）。 |
| 2026-07-20 | Phase2A 个人知识组织整体验收 closure | 通过 | 验收范围：REQ-026 内链 / 反链（TC-P2-LINK-001）、REQ-012 标签（TC-P2-TAG-001）、REQ-025 快速录入（TC-P2-QUICK-001）。证据：后端单测 / service 回归 / API smoke / 前端 build / 浏览器 smoke 均已分别记录在上方 Sprint / Task 验收与 §2 矩阵；`docs/08-dev-plan.md` 已补 Phase2A 完成包。结论：Phase2A「互联 / 组织 / 快录」闭环通过；未进入 Phase2B，REQ-014 AI 润色 / 写作引用与团队 MVP 范围仍待确认。 |
| 2026-07-30 | Phase2B·REQ-014 AI 润色 / 写作引用后端（Sprint-19 / task-019 后端 half） | 通过（后端单测 + 回归；**RG-008 升 Go**；前端 half 后续已闭合） | migration 010 `lumen_ai_drafts`（hash + 摘要留存，不存原文 / key）；entity `AiDraft` + `AiDraftORM`；`repository` pg/demo `create_ai_draft`；`service/ai_polish.py` polish / citation（citation 复用 `rag._find_candidate_chunks` 权限收敛，越权 chunk 不进 prompt / 不返回）；API-028 `POST /api/documents/{id}/polish`（4001/4003/4004/4220/**5030**）；LLM 不可用→`LlmUnavailableError`→5030 不落库不编造；citation 无可见来源→「未找到可引用来源」不调 LLM。验证：`tests.backend.test_ai_polish` service **9/9 绿**（DemoRepository + chat_fn 注入：polish 生成 / 越权不进 prompt / hash+摘要留存 / LLM 失败与未配置均不落库 / citation 无来源不调 LLM / 4003/4004/4220）+ 全量后端 `discover` **125 OK(skipped=3)**（该轮 PG up，API 层 6 例 + `PgRepository.create_ai_draft`/`AiDraftORM` PG 落库路径跑过）；`compileall` exit 0。**RG-008→Go、RISK-P2-005 关闭（后端）；TC-P2-AI-001 后端通过**。后续前端闭环与 live UI smoke 见 TC-P2-AI-001 状态。 |
| 2026-07-31 | Phase2B·REQ-014 AI 润色前端 UI smoke（task-019 前端 half；live 实跑） | 通过（真 GLM + 真 PG + 交互点击流） | 栈：lumen-pg + 后端 18000（注入 .env GLM）+ 前端 5173（identity + `/api` 代理 ✓）。**后端 live 实跑**（python urllib，UTF-8）：alice 建文档→polish 200（真 GLM 出草稿）→citation 200（sources 仅可见 chunk：Frontend Indexed Note / Semantic Timing Note / Term Source）→应用 PUT 200（版本 v1→v2）→越权 kira→alice 私有 404/4004；RG-008 存储护栏 live 验证（`lumen_ai_drafts`：`input_excerpt_hash`=64 位 sha256、`prompt_summary` 仅摘要无原文/key、`cited_chunk_ids`=可见 chunk）。**交互 UI 点击流**（alice 浏览器实测）：选区→触发 polish→草稿预览出现、citation sources 可见、应用替换选区 + 版本 +1（后端日志 polish×5 / citation×2 / PUT×8 / 版本恢复 全 200）。**TC-P2-AI-001 UI smoke 通过 → REQ-014 vertical slice 闭环。** 面板可见性 UX 缺陷发现并修复，见 §5.1。 |
| 2026-08-04 | D-C-001 citation 同步延迟量化（REQ-014 follow-up） | 通过（真 PG + 真 GLM，当前不做异步 job） | 新增 `scripts/smoke-ai-citation-latency.py`：加载 `.env`（不打印密钥）、创建隔离 user/space/source docs/target doc fixture、调用 `polish_selection(mode="citation")` 3 次、校验 sources 非空并清理 fixture。结果：`provider=glm`、`model=glm-5.2`、`runs=3`、sources=3；samples=`5041.79ms / 5914.06ms / 3802.70ms`，p50=5041.79ms，p95/max=5914.06ms，均低于 `--max-seconds 15`。本机 embedding 仍因 torch DLL 权限走 text-only / vector recall skipped，但 fixture 关键词召回足够，未影响 citation sources。结论：当前团队 MVP 保持同步 citation，不新增 `lumen_ai_drafts` job 状态机；若后续生产级慢 LLM / 大来源集超阈值，再转独立异步任务。 |
| 2026-08-04 | Sprint-18 单文档 PDF 导出（REQ-027 / API-019 / TC-P1-017） | 通过（后端单测 + 全量后端回归 + 前端 build + 产品 PDF 样例） | 新增 migration 013 `lumen_doc_exports`、`DocExport` entity / ORM、Demo/Pg repository 导出记录；`POST /api/export-pdf` 同步生成 PDF 并返回 `export_id/status/artifact_path`，不可见文档 4004、非法版本 4004、无效参数 4220、PDF 依赖 / 字体不可用 5030；`service/export.py` 使用 ReportLab 渲染 Markdown 子集（标题 / 段落 / 列表 / 表格 / 引用 / 页眉页脚），artifact 首版写入 `tmp/pdf_exports`，成功标记 done，失败标记 failed 且不保留坏文件。前端文档详情新增"导出 PDF"入口并接 `exports.ts` client。验证：`py_compile` 通过；`tests.backend.test_export` **20/20 OK**；backend discover **196 OK(skipped=2)**（embedding torch DLL 权限警告按 text-only fallback 继续）；`volta run --node 22.17.1 npm run build` 通过；产品样例 `tmp/pdf_exports/Sprint18 产品 PDF 验证-v1-export-1.pdf` 经 `pdftoppm` 渲染、人工 PNG 检查、`pdfplumber` 抽文本命中关键中文与 PIL 非白像素检查通过（`non_white_pixels=122856`）。残留：首版同步生成；下载端点已由 v1.7.0 补齐；仍不含异步队列 / 过期清理 job / 水印 / Word-PDF 解析 / zhparser。 |
| 2026-08-04 | Sprint-18 PDF 下载闭环收尾（REQ-027 / API-019 / TC-P1-017） | 通过（后端下载单测 + 全量后端回归 + 前端 build + 运行态 API smoke） | 新增 `GET /api/export-pdf/{export_id}/download`，返回 `application/pdf`；下载前复验 token 当前空间、源文档可见性、导出任务 `done` 状态与 artifact 目录边界。前端"导出 PDF"按钮改为 `POST /api/export-pdf` 成功后立即下载 artifact，不再只提示服务器本机路径。验证：`.venv\Scripts\python.exe -m unittest tests.backend.test_export` **27/27 OK**；`.venv\Scripts\python.exe -m unittest discover -s tests/backend -v` **203 OK(skipped=2)**（既有 embedding torch DLL 权限警告按 text-only fallback 继续）；`volta run --node 22.17.1 npm run build` 通过（259 modules）；运行态 demo API smoke 通过（OpenAPI 含 `/api/export-pdf/{export_id}/download`，`export_id=1` 下载 9145 bytes，prefix `%PDF`）。残留：仍不做异步队列 / 过期清理 job / 水印 / Word-PDF 解析 / zhparser；系统 Python 缺 `sqlalchemy`，后端验证需使用项目 `.venv`。 |
| 2026-08-01 | Sprint-21 Doc-First UX slice 3a/3c smoke + TC-P1-014 回归 | 通过（用户本地 Chrome/Edge smoke） | 范围：slice 3a 栏显隐 / 默认收起 / Ctrl+B/R + 3a hotfix（左栏收起不再导致主区消失）；slice 3c 首页默认落地、主区少容器视觉收口、documents 空态引导与返回。验证：`volta run --node 22.17.1 npm run build` 已通过；本轮修复本地 demo 启动脚本后确认前端 `http://127.0.0.1:5173` 与后端 `http://127.0.0.1:18000/docs` HTTP 200；用户确认 Chrome/Edge 本地 smoke + TC-P1-014 回归通过，覆盖 documents 减框、空态「展开左目录 / 新建文档」、文档页 / 新建态「返回」、900px 不破版、文档 / 搜索 / 问答 / 术语主流程。后续：slice 3d 导入弹窗化已另行补验收记录；demo 脚本 `Path`/`PATH` 根因已起模板回流提案。 |
| 2026-08-01 | Sprint-21 Doc-First UX slice 3d 导入弹窗化 + TC-P1-015/014 回归 | 通过（用户 smoke） | 范围：导入入口从 ContextPane 常驻 section 迁到 DocumentsFeature toolbar「导入」按钮触发的居中 modal；保留拖拽、多文件 / 文件夹选择、权限、逐条结果、同名跳过与导入后刷新；不改 API-029 / `useImport` 业务逻辑 / 后端 / DB。验证：`volta run --node 22.17.1 npm run build` 通过（248 modules）；`git diff --check` 通过（仅 CRLF warning）；用户确认 smoke 通过，覆盖 TC-P1-015 导入入口形态回归与 TC-P1-014 栏显隐 / 900px 布局回归。残留：未 bump VERSION / CHANGELOG，未提交 / 推送；3b 单列阅读/编辑另行启动前需先拆 inspector。 |
| 2026-08-01 | Sprint-21 Doc-First UX slice 3b 单列阅读/编辑切换 + smoke 反馈修复 + TC-P1-014 回归 | 通过（用户 smoke） | 范围：§9.5.4 单列阅读/编辑/并排三态切换；快速录入迁 TopBar + 用户头像；右栏重构 `DocumentInspectorFeature`（版本/链接/标签/AI tabs）。smoke 反馈修复：① download 中文标题 `.md` 导出 500 → `export.py` ASCII fallback + `filename*=UTF-8''` + 前端解析 + `test_export` 14 OK ② quick-entry A：默认 create_document + 移除 draft 入口（后端 draft 契约保留）③ 标签 inspector 内联「新建并打标签」+ 首页标签卡片 + placeholder。验证：`volta run --node 22.17.1 npm run build` 通过（252 modules）；用户本地 Chrome smoke 全过（文档三态 / 顶栏 / inspector tabs / 快速录入新默认 / 标签 G23–G26 / 900px）。残留：未 bump / 未提交。 |
| 2026-08-03 | Sprint-22 folder-tree 后端核心（task-027；REQ-039） | 通过（后端单测 + 回归 + 临时 PG CTE smoke；前端文件管理器当时待下一 slice，已由 task-029 补齐基础能力） | migration 011 `lumen_folders` + `lumen_documents.folder_id`；entity `Folder` + `FolderORM`；`repository` pg/demo 11 方法（pg `WITH RECURSIVE` 防环 / demo 内存递归）；`service/folder.py`（FolderView + 异常 4003/4004/4090/4220 + CRUD/移动防环跨空间/删非空 4090/排序；document_count 按可见性过滤）；API-034..037 `/api/folders`（token current_space_id；PATCH 用 model_fields_set 区分未传/null；2026-08-02 路径裁定）。验证：`tests.backend.test_folder` **19/19 OK**（18 service + 1 API）+ 回归 `test_tags+test_document+test_quick_entry+test_doc_links` **45/45 OK** + `import backend.main` 通过 + 临时 PG folder CTE smoke 通过（递归防环、跨空间拒绝、可见计数、删非空拒绝）。越界：前端文件管理器 / folder 独立权限 / 文档 order / 软删除。 |
| 2026-08-03 | Sprint-22 API-029 导入保留结构（task-028；REQ-037/039） | 通过（后端/API + 前端默认参数 + 临时 PG import smoke；前端文件管理器当时待下一 slice，已由 task-029 补齐基础能力） | API-029 `POST /api/import/batch` 增加 `preserve_structure=true` 默认建/复用 `lumen_folders` 并回填 `lumen_documents.folder_id`；`false` 保留旧标题前缀向后兼容；成功项返回 `items[].folder_id`；同名冲突在保留结构模式下按同 folder 下标题判断；前端 `useImport` 默认显式提交 true。修复 PgRepository `_to_document` 未映射 `folder_id` 的读回缺陷。验证：`tests.backend.test_imports tests.backend.test_import_api` **11/11 OK**；folder/import/document/tag/quick/doc_links 回归 **75/75 OK**；临时 PG import smoke 通过（默认建 folder、同名文件跨 folder 共存、`false` 旧标题前缀）；`volta run --node 22.17.1 npm run build` 通过（252 modules）。沙箱 PG smoke 中 embedding 降级为 text-only（torch DLL 权限警告），不影响本验证。 |
| 2026-08-03 | Sprint-22 前端文件管理器与单文档移动补齐（task-029；REQ-039/004） | 通过（后端针对性测试 + 前端 build + 运行态 API smoke + 用户浏览器 smoke；v1.5.2 补浏览器自动化 smoke） | API-038 `PATCH /api/documents/{document_id}/folder`：文档可见且可写，目标 folder 必须当前空间，`folder_id=null` 移到根；移动只更新 `lumen_documents.folder_id`，不新增版本 / 不重建索引。前端：左侧 `FolderTree` 文档行右键菜单“移动到”；folder 菜单受控关闭；文件夹 inline 新建 / 重命名；移动后刷新文档列表与已加载 folder 计数。验证：`.venv\Scripts\python.exe -m unittest tests.backend.test_document tests.backend.test_folder tests.backend.test_imports tests.backend.test_import_api` **38/38 OK**；`volta run --node 22.17.1 npm run build` 通过（255 modules）；运行态 OpenAPI 已出现 API-038，临时文档 API smoke 移动成功且 `folder_id` 读回一致，临时数据已清理；用户浏览器测试确认无问题（2026-08-03）；`scripts/smoke-folder-tree-browser.mjs` 通过（2026-08-04）。残留：目标列表首版仅含已加载 folder。 |
| 2026-08-03 | Sprint-20 主题时间线（task-030；REQ-013a/024） | 通过（后端自动化 + 前端 build + 运行态 API smoke + Edge headless 浏览器 smoke；v1.5.2 补真实 PG 大数据性能 smoke） | Candidate A 时间线不建事件表，API-033 `GET /api/spaces/{id}/timeline` 实时聚合 documents / tag_links / doc_links / chunks；migration 012 仅加时间索引；前端接入独立时间线视图（关键词、标签入口、密度热条、事件列表）。本轮 smoke 发现并修复 demo runtime 新建文档 `created_at/updated_at` 为空导致 timeline 无事件的缺口，补 `DemoRepository.create_document/update_document` 时间戳与回归用例。验证：`tests.backend.test_timeline` **7/7 OK**；timeline/document/tag/doc_links 回归 **38/38 OK**；backend discover **190 OK(skipped=2)**（embedding torch DLL 权限警告按 text-only fallback 继续）；frontend `npm run build` 通过（259 modules）；运行态 API smoke 通过（OpenAPI 含 API-033，创建文档/标签，关键词 timeline、tag timeline、空 q 422）；Edge headless CDP 浏览器 smoke 通过（登录、切到时间线、搜索 `Phoenix`，事件列表命中 `Phoenix Sprint20 Timeline Smoke`，density=1，无错误文本）；真实 PG 大数据性能 smoke 通过（620 docs + 240 links，density_events=2100，returned=200，degraded=True，window=week，elapsed_ms=2677.61）。残留：`linked` actor 按 TL-C-011 返回 `null`；无新增依赖 / 无 timeline 事件表。 |
| 2026-08-04 | Sprint-20 release 状态收口（v1.5.0 → v1.5.1 patch） | 通过（文档 / 发布状态修正） | Git 事实：`origin/main` 已包含 `3e23d78` + `28843cb`，HEAD 为 `28843cb2af431e5fa33f7c8eeeb0ad1dc27dccb3`；GitHub Actions `Project Check` run 30830484733 为 success；annotated tag `v1.5.0` 已推送，`refs/tags/v1.5.0^{}` 解引用到 `28843cb2af431e5fa33f7c8eeeb0ad1dc27dccb3`。本次 v1.5.1 仅修正 `docs/08-dev-plan.md` / `docs/09-verification.md` 中 release 状态漂移，并同步 `VERSION` / `CHANGELOG.md` / `CHANGELOG-PLAIN.md`；不改业务代码、API、DB 或测试契约。 |
| 2026-08-04 | v1.5.2 Sprint-20 / Sprint-22 验证债收口 | 通过（验证脚本 + 文档状态修正） | 新增 `scripts/smoke-folder-tree-browser.mjs`：Node 22 内置 fetch/WebSocket + Chrome/Edge CDP，运行态 OpenAPI preflight 后执行登录、Documents 视图、目录树渲染、UI 新建子文件夹、UI 单文档移动、API `folder_id` 后验并清理 fixture；实测通过 `FOLDER_TREE_BROWSER_SMOKE ok root=1 child=2 document=201`。新增 `scripts/smoke-timeline-pg-performance.py`：真实 `lumen-pg` / `PgRepository` / `get_timeline()`，临时写入 620 docs + 240 links 后自动清理；实测 `TIMELINE_PG_PERF_SMOKE ok documents=620 links=240 density_events=2100 returned=200 degraded=True window=week elapsed_ms=2677.61`。不改业务 API / DB 契约。 |
| 2026-08-05 | **Phase2B 收口（团队 MVP 完成）** | 通过（范围三项全部验收 + 退出标准达成） | 范围：REQ-014 AI 润色 / 写作引用（TC-P2-AI-001：后端 9/9 + RG-008 Go + live UI smoke 2026-07-31 + D-C-001 2026-08-04 量化）；REQ-013a/024 主题时间线 + 密度热条（TC-P2-TL-001：task-030 + 运行态 API smoke + Edge headless + 真实 PG perf smoke）；REQ-039 文档目录树（TC-P2-FOLDER-001：task-027/028/029 + 浏览器自动化 smoke 2026-08-04）；Sprint-21 Doc-First UX（TC-P1-014/015 回归）；导入规模修复（RISK-P1-008，v1.7.1）；wikilink 别名/锚点兼容修复（v1.7.4）。退出标准三条达成：真实验收 TC / RG-008 数据外发风险已接受（真实外发 + 护栏）/ 产品红线未破。未做：多人 / 多空间真实团队端到端场景（残余验证债，见 §6）。 | 收口不升 Phase；下一阶段范围待用户定义（候选：真实团队验证 / RG-009 Vault PoC / 产品化） |
| 2026-08-05 | Phase2B 收口后 UI 收口批次（左右栏拖拽 / 保存回阅读态 / 目录右键删除 / 快速录入 / 搜索布局） | 通过（前端 build 262 modules 绿 + 用户 Chrome/Edge 浏览器 smoke） | 左右栏新增拖拽分隔条并持久化（localStorage `lumen-demo-pane-widths`，左 180–420 / 右 220–480，默认 240）；编辑保存成功自动回阅读态（`savedRevision` 信号，连点不再产生多余版本）；文档删除入口收敛到左侧目录右键菜单（编辑工具栏删除按钮移除）；快速录入抽屉 420px→560px；搜索视图改「工具栏 / 输入栏 / 结果区」三行网格消除输入栏大空档。附带收口：编辑器大段留白、编辑工具栏视觉（去框 / 防按钮竖排）、顶栏面板图标 + 帮助入口、左侧栏图标语义化 + 收起/展开切换 + 显示当前文件、右栏重复标题去除、AI 润色模式紧凑分段控件、移除 9 处用户可见 REQ 角标（代码注释与 docs 保留追溯）。验证：`volta run --node 22.17.1 npm run build` → 262 modules 绿；用户本机 Chrome/Edge smoke 覆盖清单全部通过。残留：无（纯前端 UI 收口，不改 API / DB / 测试契约）。 |

| 2026-08-06 | Sprint-23C REQ-018 模式 B 本地挂载（TC-P2-VAULT-001） | 通过（CDP 前提 smoke + 用户人工 6 口径） | 纯前端，后端零新 API。FSA 句柄 + 原生 IndexedDB 持久化 + 刷新恢复 granted + 本地倒排索引/搜索（单字可搜）+ 左侧上下分区（垂直分隔条调高）+ 主区本地文档预览 + 按需导入走 API-029 + Network 零上传。验证：tsc build 270 modules 绿；smoke-local-vault-index 8 断言；smoke-vault-local-mount-browser CDP 前提 + 分区 UI；用户人工 smoke（2026-08-06）6 口径全过（①授权+刷新恢复 ②本地树 ③本地搜索含单字 ④分区隔离 ⑤主区预览+按需导入 ⑥Network 零上传）。验收期修复：上下分隔条调高、按钮深色文字、单字搜索（query 保留单字）、主区本地预览（去左栏预览遮挡）。硬天花板：本地挂载内容不进服务端 RAG。 |
| 2026-08-06 | Sprint-24 子树导入 UI（TC-P2-VAULT-002；REQ-018 模式 B 增强） | 通过（前端 build 270 modules + 用户人工 smoke 4 项） | 本地挂载目录树 hover「⤓ 导入」子树导入（API-029 + `preserveStructure`）+ 批量确认（≥2 文件 / 整库）+ 进度带范围 + 结果带去向；单篇不确认；导入中按钮禁用。数据层 `useLocalVaultMount` `LocalMountTreeNode.path`；UI `LocalMountPane.tsx`；样式 `local-mount.css`。验收期修复 2 处（见 §5.1）：上下分隔条拖动方向反转、首页空左栏视图感知。纯前端，不改 API-029 契约 / 后端 / 不引依赖。 |
| 2026-08-06 | Sprint-25 帮助手册 L0+L1（TC-P2-HELP-001；REQ-011 可用性收口） | 通过（自动化：build 273 modules + 浏览器 smoke；人工浏览器 smoke 新用户路径确认通过 2026-08-06） | L0：`docs/env/user-guide.md` 任务导向重组为唯一内容源（开始使用 / 把资料放进知识库 / 找到内容 / 组织与维护 / 导出与分享 / 权限 / 能力边界 / FAQ，修正导入入口为弹窗形态，标注 Phase2B/2C 章节待补）。L1：新增 `app/onboarding-store.ts`（localStorage `lumen-demo-onboarding`，不可用降级）+ `features/OnboardingGuide.tsx` + `styles/onboarding.css`，登录后一次性 3 步引导（新建文档 → 保存后去搜索 → 去问答提问，可跳过）；欢迎页新手清单（3 步进度）+「示例文档未建索引」提示；搜索 / 问答空态「去新建文档 / 去导入」；标签 / 时间线空态下一步入口；顶栏帮助弹层升级（分类速查 + 轻量过滤可检索「导入」+ 查看完整手册链接，单一来源）。验证：`volta run --node 22.17.1 npm run build` 绿（273 modules）；`node scripts/smoke-help-onboarding-browser.mjs` 通过（登录 → 3 步引导 → 首页未建索引提示 → 搜索/问答空态按钮 → 时间线/标签空态入口 → 帮助过滤 10→3 命中「导入」→ 跳过引导持久化 completed=true 且刷新不再弹出）。用户人工 smoke 新用户路径通过（2026-08-06）；验收期修复 2 缺陷（见 §5.1：帮助弹层无法关闭、搜索/问答空态按钮超高）。残留：L2 可搜索帮助中心 / L3 / L4 留后续；Phase2B/2C 功能章节待后续补 user-guide。 |

| 2026-08-07 | Sprint-26 账号体系基础（TC-P2-AUTH-001；REQ-040/041/042） | 通过（自动化：后端 `tests/backend/test_auth.py` 20/20 + backend discover 222 OK（skipped=2）；RG-011/012/013 Go） | migration 014 `lumen_users` 扩列（email / password_hash / status / last_login_at / failed_login_count / locked_until）+ `lumen_sessions`（不透明 token，token 只存 SHA-256 摘要，TTL 8h / 撤销 / 滑动续期 / 多设备）；auth service（bcrypt cost 12、锁定 5 次 / 15min、结构化审计 register / login_success / login_failed / login_locked / logout）；`api/auth.py` 六端点（register / login / logout / refresh / sessions）；统一 `get_current_user` 收敛 13 router；`main.py` 生产 fail-fast（`LUMEN_ENV=production` + demo 仓储拒绝启动）；seed 用户统一设 demo 密码 `demo-pass-1234`（PG 强制凭证，内存模式保留无密码快捷登录）；前端登录/注册 tab 面板 + 退出登录菜单。验证：RG-011 PoC（bcrypt 5.0.0，Python 3.14.3，cost 12 ≈0.21s）；`test_auth.py` 覆盖注册/登录/锁定/会话撤销/续期轮换/多设备/跨用户隔离。浏览器 smoke：`scripts/smoke-auth-browser.mjs` PASS（注册 / 登录 / 登出 / 凭证登录 / refresh 轮换 / 多设备会话撤销：重复撤销 200 幂等、缺失 404、错误凭证 401、重复 email 409、短密码 422）+ demo 启动验证通过（`run-sprint16-demo`，18000/5173）。残留：登录/注册为内联 tab 而非独立路由（accounts-auth §15）；`LUMEN_ENABLE_DEMO_AUTH` 未实现（demo 模式由仓储类型决定）。前端 build 273 modules 绿（2026-08-07 复核）。
| 2026-08-07 | Sprint-27 权限多人化（TC-P2-ACC-001；REQ-043/044） | 通过（后端全路径审计 + P0 修复 + 多用户隔离回归 + 双用户浏览器 smoke） | 全路径审计 20 条（列表/单文档/版本/搜索 hybrid/RAG 候选/时间线/目录树计数/标签/导出 ZIP/单文档导出/链接/快速录入/术语/AI 润色/导入/空间/会话）：P0 1 处——`GET /api/doc-links` 未校验源文档可见性，同空间成员可枚举他人 PRIVATE 出链 `link_text`；修复 `backend/service/doc_links.py` + `backend/api/doc_links.py`（不可见→4004）。P2 记录：`visible_document_where_clause` 全仓未使用（过滤在 Python 层，无 SQL 下沉）；`upsert_link` 标题解析可指向同空间不可见文档（无读泄露）。验证：`test_permission.py` MultiUserIsolationTest（注册双用户 10 路径零泄露 + external 只读 4003 + 跨空间）+ `test_doc_links.py` 4004 用例 → 权限+链接 24 OK；backend discover 229 OK（skipped=2，无回归）；`scripts/smoke-sprint27-isolation-browser.mjs` PASS（注册 A/B 跨空间 + seed alice/kira 同空间 PRIVATE 零命中，浏览器 kira 无私有文档 / alice 可见）。提交 `6c293a0`（P0+回归）、`ae21614`（smoke）。残留：P2 两项记录待用户确认接受；本机 embedding torch DLL 权限 → text-only 降级（既有）。

| 2026-08-07 | Sprint-28 角色分层 + 用户管理 + 团队空间加入（TC-P2-ACC-002；REQ-045/046/047） | 通过（自动化：backend discover 275 OK（skipped=2）+ 浏览器双视角 smoke PASS） | migration 016（`lumen_users.role` admin/member 默认 member + CHECK + seed 对齐 alice=admin / kira·brightlite-member=member + `lumen_space_members.created_at` 支撑 joined_at）；admin 域用户管理（API-044 列表 / 过滤 / 不泄露 password_hash；API-045 改角色 / 禁用启用，禁用后登录 4030 且既有会话失效）；space 域成员 CRUD（API-046..049：按 email 添加 / 改空间角色 / 移除，移除后失权 4003，非空间 admin 4030，最后一个 admin 4090）；受限用户搜索（API-050，member 4030）；登录响应新增 `role`；前端用户管理页 + 空间设置成员管理（入口按角色显隐）。验证：`tests/backend/test_role.py` / `test_admin_users.py` / `test_space_members.py` / `test_api_sprint28.py` + 既有 auth / permission 回归 → discover 275 OK（skipped=2）；`volta run --node 22.17.1 npm run build` 绿；`node scripts/smoke-sprint28-role-admin-browser.mjs` PASS（admin（alice）用户菜单「用户管理」+ 成员视图管理控件可见；member（kira）两者均不可见；API 矩阵全绿：member 4030 / 改角色 / 禁用登录 403 / 重复添加 409 / 最后一个 admin 4090 / 搜索 4030 / 移除后失权 4003）。残留：分页 / `last_login_at` 显式排序 / 响应 `updated_at` 未实现、refresh 不含 role（accounts-auth §18.9 偏差）；移除用户 / 重置密码 / 邀请码 / REQ-016 留后续。 |
| 2026-08-07 | **Phase2D 收口（账户与多人权限完成）** | 通过（三 slice 全部验收 + 退出标准达成） | 范围：Sprint-26 账号体系基础（TC-P2-AUTH-001 / PR#112 v3.0.0）；Sprint-27 权限多人化（TC-P2-ACC-001 / PR#114，P0 `GET /api/doc-links` 源文档可见性修复）；Sprint-28 角色分层 + 用户管理 + 团队空间加入（TC-P2-ACC-002 / PR#117 v3.1.0）。退出标准全部达成：三 TC 通过，权限红线（凭证安全 / 跨用户隔离 / 管理接口仅 admin / 用户列表不暴露 `password_hash`）未被破坏，「账号 → 权限 → 团队协作」闭环成形。已知偏差经用户确认全接受、留后续（accounts-auth §18.9：API-044/046 未分页 / `last_login_at` 未显式排序 / API-045/048 无 `updated_at` / refresh 不含 `role`；Sprint-27 P2：`visible_document_where_clause` 未使用 / `upsert_link` 目标解析）。收口不升 Phase；下一阶段范围待用户定义（候选：移除用户 / 重置密码 / 邀请码 / REQ-016 / 真实团队端到端验证 / 产品化）。 |
| 2026-08-07 | 维护态 UI 改进批次（术语领域树 TC-P2-TERM-001 / REQ-048 + 批2b 步2 标签 CRUD 前端接线 API-027 + 批1 顶栏 / 批2a 命令面板） | 通过（编码完成 + 自动化 + 运行时 smoke + 用户浏览器验收；已提交 `979094e` / v3.2.0） | **术语领域树（REQ-048）**：migration 017 `lumen_term_categories` 树表 + `lumen_terms` 扩 `category_id`/`category`/`source`；后端 `service/term_category.py` + `api/term_categories.py`（API-051..053 领域树 CRUD / 移动防环 / 排序 / 删非空 4090）+ `terms.py` 扩字段（`category_id` 跨空间→4220）；前端 `useTermCategories` / `TermCategoryTree` / `TermsFeature` 单详情面板（阅读/编辑态分离）/ `ContextPane` 左栏全局区 + 领域树（上下分区可调高）/ `usePaneSectionHeight` 通用 hook。验证：`tests/backend/test_term_category.py` 18 例 + `test_term.py` 扩字段 2 例 + 后端全量 **298 OK** + 真 PG migration 017 应用（`init_db()` schema 校验）+ build **291 modules 绿** + 运行时 API smoke（领域建树 / 术语挂领域 / term_count）+ **用户浏览器验收通过**（3 轮反馈迭代：新建领域入口 / 右键新建术语 + 预填领域 / 主区去冗余列表 / 左栏分区调高）。**批2b 步2 标签 CRUD**：`useTags` + `TagsFeature` 接线 `updateTag`（PUT 重命名/描述/颜色）+ `archiveTag`（DELETE 归档，确认后移除列表、关联文档保留）；build + 运行时 smoke（创建→重命名→归档→active 移除 / archived 保留）+ **用户浏览器验收通过**。批1 顶栏图标 / 批2a 命令面板已完成并验收。设计分析：`docs/research/2026-08-07-term-domain-tree-analysis.md`（TM-C-001..007 用户确认）。**已提交**：`979094e`（65 文件，直推 main）+ bump **v3.2.0**（VERSION / CHANGELOG 三件套；REQ-048 + API-051..053 = MINOR）；CI Project Check success（run 31191740016）。 |

| 2026-08-08 | 维护态批次批3（AI 助手抽屉 + 基于知识库开关 + 多轮 + LLM 多通道切换，TC-P2-ASSIST-001 / REQ-008 扩展） | 通过（编码完成 + 自动化 + smoke + 用户浏览器验收；已提交 `59dd898` / v3.3.0） | **后端**：`rag.answer_question` 增 `history` / `use_knowledge_base` / `llm_provider`；`llm_adapter` 多配置（`LLM_PROVIDERS` 命名 + `deepseek` provider + `list_configs()` 脱敏）；API-010 请求体扩展 + 新增 `GET /api/llm-configs`；修复 `_resolve_chat_fn` 闭包绑定 config 的切换丢失 bug。**前端**：`useAiAssistant` / `AiAssistant` 抽屉（右下角悬浮图标 / 对话区 / 来源可点开 / 多轮 / LLM 通道下拉 / 全局 Esc）+ 命令面板「问 AI」改开抽屉 + `run-sprint16-demo.ps1` 加载 `.env` 注入后端（原不加载致 LLM 全降级）。验证：`test_rag.py` +7 + `test_llm_adapter.py` +6 + 后端全量 **310 OK** + build **294 modules 绿** + `scripts/smoke-ai-assistant-browser.mjs` **PASS**（切 deepseek 通道通用对话真实出文）+ **用户浏览器验收通过（2026-08-08）**。REQ-008 扩展不另编号（用户确认）；设计：`docs/design/ai-assistant.md`；契约：`07` API-010 + `GET /api/llm-configs`。**已提交**：`59dd898`（v3.3.0，2026-08-08）+ project-rules §1 已回写。 |
| 2026-08-08 | 维护态使用反馈批2（④ 长文档 TOC / ⑤ md 编辑工具栏 / ⑥ 文件夹内新建文档；v3.4.0） | 通过（编码完成 + 自动化 + 浏览器 smoke） | **④ TOC**：新增 `app/markdown-toc.ts`（ATX 标题提取 + slug + 层级 `tocDepth`）；`MarkdownBlock` 加 `showToc`——标题注入 id（按行号从 `buildHeadingIds` 精确取值，与 TOC 计数对齐）+ 左侧 sticky 目录（点击 `scrollIntoView`）；DocumentsFeature 阅读态与 LocalDocPreview 均启用。**⑤ md 工具栏**：新增 `app/markdown-editor-actions.ts`（13 个动作 `applyMarkdownAction`：加粗 / 斜体 / H1-3 / 列表 / 引用 / 行内代码 / 代码块 / 链接 / 图片 / 分割线，有选区包裹 / 无选区插入占位 / 块级前缀换行补位）；`DocumentsFeature` 编辑态 textarea 上方 `.editor-md-toolbar`，点击插入并恢复选区（`requestAnimationFrame` 设置 selectionRange）。**⑥ 文件夹内新建**：后端 `service/document.py` `DocumentCreate.folder_id`（服务端校验目标文件夹属于当前空间）+ `api/documents.py` `DocumentWriteRequest.folder_id` + `pg/demo repository.create_document(folder_id)`；前端 `Draft.folder_id` + `useDocuments.creatingFolderId`（新建态草稿携带）+ `handleCreateDocument(folderId)` + `FolderTree` 文件夹右键「在此新建文档」+ `ContextPane`/`App` 透传。验证：后端 **311 tests OK**（create folder_id 无回归）；前端 build **296 modules 绿**（+markdown-toc / markdown-editor-actions）；**浏览器 smoke `scripts/smoke-batch2-ui-browser.mjs` PASS**（登录 → 文件夹右键新建 → md 工具栏插入 `**加粗文字**` → 填含标题正文 → 保存回阅读态 → TOC 目录显示 + 标题带 id 锚点 + 新文档 `folder_id` 持久化）。**已提交**：`v3.4.0` 直推 main。 |
| 2026-08-08 | REQ-049 本地挂载文件可编辑（增删改查）+ ⑪ 部署落地（方案 A 全容器化）；v3.5.0 | 通过（编码完成 + 前端 build + compose 校验；真实写文件待用户浏览器验收） | **REQ-049**（纯前端，不改「不上传 / 不进 RAG」红线）：`local-vault-fs.ts` 补 `writeVaultFile` / `createVaultFile` / `deleteVaultFile` / `renameVaultFile`（Chromium `move()`）+ `parentDirectoryForPath` + walk 收集目录句柄；`useLocalVaultMount` 补 `editingPath/editingText` + `beginEdit/saveEdit/cancelEdit` + `createFile/deleteFile/renameFile`（写前 `readwrite` 授权，写后重建索引）；`LocalDocPreview` 主区「编辑」→ textarea → 保存/取消（仅本地文件系统）；`LocalMountPane` 目录右键/「＋」新建文件、文件右键重命名 + 删除确认；`localVault` vm 提升到 App 共享（左栏 + 主区同一实例）。**⑪ 部署**：`backend/Dockerfile`（Python 3.14-slim + CPU torch + uvicorn 0.0.0.0）+ `frontend/Dockerfile`（Node 22 build → Nginx 托管 + /api 反代）+ `frontend/nginx.conf`（SPA fallback + envsubst 注入后端地址）+ `docker/docker-compose.prod.yml`（postgres + backend + frontend，env_file `../.env` 透传 LLM，DATABASE_URL 覆盖容器内）+ `docs/env/deploy-guide.md`（部署手册）+ `.env.example` 补多配置格式示例。验证：前端 build **296 modules 绿**（REQ-049）；`docker compose -f docker/docker-compose.prod.yml config --quiet` 通过（⑪ 编排语法）；真实 FSA 写文件（新建/编辑/删除/重命名）需用户浏览器授权验收（`showDirectoryPicker` 无法 headless 自动化）。**已提交**：`v3.5.0` 直推 main。 |
| 2026-08-08 | 维护态批4（REQ-049 增强：多挂载目录 + 导入入口补充 + 并排布局修复 + md 工具栏视觉优化 + 编辑撤销）；v3.6.0 | 通过（编码完成 + 前端 build 296 modules 绿 + 用户浏览器验收） | **多挂载**：`local-vault-fs.ts` IDB 单句柄 → 句柄数组（`loadVaultHandles` / `saveVaultHandle` 追加 / `removeVaultHandle` / `clearVaultHandle`，兼容旧单值升级）；`useLocalVaultMount` 重构为 `mounts` 数组（每挂载独立 handle/状态/目录句柄，docs 聚合且 path 加挂载名前缀保证全局唯一）；LocalMountPane 支持多次「挂载 vault」+ 待重授权挂载列表逐个授权/移除 + 卸载全部。**导入入口**：文件夹右键补「导入全部挂载」，文件右键补「导入此篇」（不再依赖全局选中）。**并排布局修复**：切「并排」自动收起右栏（Inspector）释放横向空间 + `editor-content-grid` grid 显式定位（工具栏 grid-column 1/-1 占首行，编辑/分隔/预览第二行三列）修复 4 子元素挤 3 列导致的编辑列压窄错位。**md 工具栏**：去边框紧凑分组（格式/结构/插入 + 分隔线）+ 单行滚动 + hover 仅文字变蓝（`:not(:disabled)` 压过 base.css 全局深蓝底）；块级插入改行首加前缀保留原文（原整行替换误删光标行文字）；代码块光标处插入不覆盖原文。**编辑撤销**：自建 undo 栈（上限 50 步，Ctrl+Z 恢复内容+光标，切换文档/保存重置；受控 textarea 使浏览器原生撤销失效）。验证：前端 build **296 modules 绿**；用户浏览器验收通过（多挂载平铺树 / 右键导入入口 / 并排正常 / 工具栏 hover 文字变蓝 / 插入保留原文 / Ctrl+Z 撤销）。**已提交**：`v3.6.0` 直推 main。 |
| 2026-08-10 | REQ-051 浏览器 UI smoke 补全（TC-P2-AUTH-002「浏览器 smoke」证据；原 v3.7.0 留候选） | 通过（CDP 自动化 smoke PASS） | 新增 `scripts/smoke-req051-password-ui-browser.mjs`（CDP 直连，参考 smoke-auth-browser.mjs，零依赖）。覆盖：小眼睛双向 toggle（登录 + 注册密码框，input.type password↔text + aria-label「显示/隐藏密码」切换）+ 忘记密码 modal 两步（入口 `.auth-link-button` → step email 注册邮箱恒响应 `.modal-notice`（防枚举）→ step confirm invalid token + 合规新密码 → `.modal-error`（对应 API-056 4010）→ 短密码 submit 禁用 → 关闭）。不覆盖（由他证兜底）：confirm 成功路径（demo 无 SMTP，valid token 只在后端日志）由后端 reset 单测覆盖；防枚举（注册 vs 未注册响应一致）由 batch5 API smoke API-055 覆盖。验证：内存模式 demo + `volta run --node 22.17.1 node scripts/smoke-req051-password-ui-browser.mjs` PASS（exit 0；loginToggleBidirectional / registerToggle / modalOpened / resetRequestNotice / confirmInvalidTokenError / shortPasswordDisablesSubmit / modalClosed 全断言通过）。 |
| 2026-08-12 | Sprint-34（维护态批9）CQ-P1-003 事务边界 UoW（TC-P2-GOV-004） | 通过（3 slice 全闭环 + PG integration 验证） | Slice A（PR #141 `5f017b6`）：`backend/repository/uow.py`（contextvar 感知 `_session_scope` 收口 commit + `UnitOfWork`/`DemoUnitOfWork` 双实现 + `unit_of_work` 工厂按 is_demo 分流 + `UnitOfWorkProtocol`）+ pg_repository 96 处 `with SessionLocal()`→`_session_scope()` 机械替换、删 50 处 commit；`test_uow_contract.py` 5 测试。Slice B（PR #142 `09137a4`）：document.py create/update/restore_version 三函数写步包 `with unit_of_work(repository):`（权限校验读在外，nested join）+ imports.py `import_extracted_text` 主流程原子事务、`create_import_job`/`fail_import_job` 事务外独立提交、删 :108 重复 `replace_document_chunks`、chunk_count 改 `list_document_chunks`。Slice C（PR #143 `7c64545`）：`test_uow_rollback.py` 故障注入验证 rollback 真语义（文档/版本/chunks 全撤销 + import_job 不残留 done）+ 修复 `create_import_job` pending-id 缺陷。验证：默认 **306 passed 零回归 ×3** + ruff 37 不增；Slice C `test_uow_rollback` + `test_import_lifecycle`（PG）PASSED。integration 首次全量跑暴露 7 个存量失败已登记 `docs/05 §4.2.4` 债（与本次无关）。 |
| 2026-08-12 | Sprint-35（维护态批10）7 个存量 integration 失败整治（债清理，非功能·不改 Phase） | 通过（integration 全量 48 passed 零失败 + 默认 306 零回归 + ruff 37 不增） | 单文件 `backend/repository/pg_repository.py` 补 17 处缺失 `session.flush()`：① pending-id 类 8 处 create_*（create_session/create_tag/create_quick_entry/create_ai_draft/create_doc_export/create_term/create_folder/create_term_category）`_to_xxx` 读 `row.id` 前显式 flush 分配 BIGSERIAL id；② datetime 类 9 处 `= func.now()` 后立即转换（update_session_space/update_document/restore_document_version/update_term/rename_folder/move_folder/rename_term_category/move_term_category/update_doc_export）flush 使 server-side 时间戳 reload 成真 datetime；修正 `test_ai_polish` 误判（LLM 已 patch mock，实为 create_ai_draft pending-id 致 draft_id=None）。验证：integration 全量 **48 passed**（41+7）零失败；默认 **306 passed** 零回归；ruff **37** 不增。**已提交**：v3.8.7 直推 main。 |

| 2026-08-13 | Sprint-40（维护态批15）权限查询边界 scoped query（TC-P2-GOV-005；CQ-P1-004） | 通过（本地验证 + CI 7 job 全绿） | `RepositoryProtocol.list_visible_documents(user_id, space_id)` 安全默认查询（pg 两段式：membership 校验 + SQL where 下推 `space_id` + 非 private 或 owner；demo 复用 `filter_visible_documents` 单一事实源）+ `list_documents()` / `list_memberships()` 标 internal + 用户态 8 处调用点收口（timeline / search / rag / folder / export / tag / imports / api documents）+ repository 级 cross-space / cross-user 负向与契约测试。验证：mypy **0 error**（55 files）+ ruff passed + 默认 pytest **304 passed / 4 skipped** 零回归 + CI PR #151 **7 job 全绿**（backend-test / backend-integration 48 用例 / backend-typecheck / frontend-build / frontend-lint / ruff / project-check）。PR #151 squash merge main `945bf8f`；bump v3.8.12。 |
| 2026-08-13 | Sprint-41（维护态批16）API 响应契约 response_model·codegen（TC-P2-GOV-006；CQ-P1-006） | 通过（本地验证；CI 待 PR 跑） | `backend/model/schemas.py` 新增 `ApiEnvelope[T]` 泛型 + 域响应模型（约 40 个）；62/62 JSON 端点按域接入 `response_model=ApiEnvelope[X]`（内容域 27 / 术语导入域 16 / 账户空间域 19；二进制 md/zip/pdf 3 个明确排除）；`scripts/export-openapi.py` 固定 `openapi/openapi.json` 快照（32→126 components schemas）；CI 新增 `schema-diff` job（Slice A/B advisory → B-3 升 required）。验证：本地路由全量 smoke **60/60 通过**（62 JSON + 3 二进制端点 TestClient + DemoRepository 实测，response_model 校验零失配）+ mypy **0 error**（56 files）+ ruff passed + 默认 pytest **304 passed / 4 skipped** 零回归 + 快照再生成 `git diff --exit-code` 绿；integration 48 用例由 CI 兜底（本地 docker 不可用未跑）。 |
| 2026-08-13 | Sprint-42（维护态批17）强依赖 fail-fast / readiness（TC-P2-GOV-007；CQ-P1-001） | 通过（本地验证 + CI 8 job 全绿） | `backend/main.py` lifespan except 分支化（PG 模式 init 失败 `logger.error`+raise 拒绝启动；demo 仓储模式保留容忍降级）+ 2 处 `print`→`logger`；`error_codes.py` 新增 `DB_NOT_READY=5031`（→503，与 5030 AI/OCR 区分）；`schemas.py` `HealthView`；新 `api/health.py`（`/api/health/live` 200；`/ready` demo 200 / PG `db.ping()` 失败 503+5031，`response_model=ApiEnvelope[HealthView]`）+ `create_app` 注册 + openapi 重生成（+91）；`test_health.py` 5 用例 + `test_error_contract` 码集+5031；prod compose 后端 healthcheck 接 `/api/health/ready`。验证：mypy **0 error**（57 files）+ ruff passed + 默认 pytest **313 passed / 49 deselected** 零回归 + 快照无漂移 + CI PR #154 **8 job 全绿**。PR #154 squash merge main `dd75329`；bump v3.8.14。 |
| 2026-08-13 | Sprint-43（维护态批18）日志统一（TC-P2-GOV-008；CQ-P2-002） | 通过（本地验证 + CI 8 job 全绿） | `backend/repository/pg_repository.py` embedding 降级 `print` → `logger.warning`（后端生产 print 债清零）；`service/document.py` 索引回填单文档失败、`service/rag.py` RAG / 通用对话 LLM 降级 ×2、`api/auth.py` 登录 `SpaceAccessError` 回退——补 `logger.warning` 记原因（对齐 L0-4「失败必须可见」）；`ruff.toml` select 加 `T20`（flake8-print）禁 print（`scripts/` smoke / CLI stdout 豁免）。验证：mypy **0 error**（57 files）+ ruff passed（含 T20）+ 默认 pytest **313 passed / 49 deselected** 零回归 + grep 复核 backend/ 无 print 残留 + T20 负向探针确认可抓 print + CI PR #155 **8 job 全绿**。PR #155 squash merge main `edb5df1`；bump v3.8.15。 |
| 2026-08-13 | Sprint-44（维护态批19）配置集中 + secret 校验（TC-P2-GOV-009；CQ-P2-003） | 通过（本地验证 + CI 8 job 全绿） | 新 `backend/config.py`（pydantic-settings `Settings` 三字段显式 alias + `get_settings()` 单例 + `validate_runtime_secrets`）；`db.py` / `main.py` / `auth.py` 静态 env 收敛（`llm_adapter` 动态命名配置、`embedding` 环境注入显式豁免）；`main.py` lifespan 生产 fail-closed 校验弱默认 signing key（仅 `LUMEN_ENV=production`，test / 本地开发 / demo 豁免，理由见 docs/05 §4.2.4）；新依赖 `pydantic-settings==2.15.0`（已确认）。验证：test_config 8/8（production 弱默认 / 空拒、强 key 放行、test·development·空豁免）+ mypy **0 error**（58 files）+ ruff passed + 默认 pytest **321 passed / 49 deselected** 零回归 + CI PR #156 **8 job 全绿**。PR #156 squash merge main `7388ff6`；bump v3.8.16。 |
| 2026-08-13 | Sprint-45（维护态批20）前端 ratchet + App 减压（TC-P2-GOV-010；CQ-P1-008） | 通过（本地验证 + CI 8 job 全绿） | `App.tsx` 546→360：拆 `AuthShell`（登录/注册 + 忘记密码）/ `WorkspaceShell`（主工作区容器）/ `OverlayShell`（命令面板 + AI 助手），App 只留状态编排 + 装配（props 聚合对象 + tsc 兜底）；新 `scripts/check-frontend-file-size.mjs`（Node 零依赖，`wc -l` 口径）+ `frontend/.file-size-baseline.json`（19 个超限基线）+ `package.json` `check:file-size` + CI project-check step（拦新增超限/超限膨胀）。验证：lint **0 problem** + build **304 modules**（+3 shell）+ file-size OK + 负向探针（260 行临时文件 fail）+ CI PR #157 **8 job 全绿**。PR #157 squash merge main `7b5444f`；bump v3.8.17。 |
| 2026-08-13 | Sprint-46（维护态批21）前端文件膨胀拆分 Slice A——App 减压收口 + ratchet 分层阈值（TC-P2-GOV-011；CQ-P1-008 候选 E4） | 通过（本地验证 + CI 8 job 全绿） | ratchet 分层阈值：`check-frontend-file-size.mjs` 按 docs/05 §4.1 分层（`.css` / `App.tsx`→300，`.ts/.tsx`→250）；App 减压收口：`App.tsx` 359→90（新 `app/useAppShellState.ts` 57 行 UI/布局 + 弹窗 state + 新 `app/useAppState.ts` 339 行域 hook 编排 + 跨域回调 + effects，依赖顺序原样搬移，App 只剩装配）；baseline 重生成（19→18）。验证：lint **0 problem** + build **306 modules**（+2 hook）+ file-size OK + 负向探针（280 行临时文件 fail）+ 浏览器 smoke（demo + headless Edge 渲染登录页无运行时错误）+ CI PR #158 **8 job 全绿**。PR #158 squash merge main `ee95523`；bump v3.8.18。 |
| 2026-08-13 | Sprint-47（维护态批22）前端文件膨胀拆分 Slice B——hooks/工具拆分（TC-P2-GOV-012；CQ-P1-008 候选 E4） | 通过（本地验证 + CI 8 job 全绿） | 4 个超限 .ts 按风险低→高拆：`local-vault-fs` 310→walk(67)/idb(152)/fs(94) 三纯工具；`useFolders` 300→folder-utils(46)+useFolderInlineEdit(122)+主 hook(202)；`useDocuments` 335→useDocumentSideData(110)+download-actions(30)+主 hook(286 登记)；`useLocalVaultMount` 442→tree(32)/types(60)/useLocalVaultEditor(184)+主 hook(236)；baseline 18→15。验证：lint 0 + tsc 0 + build **314 modules**（+9 文件）+ file-size OK + 负向探针（280 行 fail）+ 浏览器 smoke（headless Edge 登录页无错误）+ CI PR #159 **8 job 全绿**。PR #159 squash merge main `bad7e4c`；bump v3.8.19。 |

### 5.1 缺陷与回归记录

| 缺陷 | 来源 | 修复 | 回归范围 | 结果 |
|---|---|---|---|---|
| 新建 / 编辑文档无法被搜索或问答命中 | Sprint-4 实现期发现 | `c5c177e fix: index edited documents for search and rag` | 搜索 + RAG 索引（新建 / 编辑文档入库后被检索召回） | 回归通过；53 后端 tests 含相关用例 |
| AI 润色侧边栏生成草稿后「生成 / 应用」按钮被折叠遮挡；≤1199px 小屏 `.inspector-pane` `max-height:190px` 容不下整个面板 | task-019 前端 UI smoke（2026-07-31）发现 | `fix/ai-polish-panel-visibility`：`AiPolishFeature` 草稿生成后 `useEffect`+`scrollIntoView` 把「应用 / 丢弃」滚进视野；`responsive.css` `.inspector-pane` max-height 190→360px | 前端 `npm run build` 绿（Node 22）+ 用户 live 复测通过 | 已修复（本次 PR） |
| 本地 demo 脚本在 Windows PowerShell 下无法稳定后台启动：`Start-Process` 因 `Path` / `PATH` 重复报错；普通 `-Detached` 后端子进程会被 AI 执行器回收；备用端口启动时前端命令叠加了 `package.json` 默认 `5173` 与脚本指定端口 | Sprint-21 smoke 启动 demo 时发现；Sprint-20/22 smoke 复盘收敛（2026-08-01~03） | `scripts/run-sprint16-demo.ps1` 保留 `Normalize-ProcessPathEnvironment`；`-Detached` 改为 WMI/CIM launcher 并写 `%TEMP%\lumen-sprint16-demo\runtime.json`；前端改为直接 `npm exec vite -- --port <FrontendPort>`，不再叠加 `npm run dev` 默认端口；stop 命令可读取 runtime state | 本地 demo 启动与 smoke 支撑；不影响业务功能 / API / DB | 提权启动 `18133/5174` 后内置 API-033 OpenAPI preflight 通过；父命令结束后独立 `check-runtime-openapi.ps1` 仍通过；前端日志仅显示 `5174` |
| 中文标题文档点击「下载 .md」报操作失败（HTTP 500） | Sprint-21 slice 3b smoke（2026-08-01）发现 | 根因=后端 `Content-Disposition: filename="{中文}"` 命中 Starlette latin-1 编码 + 旧后端进程内存未重载修复。修复：`backend/api/export.py` ASCII fallback + `filename*=UTF-8''{quote}`；`frontend/src/api/client.ts` 优先解析 `filename*`；`tests/backend/test_export.py::test_export_document_api_supports_unicode_filename` | 重启 demo 加载 hotfix 后 HTTP `export?format=md` 200 + `test_export` 14 OK（含真中文标题 `filename*` 断言） | 已修复（本轮） |
| 标签「新建不了」+ inspector/首页缺新建入口 | Sprint-21 slice 3b smoke（2026-08-01）发现 | 实为交互认知（未输入名字即点新建，按钮 disabled）+ 入口缺口。修复：`DocumentInspectorFeature` 标签 tab 加内联「新建并打标签」（`useTags.handleCreateAndTag` + 透传链）；`WelcomeFeature` 补「标签」卡片；`TagsFeature` placeholder 改「输入标签名，回车或点新建」 | 前端 `npm run build` 绿 + 用户 smoke G23–G26 全过 | 已修复（本轮） |
| 单篇文档右键菜单已出现但选择后实际未移动 | Sprint-22 task-029 用户浏览器 smoke（2026-08-03）发现 | 根因=运行中的后端进程未加载 API-038，浏览器请求 `PATCH /api/documents/{id}/folder` 返回 404；处理：清理旧 uvicorn 残留，启动真实后端，确认运行态 OpenAPI 已含 API-038；临时文档 API smoke 移动成功并清理 | API-038 运行态路由、单文档移动、前端右键移动菜单 | 已修复；用户浏览器测试确认无问题（2026-08-03） |
| 浏览器 smoke 前缺少可复用运行态 route preflight，旧后端 / 错端口会拖到点击阶段才暴露 | Sprint-21/22/20 smoke 中断复盘（2026-08-03） | 新增 `scripts/check-runtime-openapi.ps1` 只读检查 `/openapi.json` path/method；`scripts/run-sprint16-demo.ps1` 新增 `-RequiredBackendRoute`；`docs/env/local-demo-runbook.md` 写入浏览器 smoke 前置门禁；模板回流提案补 OpenAPI preflight / 结果文件 / 自动化 fallback | 本地 demo 启动、运行态 API identity、浏览器 smoke 前置检查；不改变业务 API / DB | 已处理（项目脚本 + SOP）；API-033 内置 preflight 与独立 preflight 均通过；去项目化项已补 `_proposals/TEMPLATE-UPGRADE-powershell-start-process-path-normalization.md` |
| `[[目标\|别名]]` / `[[目标#锚点]]` 等 Obsidian 常见格式无法解析（`extract_wikilinks` 未剥离 alias / anchor，整段文本作为 target → unresolved） | REQ-018 模式 A 收尾复盘（2026-08-05）发现 | `backend/service/document.py` `_WIKILINK_PATTERN` 只捕获标题部分并剥离 `\|alias` / `#anchor`；`tests/backend/test_doc_links.py` 补 extract 断言 + `test_sync_wikilinks_strips_alias_and_anchor`（同目标去重） | `test_doc_links` 9/9 OK；后端全量 210 OK（embedding torch DLL 沙箱警告按 text-only fallback，非本修复引入） | 已修复（本轮；PR 待合并） |
| 左右栏宽度固定不可调，且刷新后不保持 | Phase2B 收口后 UI 收口 smoke（2026-08-05 用户反馈） | 新增 `frontend/src/app/pane-width-store.ts`（localStorage `lumen-demo-pane-widths`，left 180–420 / right 220–480，默认 240）+ `usePaneWidth.ts`（pointer capture、←/→ 键盘 10px 步进、双击复位）；App 左栏与 DocumentsFeature 右栏各加 `.pane-resizer` 分隔条，`.workspace-layout` / `.document-view-grid` 改 4 / 3 列；responsive 小屏隐藏分隔条并修正列位 | 前端 build + 用户 Chrome/Edge smoke（拖拽变宽、刷新后宽度保持、右栏可唤出） | 已修复（本轮） |
| 编辑保存后停留在编辑态，连点「保存」会不断产生新版本 | Phase2B 收口后 UI 收口 smoke（2026-08-05 用户反馈） | `useDocuments.ts` 保存成功自增 `savedRevision`；`DocumentsFeature` 收到信号且非新建态时自动回阅读态（`setDocumentMode('read')`）；保存按钮由 `isBusy` 守卫防重复提交 | 前端 build + 用户 smoke（保存后自动回阅读态、连点不产生多余版本） | 已修复（本轮） |
| 文档删除入口只在编辑工具栏，左侧目录右键菜单缺「删除文档」 | Phase2B 收口后 UI 收口 smoke（2026-08-05 用户反馈） | `FolderTree` 文档行右键菜单新增「删除文档」（danger 项）；`useDocuments.handleDelete` 改为 `handleDeleteDocument(documentId)`（可删任意树内文档，confirm 确认，删除的是当前选中文档时清空选中态）；编辑工具栏删除按钮移除，删除入口收敛到目录右键 | 前端 build + 用户 smoke（目录右键删除通过） | 已修复（本轮） |
| 快速录入抽屉 420px 偏窄，模式选项 / 标签在窄宽下排版不佳 | Phase2B 收口后 UI 收口 smoke（2026-08-05 用户反馈） | `quick-entry.css` `.quick-entry-drawer` 宽度 `min(420px, 92vw)` → `min(560px, 94vw)` | 前端 build + 用户 smoke（抽屉 560px、模式选项 / 标签正常、文字单行） | 已修复（本轮） |
| 搜索视图关键词输入栏与标题之间大空档（`.search-panel` 1fr 行把输入栏压到底部留白） | Phase2B 收口后 UI 收口 smoke（2026-08-05 用户反馈） | `panels.css` `.search-panel` 改三行网格 `auto auto minmax(0, 1fr)`（工具栏 / 输入栏 / 结果区），输入栏紧贴标题 | 前端 build + 用户 smoke（输入栏紧贴标题，无大空档） | 已修复（本轮） |
| 大屏「Markdown 内容」与编辑框大段留白（`base.css` 全局 `label { display: grid }` + `.editor-field` 两个 auto 行被 `align-content: stretch` 均分拉伸） | Phase2B 收口后 UI 收口（2026-08-05 实现期发现） | `workspace.css` `.editor-field { grid-template-rows: auto minmax(0, 1fr) }`；textarea `min-height: 440px` → `height: 100%` | 前端 build（262 modules）+ 用户 smoke | 已修复（本轮） |
| 右侧栏「文档侧栏」重复标题 + AI 润色「模式」大边框方块控件偏重 | 2026-08-05 用户反馈 | `DocumentInspectorFeature` 删除 `inspector-header` 重复标题与不再使用的 `activeTabInfo`，`document-inspector.css` 面板 grid 行改 `auto minmax(0, 1fr)`；`ai-polish.css`「模式」改紧凑分段控件（隐藏 radio 保可访问性，激活态蓝底） | 前端 build + 用户 smoke | 已修复（本轮） |
| 本地挂载分区上下分隔条拖动方向与光标相反（拖下→分隔条反向上移） | Sprint-24 TC-P2-VAULT-002 smoke（2026-08-06）发现 | 根因=`useLocalMountHeight` 符号反转：底部 pane 高度 H 与分隔条位置反向（H 增→分隔条上移），旧代码拖下 `H+delta` 使分隔条上移；拖拽与 ↑↓ 键盘同源同错。修复：拖拽 `height - delta`、ArrowDown `-20` / ArrowUp `+20`，统一为「分隔条跟随光标」 | 前端 build 270 modules + 用户 smoke（拖下=下分区变矮、拖上=变高） | 已修复（本轮） |
| 帮助弹层打开后无法关闭（无关闭按钮，点击外部 / Esc 均不生效，只能再点 `?`） | Sprint-25 用户反馈（2026-08-06，人工复现确认） | 根因=TopBar `helpOpen` 无关闭机制。修复：`TopBar.tsx` 加 `useRef` + `useEffect`（弹层打开时监听文档 `mousedown` / `touchstart`，点击 `.help-wrap` 外即关闭；`Escape` 关闭）；弹层头部新增「×」关闭按钮（`help-header` / `help-close` 样式入 `layout.css`） | 前端 build 273 modules 绿；CDP 浏览器实测四种关闭方式（外部点击 / Esc / × / 再点 `?`）均生效；`scripts/smoke-help-onboarding-browser.mjs` PASS | 已修复（本轮，Sprint-25 提交内） |
| 搜索 / 问答空态「去新建文档」「去导入」按钮被纵向拉伸成超高条（实测搜索 294px / 问答 268px 高） | Sprint-25 用户反馈（2026-08-06，人工复现确认） | 根因=`.task-workspace` 的 `minmax(0, 1fr)` 行把 `.empty-state-guide` 拉满 → grid 默认 `align-content: stretch` 撑行 → `.empty-state-actions`（flex 默认 `align-items: stretch`）把按钮拉满行高。修复：`onboarding.css` `.empty-state-guide` 加 `align-content: start`、`.empty-state-actions` 加 `align-items: center` | 前端 build 273 modules 绿；CDP 浏览器实测按钮恢复 30px 高、空态不再占满面板；smoke PASS | 已修复（本轮，Sprint-25 提交内） |
| 刷新后首页默认显示左侧空栏，点 toggle 也显空栏 | Sprint-24 smoke（2026-08-06）发现 | 根因=左栏可见性全局（`leftPaneOpen` 持久化），而 `ContextPane` 在 home / tags / timeline 无内容分支（空列）。修复：`App.tsx` 左栏可见性改视图感知——仅 documents / search / query / terms 可显示，home / tags / timeline 永远收起（toggle / Ctrl+B 只改偏好，无内容视图不显形） | 前端 build 270 modules + 用户 smoke（首页无左栏、点 toggle 不显、文档视图左栏正常） | 已修复（本轮） |
| 导入产生的文档无法通过 API 删除（`DELETE /api/documents/{id}` 返回 HTTP 500） | 团队 E2E 验证 E2E-18 清理阶段（2026-08-08，PG 模式）发现 | 根因= `lumen_imports.parsed_doc_id` 外键无 `ON DELETE CASCADE`（`backend/migrations/004_sprint8_imports_terms.sql:14`），而 `PgRepository.delete_document`（`backend/repository/pg_repository.py:658`）只删 `lumen_documents`，依赖级联清理，imports 一路未覆盖 → 删除导入文档时违反 `lumen_imports_parsed_doc_id_fkey` 约束。修复：`delete_document` 删除前先 `UPDATE lumen_imports SET parsed_doc_id=NULL WHERE parsed_doc_id=:id` 解绑引用（方案 A，repository 层，不改 migration / 现有库无需迁移）。内存 Demo 无 FK 不受影响；versions / chunks 的 FK 均有 CASCADE，仅 imports 遗漏 | 回归验证（2026-08-08）：`tests/backend/test_pg_repository.py` 17/17 OK（含新增 `test_delete_imported_document_unbinds_import_job`）+ 后端全量 311 OK + 真 PG 复验（导入 → `DELETE` HTTP 200 `deleted:true`，`lumen_imports.parsed_doc_id` 解绑为 NULL，文档 4004） | **已修复（本轮）**；残留导入文档随测试 TRUNCATE 一并清除，DB 已回干净 seed 基线 |
| AI 助手切换用户后对话未清空（登出 → 换账号登录，前用户对话及来源残留） | 团队 E2E 验证 E2E-14（AI 助手浏览器交互，2026-08-08）用户发现 | 根因=`useSession` 登录 / 登出 / 换账号只更新 session，无回调通知 App；`aiAssistant.reset()` 仅挂在 `handleSpaceChanged`（空间切换）上，用户身份变化不触发清空 → 前用户 AI 对话（含 RAG 来源，可能指向前用户可见文档）残留给下一位用户，违反跨用户隔离红线。修复：`App.tsx` 登录态变化 `useEffect`（依赖 `[session.session?.token]`）中追加 `aiAssistant.reset()`，登录 / 登出 / 换账号 / 鉴权失效时清空 AI 助手对话；不改 `useSession.ts`，复用既有模式 | 前端 `volta run --node 22.17.1 npm run build` 294 modules 绿（tsc 无错误）+ 用户浏览器复测通过（alice 发消息 → 登出 → kira 登录 → AI 助手对话已清空） | **已修复（本轮）** |
| 首页新手清单做完后仍保留、无法关闭（登录引导弹层无「×」关闭；首页新手清单无关闭 / 收折入口） | 2026-08-08 用户使用反馈（维护态批1） | 修复：`OnboardingGuide` header 加「×」关闭（`onDismiss`，未完成时下次登录重新弹出）；`WelcomeFeature` 首页清单加「× 关闭」（会话内记忆，与 guideDismissed 同语义）+ 完成自动收起 + 「展开」；`onboarding.css` 补关闭 / 收折样式 | 前端 build 296 modules 绿 + 用户浏览器确认（引导弹层可关闭、首页清单可关闭 / 完成收起） | **已修复（本轮，v3.3.3）** |
| 预览本地文档后再点「新建」，主区仍显示本地预览、看不到新建编辑视图 | 2026-08-08 用户使用反馈（维护态批1） | 根因=`WorkspaceMain` 以 `localPreviewDoc` 决定渲染 LocalDocPreview / DocumentsFeature，而 `App.tsx` 仅在 `documents.selectedId !== null` 时清预览；新建态 selectedId 保持 null → 预览不清。修复：`App.handleCreateDocument` 包装先 `setLocalPreviewDoc(null)` 再进入新建，全入口（toolbar / 首页卡片 / 命令面板 / ContextPane / OnboardingGuide）透传 | 前端 build 296 modules 绿 + 用户浏览器确认（本地预览后新建 → 正常进入新建编辑视图） | **已修复（本轮，v3.3.3）** |
| 文档编辑页权限下拉「外部只读」出现两次 | 2026-08-08 用户使用反馈（维护态批1） | 根因=`constants.ts` `permissionLabels` 含 4 键，`external` 与 `external_readonly` 均映射「外部只读」→ `Object.entries` 渲染重复项；后端枚举仅三值（`entities.py` private/team/external）。修复：移除前端遗留 `external_readonly`（`DocumentPermission` 类型 / `permissionLabels` / `useAiPolish.isExternalPermission`） | 前端 build 296 modules 绿 + 用户浏览器确认（下拉仅三项：私有 / 团队共享 / 外部只读） | **已修复（本轮，v3.3.3）** |
| 右上角用户图标只显示 `#1` 而非用户名 | 2026-08-08 用户使用反馈（维护态批1） | 根因=登录 / refresh 响应不含 `name`（`api/auth.py`），`Session` 类型无 name。修复：后端 login / refresh 响应补 `name`（从 `find_user_by_id` 取，additive）；前端 `LoginResponse.name` + `Session.name` + `TopBar` 显示用户名（旧 localStorage 无 name 时回退 `#userId`）；`session-store` 持久化 name | 后端 311 tests OK（auth 无破坏性变更）；前端 build 296 modules 绿 + 用户浏览器确认（右上角显示用户名） | **已修复（本轮，v3.3.3）** |
| `PgRepository.create_import_job` 返回 `ImportJob.id=None`（真实 PG 导入 `complete/fail_import_job` KeyError） | Slice C integration 测试（2026-08-12）发现 | 根因=`_to_import` 在 `_session_scope` commit 前读 `job.id`，SQLAlchemy autoflush 不因访问 pending 主键触发 → 主键未分配返回 None。修复：`create_import_job` `session.add` 后显式 `session.flush()` 分配 BIGSERIAL id。同类风险：`create_term` 等 `create_*` 可能同因（已登记 `docs/05 §4.2.4` 债） | `test_import_lifecycle`（PG）KeyError→PASSED；`test_uow_rollback` PASSED；默认 306 零回归 | **已修复（本轮，Slice C PR #143）** |
| PgRepository 7 个存量 integration 失败（`create_*` pending-id + `func.now()` 列未 flush 的 datetime 序列化两类同根） | PG integration 首次全量跑（2026-08-12）暴露（此前被默认 `-m "not integration"` 跳过；登记 `docs/05 §4.2.4` 债） | 单文件 `backend/repository/pg_repository.py` 补 17 处缺失 `session.flush()`：① 8 处 create_*（create_session/create_tag/create_quick_entry/create_ai_draft/create_doc_export/create_term/create_folder/create_term_category）`_to_xxx` 读 `row.id` 前显式 flush 分配 BIGSERIAL id；② 9 处 `= func.now()` 后立即转换（update_session_space/update_document/restore_document_version/update_term/rename_folder/move_folder/rename_term_category/move_term_category/update_doc_export）flush 使 server-side 时间戳 reload 成真 datetime（实验证实）。`test_ai_polish` 原误判为「LLM 环境」，实为 `create_ai_draft` pending-id（LLM 已 patch mock） | integration 全量 41+7=**48 passed 零失败**；默认 **306 passed 零回归**；ruff **37 不增** | **已修复（本轮，维护态批10 / Sprint-35，v3.8.7）** |

## 6. 风险与未验证项

| Risk-ID | RG / Gate | 风险 / 未验证项 | 影响范围 | 当前处理 | 关闭依据 |
|---|---|---|---|---|---|
| RISK-P1-001 | RG-001 | ~~PostgreSQL+pgvector 未接入~~ | ~~REQ-007/008 真实化~~ | ✅ **已解决**（Sprint-8 / task-008 T1–T6：lumen-pg + PgRepository + RAG 向量召回；RG-001→Go，见 `05 §5.1`） | Sprint-8 验收记录、TC-P1-007/008 |
| RISK-P1-002 | RG-002 | ~~真实 Embedding（bge-small-zh）未接入后端~~ | ~~REQ-007/008 向量检索~~ | ✅ **已解决**（T6 写 `lumen_chunks.embedding` 512 维；RG-002→已启用；约束 `HF_HUB_DISABLE_XET=1`） | embedding tests、TC-P1-007/008 |
| RISK-P1-003 | TE-C-003 / RG-001 | ~~Docker Desktop Linux engine 阻塞~~ | ~~起库 / 真实 PostgreSQL~~ | ✅ **已解决**（daemon live，TE-C-003 闭合；`docker/compose.yml` 起 lumen-pg healthy） | Sprint-8 T1 验证 |
| RISK-P1-004 | RG-001 / RG-002 | ~~search 仍关键词检索（未向量化）~~ | ~~REQ-007 语义搜索~~ | ✅ **已解决**（task-009：substring + `ts_vector` SQL 候选 + pgvector 语义召回；zhparser 可选，当前镜像无扩展时回退 `simple`） | task-009 tests |
| RISK-P2-001 | RG-006 候选 | 真实 PDF / Word 解析 | REQ-009 相关交付 | 仅 `.md`/`.txt` 已提取文本；python-docx/pdfplumber 未接入；后续真实化前需 tech-env-eval | 待后续 tech-env-eval 与 TC |
| RISK-P2-002 | RG-003 | OCR 质量与资源占用 | REQ-010、内容导入 | OCR 未实现，REQ-010 移至后续阶段（RG-003 No-Go）；当前降级为已提取文本 | 待 OCR 引擎定版与资源验证 |
| RISK-P2-AUTH-001 | RG-011/012/013 | ~~浏览器路径（登录/注册页、登出、token 刷新）未实跑；demo 启动验证未做~~ | ~~REQ-040/041/042、前端~~ | ✅ **已解决**（2026-08-07：浏览器 smoke PASS + demo 启动验证通过） | `scripts/smoke-auth-browser.mjs` PASS + `run-sprint16-demo` 启动验证（18000/5173）；`docs/09` §5 记录 |
| RISK-P1-005 | RG-004 | LLM 外部调用可用性 | REQ-008、REQ-036 | GLM `glm-5.2` 真实问答已验证（Sprint-7 首验 + 2026-07-11 新中转 `192.168.15.190:7777/v1` 复测，RG-004→Go；旧 `47.107.134.2` key 已停用）；GPT/ollama 待验证 | GLM 复测记录；GPT/ollama 非 P1 必过 |
| RISK-VISION-001 | 后续 AI Gate | P2 / 愿景高风险 AI 能力 | REQ-020 / 021 / 030 / 032 / 033 等 | 不进入 Phase1 必过项，技术验证通过后再补用例 | 待愿景技术验证 |
| RISK-P2-003 | WSG-001..006 / P2-UI-G-001..006 | P2 UI 少容器清爽稿通用 gate 未形成独立全量 smoke | Sprint-11、TC-P2-WSG-001、TC-P2-UI-001~005、Phase2B | Phase2A 三个 vertical slice 已分别完成浏览器 smoke / build；Sprint-11 仍作为后续 Phase2B UI gate 草案保留，不阻塞 Phase2A closure | Phase2B 启动前重新确认 UI gate 与 smoke 范围 |
| RISK-P2-004 | OI-005 / TC-P2-TAG-001..QUICK-001 | ~~Phase2A 核心 DB / API / TC 契约未实现~~ | ~~REQ-012 / 025 / 026~~ | ✅ **已解决**（REQ-026 / REQ-012 / REQ-025 已完成后端 + 前端 vertical slice；TC-P2-LINK-001 / TAG-001 / QUICK-001 通过） | 2026-07-20 Phase2A closure；§2 矩阵与 §5 验收记录 |
| RISK-P2-005 | RG-004 / 数据外发 / **RG-008** | AI 润色 / 写作引用可能发送真实文档片段到外部 LLM | REQ-014 | **数据外发风险已人工接受（2026-07-30，真实外发 + 权限护栏，见 RG-008 / `ai/project-rules.md §2.1`）**：sources 权限过滤、草稿只存 hash + 摘要、不做自动过滤、5030 降级 | ✅ 已解决 / 关闭：后端权限过滤（越权 chunk 不进 prompt / 不返回）、5030 不落库不编造、hash 留存均经 `tests.backend.test_ai_polish` 验证；RG-008→Go；前端 UI smoke 后续已在 TC-P2-AI-001 通过；D-C-001 citation 同步延迟量化已通过，当前不新增异步 job |
| RISK-P1-006 | RG-006 / TC-P1-017 | ~~PDF 导出库未验证；API-019 未实现~~ | REQ-027 | ✅ **已解决**（2026-08-04）：ReportLab / pypdf / pdfplumber / Pillow 已安装并锁入依赖；`simhei.ttf` 中文样例生成、Poppler 渲染、文本抽取和人工 PNG 检查均通过；Sprint-18 已实现 API-019 + `lumen_doc_exports` + 前端入口，产品样例 PDF 验证通过 | `scripts/smoke-pdf-rg006.py` + `docs/research/2026-08-04-tech-env-evaluation-rg006-pdf-export.md` + TC-P1-017 验收记录 |
| RISK-P1-007 | TC-P1-015 | ~~Sprint-16 Chrome 拖拽 / 文件夹 smoke 未补~~ | ~~REQ-037 前端人工验收~~ | ✅ **已解决**（Chrome headless drop-zone smoke 已覆盖批量 drop、路径标题、搜索与问答来源） | Sprint-16 smoke 记录（2026-07-15） |
| RISK-P1-008 | TC-P1-015 | 1000+ 文件夹导入一次请求可能撞 multipart `max_files/max_fields=1000` 限制 | REQ-037 / REQ-039 大文件夹导入 | ✅ 已解决（编码 PR #104，2026-08-04）：前端分批 50/批顺序上传 + 逐批 try/catch + 汇总，纯前端、不改 API-029；build + 后端 11 测试绿 | ✅ 1000 文件夹 smoke 通过（2026-08-04，内存 demo，非 PG/embedding）：1000 文件 ÷50/批 = 20 批顺序上传，全 HTTP 200、done 997 / skip 3 / fail 0、0.9s；对照「单请求 1000 文件」→ 400 `Too many fields. Maximum number of fields is 1000.`（复现原 bug）。PG + 同步 embedding perf 已测（2026-08-04，scripts/smoke-import-pg-performance.py）：200 文档 ~41s、~205ms/doc（bge embedding 已写入）、每批随 `list_documents` 去重略增（9.9→10.6s/批）；1000+ 文档预计数分钟，大规模 vault 的后台异步导入留后续 |
| RISK-VISION-002 | RG-009 / TC-P2-VAULT-001 | Obsidian Vault 仅本地挂载的浏览器授权、隐私边界、本地索引和同步冲突 | REQ-018 | RG-009 PoC 已通过（2026-08-05，8 能力 + 5 场景）；导入数据库 = 完整 LUMEN 能力；仅本地挂载 = 个人 / 当前设备来源，左侧下层分区显示，不进入团队搜索 / RAG | ✅ 已关闭（2026-08-06）：Sprint-23C 模式 B 编码完成，TC-P2-VAULT-001 通过（CDP + 用户人工 6 口径；本地不上传、不进 RAG） |

| RISK-P2-VAULT-003 | RG-010 / TC-P2-VAULT-003 | 本地挂载文件变更自动监听依赖实验性 FileSystemObserver，目标浏览器可能不支持 | REQ-018 模式 B 增强 | **已 Go（2026-08-06）**：Edge 139 默认可用（CDP 实测构造 / observe / 变更回调）；手动重扫兜底保留 | `docs/research/2026-08-06-tech-env-evaluation-rg010-file-system-observer.md`；TC-P2-VAULT-003 |
## 7. 待人工确认项

- Phase2A 已完成整体验收 closure；不得再把 REQ-026 / REQ-012 / REQ-025 标为待确认或未实现。
- **Phase2B 范围、进入 / 退出标准、数据外发风险接受方式（RG-008）与验证包均已确认 / 就绪（2026-07-30）**；REQ-014 AI 润色已完成并通过 TC-P2-AI-001，D-C-001 citation 同步延迟量化已关闭（当前不做异步 job）；REQ-013a/024 主题时间线已完成并通过自动化验证、运行态 API smoke 与 Edge headless 浏览器 smoke。
- Phase1.5B 的 TC-P1-017 已随 Sprint-18 通过；Word-PDF / zhparser 仍不阻塞 Phase2A closure。
- **Phase2D 收口待确认项定性（2026-08-07 用户确认）**：① Sprint-27 P2 两项（`visible_document_where_clause` 全仓未使用——过滤在 Python 层、无 SQL 下沉；`upsert_link` 标题解析可指向同空间不可见文档——无读泄露）→ **接受**，不阻塞；② Sprint-28 实现偏差（API-044/046 未分页 / `last_login_at` 未显式排序 / API-045/048 无 `updated_at` / refresh 不含 `role`，见 accounts-auth §18.9）→ **接受、留后续**，3-5 人团队验证规模非必要；③ 移除用户 / 重置密码 / 邀请码 / REQ-016 多人实时协作 → 不进 Phase2D（Sprint-28 硬约束），留候选 / 后续。Phase2D 三 slice（Sprint-26/27/28）退出标准全部达成，已收口；不升 Phase，下一阶段范围待用户定义。
- **项目收尾（2026-08-07）**：demo 目标达成，进入维护态；无未决 open items。历史待确认项均已确认 / 收口（Phase2A closure / Phase2B 范围确认 / Phase2D 收口定性）。后续若重启见 `docs/research/2026-08-07-project-closure-summary.md` §6。
