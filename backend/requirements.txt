# Phase1 后端运行依赖（降级基线：仅 Web 层，无 DB/Embedding/OCR/LLM 驱动）。
#
# ⚠️ 版本 drift（见 docs/research/2026-07-09-tech-env-evaluation-phase1-reeval.md §5.1）：
# 本文件锁定 docs/05-tech-spec.md §1 的 Python 3.12 基线（0.115.x / 0.34.x / 2.10.x），
# 该组合在 Python 3.14 下 pydantic-core 源码构建失败（缺 MSVC linker，首评 §9.2）。
# 本机 .venv（Python 3.14.3）实际运行较新版本——fastapi 0.136.3 / uvicorn 0.49.0 /
# pydantic 2.13.4（cp314 wheel 可用，53 后端 tests 通过）。
# 锁定版本与实际版本的对齐待 Phase1 真实化（pgvector/Embedding/OCR/LLM）验证后统一定基线
# （RG-001..004 仍 No-Go，见 05 §5.1）；在此之前不要按本文件锁定版本在 Python 3.14 重建 venv。
fastapi==0.115.14
uvicorn==0.34.3
pydantic==2.10.6
# LLM adapter（RG-004）：OpenAI 兼容封装，支持 GLM/GPT 中转 + 本机 ollama；详见 backend/service/llm_adapter.py
openai>=1.0

