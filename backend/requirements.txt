# Phase1 后端运行依赖（Python 3.14 实测基线，task-008 T7 / PG-C-001 闭合）。
#
# 历史 drift 已解决：原锁 Python 3.12 版本（fastapi 0.115.x / pydantic 2.10.x 等）
# 在本机 Python 3.14 下 pydantic-core 源码构建失败（缺 MSVC linker）；实测可跑的是
# 较新版本（cp314 wheel 可用）。T7 起按 .venv(Python 3.14.3) 实测版本锁定，与
# docs/05-tech-spec.md §1「Python 3.14 运行基线」一致。详见 tech-env 复核报告 §12。
#
# 安装：python3.14 -m venv .venv && .venv/Scripts/pip install -r backend/requirements.txt
# Embedding 的 CPU 版 torch 需走 PyTorch CPU index（见下方 torch 注释）。
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4

# LLM adapter（RG-004，Sprint-7）：OpenAI 兼容封装，支持 GLM/GPT 中转 + 本机 ollama；
# 详见 backend/service/llm_adapter.py。未配置 LUMEN_LLM_* 时 rag.py 走降级（模板）回答。
openai==2.44.0

# pgvector 接入（RG-001，Sprint-8 / task-008 T1–T6）：DB 驱动与 ORM。
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pgvector==0.5.0

# Embedding（RG-002，task-008 T4/T6 接入）：bge-small-zh-v1.5 生成 512 维 float32 向量，
# 写入 lumen_chunks.embedding 供 RAG 向量召回。sentence-transformers 会拉 torch / transformers /
# numpy / tokenizers / scipy / scikit-learn（传递依赖，不显式锁）。
#
# torch：本机验证用 CPU 版 2.13.0+cpu（pip 默认拉 CUDA 版体积大且本 Demo 用 CPU Embedding）。
#   推荐 CPU 安装（不锁 +cpu 本地版本以免普通 pip install 失败）：
#   .venv/Scripts/pip install torch==2.13.0+cpu --index-url https://download.pytorch.org/whl/cpu
#   若已先装 sentence-transformers 拉了 CUDA torch，可 pip install --force-reinstall 上述 CPU 版覆盖。
#
# 关键环境约束（公司网络下必崩，须设）：HF_HUB_DISABLE_XET=1（huggingface_hub 默认走 Xet 协议
# 绕过 HTTP 代理；backend/service/embedding.py 已在 import 前设此变量）。
sentence-transformers==5.6.0

# PDF 导出（RG-006，2026-08-04）：ReportLab + Windows 中文字体已通过中文 PDF 最小样例、
# Poppler 渲染、pdfplumber 文本抽取验证。pypdf/pdfplumber/Pillow 用于导出产物校验与 TC-P1-017。
reportlab==5.0.0
pypdf==6.14.2
pdfplumber==0.11.10
pillow==12.3.0
